# BOntoLink 动态本体管理系统 · 数据形式与规范

| 项 | 内容 |
|---|---|
| 文档名称 | 数据形式与规范（Data Specification） |
| 文档版本 | **V1.0** |
| 编制日期 | 2026-08-03 |
| 阅读对象 | 研发（前端 / 后端）/ 数据提供方 / 测试 / 下游消费方 |
| 对应代码 | `backend/bontolink-data/.../db/migration/`、各 `*Mapper.java`、`frontend/src/api/index.js` |
| 数据快照 | `backend/bontolink.db`（2026-08-03） |

> **状态标注**：✅ 已消费且有演示数据验证　🟡 已消费但有限制／仅部分生效　⬜ 已定义未消费（声明性字段）　🔜 规划中

**与其它文档的分工**

| 文档 | 回答的问题 |
|---|---|
| `需求规格说明书.md` | 要做什么功能、做到什么算合格 |
| `初步方案设计.md` | 怎么实现、为什么这么实现、有什么风险 |
| `使用帮助.html` | 用户怎么操作 |
| **本文** | **数据长什么样、谁给、给错了会怎样** |

四份都不重复。**接数据源、写后端接口、造测试数据、对接下游，看本文。**

---

## 目录

- [1. 数据分层总览](#1-数据分层总览)
- [2. 通用数据规范](#2-通用数据规范)
- [3. L1 归属层：分类与命名空间](#3-l1-归属层分类与命名空间)
- [4. L2 定义层：本体核心](#4-l2-定义层本体核心)
- [5. L3 接入层：数据源与物理映射](#5-l3-接入层数据源与物理映射)
- [6. L4 消费层：对外数据契约](#6-l4-消费层对外数据契约)
- [7. 数据自检清单](#7-数据自检清单)
- [8. 已知数据问题](#8-已知数据问题)
- [9. 附录](#9-附录)

---

## 1. 数据分层总览

### 1.1 四层结构

```
┌─ L1 归属层 ─────────── 所有资源挂在哪、术语作用域是什么 ──────────────┐
│  ont_biz_category      行业 → 领域 → 分组（三层树）                      │
│  ont_biz_namespace     领域绑定的 URI 与版本                             │
│  ont_biz_group(_class) 分组及其成员                                      │
└──────────────────────────┬───────────────────────────────────────────────┘
                           │ category_code 贯穿全库，是最主要的横切维度
                           ▼
┌─ L2 定义层 ─────────── 本体本身：有哪些类、字段、关系、约束 ──────────┐
│  ont_class / ont_class_property        对象类型与属性                     │
│  ont_link_types / ont_link_mappings    链接类型与字段映射                 │
│  ont_class_action + 20 余张子表        动作类型（表单/规则/提交标准）      │
│  ont_shared_properties / struct_*      共享属性与结构属性                 │
│  ont_value_types / ont_enum_*          值类型与枚举字典                   │
│  ont_interface_*                       接口契约与实现关系                 │
│  ont_type_class_*                      类型类元数据标签                   │
└──────────────────────────┬───────────────────────────────────────────────┘
                           │ 属性经 class_ds_id + physical_table/column 落到物理表
                           ▼
┌─ L3 接入层 ─────────── 数据从哪来 ────────────────────────────────────┐
│  sys_data_source        数据库类数据源（12 种）                           │
│  ont_physical_table     同步来的表/视图元数据                             │
│  ont_ext_data_source    外部 HTTP 数据源（9 种鉴权）                      │
│  ont_ext_api_*          接口目录、接口定义、调用日志                      │
│  ont_enum_sync_config   枚举字典的外部同步配置                            │
└──────────────────────────┬───────────────────────────────────────────────┘
                           ▼
┌─ L4 消费层 ─────────── 对外给出什么 ──────────────────────────────────┐
│  R<T> 统一响应包装                                                        │
│  /api/resource/classes/{id}   对象类型聚合详情（下游主入口）              │
│  /api/instance/*              实例数据（当前为确定性模拟）                │
│  /api/tc-render/resolve       类型类渲染解析                              │
│  ont_explore_design           探索看板配置（前端产生、后端只存）           │
└───────────────────────────────────────────────────────────────────────────┘
```

### 1.2 各层责任与现状

| 层 | 主要表 | 谁产生 | 谁消费 | 现状 |
|---|---|---|---|---|
| L1 归属 | `ont_biz_category` `ont_biz_namespace` | 本体管理员在界面上建 | 全部模块的左树过滤 | ✅ 18 节点 / 22 命名空间 |
| L2 定义 | `ont_class` 等约 40 张 | 业务建模师 | 语义层、下游应用 | ✅ 核心数据资产 |
| L3 接入 | `sys_data_source` 等 | 数据接入工程师 | 枚举同步、物理映射 | 🟡 凭证明文存储 |
| L4 消费 | 无独立存储，多为聚合读 | 后端聚合 | 报表 / 问答 / 探索 | 🟡 实例为模拟数据 |

### 1.3 贯穿全文的关键约定

先记住这六条，后面每一章都在展开它们。

1. **一切引用用 `id`，不用 `api_name`，也不用 RID。**
   跨表外键（`class_id`、`enum_id`、`value_type`、`ds_id`…）存的都是目标表的主键 `id`。`api_name` 是给人和下游看的稳定编码，RID 是对外标识符，二者都**不做**内部关联键。
   → 唯一例外与已知不一致见 [8.3](#83-value_type-列的注释与实际存储不符)

2. **`api_name` 一旦创建不可修改。**
   对象类型、接口、值类型的 `api_name` 在界面上锁死。改它会打断所有下游引用，而系统没有引用重写机制。

3. **不依赖数据库外键，引用完整性在 Service 层校验。**
   SQLite 默认关闭外键且行为与 PostgreSQL 有差异；更重要的是删除时要给出**可读的引用清单**而不是约束异常。代价是：直接改库会绕过全部校验。

4. **JSON 列没有数据库层约束。**
   `constraint_config`、`rule_config`、`config`、`param_json`、`auth_config`、`request_params`、`metadata` 全部是 TEXT 存 JSON。写错不会报错，只会在界面上表现为"配了没生效"。**本文第 4、5 章逐个给出它们的结构。**

5. **状态字段有三套口径，不要混。**
   见 [2.4](#24-状态字段的三套口径)。同样叫 `status`，在不同表里可能是整数 `1/0`，也可能是文本 `active/inactive/deprecated`。

6. **时间戳列在两种方言下类型不同，Java 侧禁止传字符串。**
   见 [2.5](#25-时间戳与双方言)。

---

## 2. 通用数据规范

### 2.1 主键与 ID 格式

**格式**：`{前缀}-{后缀}`

| 来源 | 后缀形态 | 示例 |
|---|---|---|
| 运行期创建 | `UUID.randomUUID()` | `class-a1b2c3d4-…` |
| 种子数据 | 可读短标识 | `class-wsc-01`、`enum-types-area-district`、`cp-1` |

> ⚠️ **代码禁止解析 ID 后缀**。种子数据的后缀是可读 slug 而非 UUID，任何"按 UUID 格式校验 ID"或"从 ID 里截取业务含义"的逻辑都会在种子数据上失败。ID 只做等值比较。

全部前缀清单见 [9.2](#92-id-前缀速查52-个)。

**校验**

- ❌ 手工插入数据时 ID 不带前缀 → 日志与排障时无法从 ID 判断资源类型；部分前端逻辑按前缀分派会失效。
- ❌ 前缀写错（如给链接类型用 `class-`）→ 不报错，但全局搜索与图谱的类型归类会错。

### 2.2 编码 `api_name`

各模块的编码字段名与格式**不统一**，这是历史原因，必须按模块查表：

| 模块 | 字段名 | 格式 | 示例 |
|---|---|---|---|
| 行业分类 | `category_code` | `ind_*` / `dom_*` / `grp_*` | `ind_water_resource` |
| 对象类型 | `api_name` | `PascalCase` | `Reservoir` |
| 共享属性 | `prop_code` | `snake_case` | `start_time` |
| 值类型 | `api_name` | `snake_case` / 缩写大写 | `text`、`DJZCLXJDM` |
| 枚举类型 | `api_name` | `snake_case` | `area_district` |
| 接口 | `api_name` | `PascalCase` | `IMonitoring` |
| 链接类型 | `link_type_code` | `snake_case` | `reservoir_to_dam` |
| 结构属性 | `struct_code` | `PascalCase` | `FullName` |
| 数据源 | `ds_code` | `ds_*` | `ds_mysql_prod` |
| 动作类型 | `api_name` | `^[a-z][a-z0-9_]*$` | `create_monitor_plot` |

> 完整规则、决策树、冲突检测命令见 **`API_NAME_CODING_STANDARD.md`** 与 **`API_NAME_CHEATSHEET.md`**，本文不重复。

**禁止**：大写字母（对象类/接口/结构属性除外）、中文、中划线、空格、过度缩写、拼音。

**校验**

- ❌ `api_name` 全局重复 → 唯一索引报错（`ont_class.api_name` 有 UNIQUE）；未加唯一约束的表则静默共存，下游按名取数时取到任意一条。
- ❌ 创建后修改 → 界面已锁死；若绕过界面直接改库，所有引用该编码的下游配置（报表、问答映射）全部失效且无提示。

### 2.3 RID

格式：`ri.ont.{module}.{code}`，如 `ri.ont.value.types.text`、`ri.ont.interface.…`。

**用途**：对外资源标识符，供跨系统引用。**不作为库内关联键**（见 [1.3](#13-贯穿全文的关键约定) 第 1 条）。

**校验**

- ⚠️ RID 为空不影响系统运行，但下游若按 RID 索引会取不到。
- ⚠️ `ont_value_types.rid` 有 `NOT NULL UNIQUE` 约束，其余表的 `rid` 多为可空。

### 2.4 状态字段的三套口径

同名 `status`，三种取值体系。**这是最容易出错的地方**：

| 口径 | 取值 | 用在哪些表 |
|---|---|---|
| **整数** | `1` 启用 / `0` 禁用 | `ont_class`、`ont_class_property`、`ont_interface`、`ont_shared_properties`、`ont_value_types`、`ont_biz_category`、`sys_data_source`、`ont_ext_*`、`ont_dict_def` |
| **文本·三态** | `experimental` / `active` / `deprecated` | `ont_link_types` |
| **文本·二态** | `active` / `inactive` | `ont_enum_types`、`ont_enum_items` |
| **整数·三态** | `0` 草稿 / `1` 已发布 / `2` 已停用 | `ont_class_action` |

另有独立语义的状态列：

| 列 | 取值 |
|---|---|
| `sys_data_source.connect_status` | `online` / `offline` / `risk` |
| `ont_ext_api_interface.api_status` | `debug` 调试 / `done` 完成 |
| `ont_ext_api_call_log.call_status` | `1` 成功 / `2` 失败 / `3` 超时 / `4` 鉴权失败 |
| `ont_type_class.is_deprecated` | `0` / `1` |
| `ont_type_class_bind.bind_deprecated` | `0` / `1`（单条绑定临时弃用） |

**校验**

- ❌ 给 `ont_link_types.status` 写 `1` → 列表的状态筛选（按 `active` 等文本匹配）查不到这条记录，界面上"凭空消失"。
- ❌ 给 `ont_enum_items.status` 写 `1` → 同上，枚举项在选择器里不出现。
- ❌ 给 `ont_class_action.status` 写 `active` → 状态标签渲染为空，发布/停用按钮行为异常。

### 2.5 时间戳与双方言

| 项 | SQLite | PostgreSQL |
|---|---|---|
| 列类型 | `TEXT` | `TIMESTAMP`（无时区） |
| 默认值 | `datetime('now','localtime')` | `CURRENT_TIMESTAMP` |
| 字符串写入 | 接受 `'2026-07-30 10:00:00'` | **拒绝隐式转换** |

**列名也不统一**：多数表用 `create_time` / `update_time`，类型类与部分新表用 `created_at` / `updated_at`。写通用逻辑时不能假定列名。

**Java 侧铁律**（二选一）：

```java
// ✅ 方案 A（推荐）：SQL 里直接写，不从 Java 传
@Update("UPDATE ont_class SET display_name=#{name}, update_time=CURRENT_TIMESTAMP WHERE id=#{id}")

// ✅ 方案 B：传 java.sql.Timestamp 对象，MyBatis 自动适配两方言
row.put("updatedAt", new Timestamp(System.currentTimeMillis()));

// ❌ 禁止：传字符串 —— SQLite 通过，PostgreSQL 报字段类型错误
row.put("updatedAt", "2026-07-30 10:00:00");
```

**校验**

- ❌ 传字符串给 PG 的 TIMESTAMP 列 → 运行期报类型错误，且**只在 PG 环境复现**，SQLite 开发环境测不出来。

### 2.6 JSON 列的通用约定

| 约定 | 说明 |
|---|---|
| 存储类型 | 一律 `TEXT`（不用 PG 的 `jsonb`，为兼容 SQLite） |
| 序列化 | 后端用 **FastJSON2**（`com.alibaba.fastjson2`）；禁止 fastjson v1 |
| 空值 | 允许 `NULL` 与空串 `''`，消费方必须两者都容错 |
| 未知键 | 反序列化时忽略，不报错——便于前端先行加字段 |
| 检索 | **无法用 SQL 检索内部字段**。现有的规则维度计数只能 `LIKE '%"kind":"xxx"%'` 兜底（见 [8.1](#81-rule_config-是全部规则类型的扁平并集)） |

**校验**

- ❌ JSON 语法错误 → 反序列化抛异常，通常被吞成"该项配置为空"，界面表现为**配了但没生效**，且无错误提示。
- ⚠️ JSON 里带 `undefined`（前端直接 `JSON.stringify` 含 undefined 的对象会丢键）→ 后端读到缺键，按默认值处理。

### 2.7 布尔与空值语义

| 项 | 约定 |
|---|---|
| 布尔列 | 一律 `INTEGER` 存 `0`/`1`，不用 `BOOLEAN`（SQLite 无原生布尔） |
| 前端传值 | 允许 `true/false`，MyBatis 自动转 `1/0` |
| 空字符串 vs NULL | **多数场景等价对待**，消费方需同时判 `null` 与 `''` |
| 顶级父节点 | 用 `'0'` 表示无父级（`ont_biz_category.parent_id`、`ont_ext_api_group.parent_id`），**不是 NULL** |
| 未分组 | `ont_ext_api_interface.group_id` 用 `'0'` |

**校验**

- ❌ 把顶级节点的 `parent_id` 写成 `NULL` 而非 `'0'` → 树装配时该节点及其子树整体丢失。
- ⚠️ 布尔列写 `'true'` 字符串 → SQLite 存进去了但比较 `=1` 不命中，功能静默失效。

---

## 3. L1 归属层：分类与命名空间

### 3.1 `ont_biz_category` — 三层分类树

| 列 | 类型 | 必填 | 说明 | 状态 |
|---|---|---|---|---|
| `id` | TEXT | **必** | `category-{UUID}`；分组节点用 `group-{UUID}` | ✅ |
| `parent_id` | TEXT | **必** | 自关联；**顶级填 `'0'` 不是 NULL** | ✅ |
| `category_code` | TEXT | **必** | **UNIQUE**，小写下划线，`ind_*`/`dom_*`/`grp_*` | ✅ |
| `category_type` | INT | **必** | `1` 行业 / `2` 领域 / `3` 分组 | ✅ |
| `ns_code` | TEXT | 选 | 绑定的命名空间，**仅领域节点有意义** | ✅ |
| `sort` | INT | 选 | 同级排序号 | ✅ |
| `icon` / `color` | TEXT | 选 | 显示配置，带到各处列表与卡片 | ✅ |
| `status` | INT | 选 | `1` 启用 / `0` 禁用 | ✅ |
| `rdfs_label` / `rdfs_comment` / `rdfs_see_also` / `rdfs_defined_by` | TEXT | 选 | RDF 语义字段 | ⬜ |

**`category_code` 是全库最重要的横切维度**：几乎每张 L2 表都有 `category_code` 列，六个资源模块的左树过滤全靠它。

**校验**

- ❌ `category_code` 重复 → UNIQUE 约束报错。
- ❌ `category_type` 与实际层级不符（如领域节点标成 `1`）→ 树能展开，但按类型统计与"新建子节点"的类型推断全错。
- ❌ 资源的 `category_code` 指向不存在的节点 → 该资源在左树任何节点下都点不出来，只能在"全部"里看到。
- ❌ 顶级节点 `parent_id` 写 NULL → 整棵子树在树装配时丢失。

### 3.2 `ont_biz_namespace` — 命名空间

| 列 | 必填 | 说明 |
|---|---|---|
| `id` | **必** | `namespace-{UUID}` |
| `ns_code` | **必** | **UNIQUE** |
| `ns_name` / `ns_uri` | 建议 | 名称 / URI |
| `curr_version` | 选 | 默认 `'1.0'` |
| `hierarchy_path` | 选 | 层次路径 |
| `status` | 选 | `0`/`1` |

配套 `ont_biz_namespace_version`（`namespace-v-{UUID}`）记录版本历史。🟡 有表有列表，版本切换与差异比对未实现。

### 3.3 `ont_biz_group` / `ont_biz_group_class` — 分组与成员

分组成员表 `ont_biz_group_class` 用 `group_type` 区分成员种类（如 `'shared_props'`），实现"同一张关联表服务多种资源"。

**校验**

- ❌ 加成员时漏填 `group_type` → 成员在对应模块的分组视图里不出现。

---

## 4. L2 定义层：本体核心

### 4.1 `ont_class` — 对象类型

```
id, rid, api_name(UNIQUE), ns_code, category_code,
display_name, rdfs_*, description, icon, color, status, metadata,
parent_class_id,
class_expr_type, class_expr_content, is_thing, is_nothing, is_common,
create_time, update_time
```

| 列 | 必填 | 说明 | 状态 |
|---|---|---|---|
| `api_name` | **必** | **UNIQUE**，PascalCase，**创建后不可改** | ✅ |
| `category_code` | **必** | 缺失则左树过滤不到 | ✅ |
| `display_name` | 建议 | 界面显示名；缺失时退化显示 `api_name` | ✅ |
| `parent_class_id` | 选 | 类层级（rdfs:subClassOf） | 🟡 存得下，无继承推理 |
| `class_expr_type` | 选 | `NULL`/`0` 普通；`union`/`intersection`/`complement`/`enumeration` | 🟡 无表达式求值 |
| `class_expr_content` | 选 | 类表达式内容 JSON | 🟡 |
| `is_thing` / `is_nothing` / `is_common` | 选 | 特殊类标记，默认 `0` | ⬜ |
| `icon` / `color` | 建议 | 带到列表、卡片、图谱、探索全部视图 | ✅ |
| `metadata` | 选 | 业务扩展 JSON | ⬜ |

**校验**

- ❌ `api_name` 重复 → UNIQUE 报错。
- ❌ 无任何属性（`ont_class_property` 为空）→ 实例探索生成不出实例，该类在探索里点开是空的。
- ❌ `status=0` → 实例探索按启用过滤，该类不出现在可选列表里。
- ⚠️ `parent_class_id` 指向自己或形成环 → 无校验，层级展示会死循环。

### 4.2 `ont_class_property` — 属性（重点）

字段按四组理解：

#### 组一：基本

| 列 | 必填 | 说明 |
|---|---|---|
| `class_id` | **必** | 所属类 `ont_class.id` |
| `api_name` | **必** | **类内唯一** |
| `prop_code` | 建议 | 属性编码 camelCase |
| `prop_type` | **必** | `data` / `object` / `annotation` / `struct`，默认 `data` |
| `data_type` | 建议 | XSD 类型，如 `xsd:string`；**对象属性留空** |
| `display_name` | 建议 | 显示名 |
| `sort` | 选 | 排序，拖拽调整；影响所有展示该属性的地方 |
| `status` | 选 | `1`/`0` |

#### 组二：语义约束

| 列 | 说明 | 状态 |
|---|---|---|
| `value_type` | 值类型引用。**实际存 `ont_value_types.id`**（如 `value-types-text`），DDL 注释写的"RID"是错的，见 [8.3](#83-value_type-列的注释与实际存储不符) | ✅ |
| `range_class_id` | 对象属性的值域类 ID，`prop_type='object'` 时必填 | ✅ |
| `sub_property_of` | 父属性 ID（rdfs:subPropertyOf） | 🟡 |
| `xsd_min_length` / `xsd_max_length` / `xsd_length` / `xsd_pattern` / `xsd_min_inclusive` / `xsd_max_inclusive` | 6 个 XSD 约束 | ⬜ 存得下，无运行期校验 |
| `owl_functional` / `owl_inverse_functional` / `owl_transitive` / `owl_symmetric` / `owl_asymmetric` / `owl_reflexive` / `owl_irreflexive` | 7 个 OWL 特性，`INTEGER` 0/1 | ⬜ 供推理引擎，界面不消费 |

#### 组三：业务标记

`is_primary` / `is_required` / `is_key` / `is_derived` / `is_multi_valued_prop` / `is_range_constraint_prop`，均为 `INTEGER` 默认 `0`。

#### 组四：物理映射

| 列 | 说明 |
|---|---|
| `class_ds_id` | 来源数据集，对应 `ont_class_ds.id`（主表 `main` / 附表 `s1`、`s2`） |
| `physical_table` | 物理表名 |
| `physical_column` | 物理列名 |

**校验**

- ❌ `api_name` 在同一类内重复 → 无唯一约束，两条并存；下游按名取数取到任意一条。
- ❌ `prop_type='object'` 但 `range_class_id` 为空 → 该属性不知道指向哪个类，图谱与跨对象取数失效。
- ❌ `value_type` 填了 RID 而非 id → 关联查不到，界面上值类型列显示为空。
- ❌ 绑了 `class_ds_id` 但 `physical_table`/`physical_column` 为空 → 映射不完整，取数时该列取不到值。
- ⚠️ 多个 `is_primary=1` → 无约束，主键语义歧义。

### 4.3 `ont_link_types` + `ont_link_mappings` — 链接类型

#### 主表关键列

| 列 | 必填 | 说明 |
|---|---|---|
| `id` | **必** | `link-types-{UUID}` |
| `link_type_code` | **必** | **UNIQUE**，snake_case |
| `l_object_type_id` / `r_object_type_id` | **必** | 源端 / 目标端 `ont_class.id` |
| `l_cardinality` / `r_cardinality` | **必** | `one` / `many` |
| `l_display_name` / `r_display_name` | 建议 | 正向 / 反向显示名 |
| `l_plural_name` / `r_plural_name` | 选 | 复数名，基数 `many` 时启用 |
| `l_api_name` / `r_api_name` | 建议 | camelCase |
| `l_visibility` / `r_visibility` | 选 | `normal` / `prominent` / `hidden` |
| `l_enabled` / `r_enabled` | 选 | `0`/`1`，可只启用单向 |
| `is_data_source_rel` | **必** | `0` 字段映射 / `1` 中间表 |
| `rel_data_table` | 条件必填 | `is_data_source_rel=1` 时的中间表名 |
| `status` | **必** | `experimental` / `active` / `deprecated` |

#### 映射表 `ont_link_mappings`

| 列 | 说明 |
|---|---|
| `mapping_id` | `link-mappings-{UUID}` |
| `link_id` | 关联主表 |
| `side` | `left` / `right` |
| `seq` | 复合键序号，**1-based** |
| `object_field` | 对象属性名 |
| `join_table_column` | 中间表列名（`is_data_source_rel=1` 时用） |

> **写入语义**：`PUT /api/link-types/{id}` 对 mappings 做**整体覆盖**（先按 `link_id` 全删再插）。提交时必须携带全部映射行，只传改动行会导致其余行被删除。

**校验**

- ❌ `is_data_source_rel=1` 但 `rel_data_table` 为空 → 关系无处落地，取数失败。
- ❌ `is_data_source_rel=1` 但映射行的 `join_table_column` 为空 → 同上。
- ❌ 复合键的 `seq` 从 0 开始或有重复 → 键序错乱，join 结果错误。
- ❌ `l_object_type_id` 指向不存在的类 → 列表 JOIN 不到显示名，该行显示为空白源实体。

### 4.4 `ont_shared_properties` / `ont_struct_types` / `ont_struct_items`

共享属性的字段集与 `ont_class_property` 的组二、组三**高度重合**（同样的 7 个 OWL + 6 个 XSD + 4 个 RDFS），差别在于：

| 项 | 共享属性 | 类属性 |
|---|---|---|
| 编码列 | `prop_code`（**全局 UNIQUE**） | `api_name`（类内唯一） |
| 归属 | 不属于任何类 | `class_id` 必填 |
| 物理映射 | 无 | 有 |
| 引用计数 | 有（`ref_count` 由子查询算出） | 无 |

**结构属性**：`ont_struct_types`（`struct-types-{UUID}`，`struct_code` UNIQUE）+ `ont_struct_items`（`struct-items-{UUID}`，`struct_id` + `prop_id` + `sort_no`）构成 M:N 关联。

**校验**

- ❌ 删除 `ref_count > 0` 的共享属性 → 接口返回 400；批量删除会跳过并回报清单。
- ❌ `ont_struct_items.prop_id` 指向已删除的共享属性 → 结构条目表显示空行。
- ⚠️ **改共享属性不会回写到已引用它的类属性**——这是设计决策不是缺陷，见《初步方案设计》2.3.3。

### 4.5 `ont_value_types` — 值类型与 `constraint_config` ★

| 列 | 必填 | 说明 |
|---|---|---|
| `id` | **必** | `value-types-{UUID}` |
| `rid` | **必** | **NOT NULL UNIQUE** |
| `api_name` | **必** | **UNIQUE**，创建后不可改 |
| `base_type` | **必** | `String` / `Integer` / `Decimal` / `Boolean` / `DateTime`，**保存后不可改** |
| `constraint_type` | **必** | `RID` / `UUID` / `Length` / `Regex` / `Enum` |
| `constraint_config` | 条件 | 约束参数 JSON，**Enum 类型不用此列** |
| `enum_id` | 条件 | `constraint_type='Enum'` 时关联 `ont_enum_types.id` |
| `default_usage_config_id` | 选 | 关联 `ont_valuetypes_usage_config.id` |

#### `constraint_config` 的五种形态

```jsonc
// RID —— 无参数
{}

// UUID —— 无参数
{}

// Length —— 长度上下限
{ "min": 0, "max": 255 }

// Regex —— 正则（注意 JSON 里反斜杠要转义）
{ "pattern": "^1[3-9]\\d{9}$" }

// Enum —— 本列为 NULL，配置在 enum_id + default_usage_config_id 两列上
null
```

#### 使用配置 `ont_valuetypes_usage_config`

| 列 | 取值 | 说明 |
|---|---|---|
| `max_select_level` | INT | 最多能选到第几级 |
| `allow_non_leaf` | 0/1 | 是否允许选中间层节点 |
| `display_format` | `label` / `code` / `code_label` / `full_label` | 显示格式 |
| `is_system_default` | 0/1 | 系统默认配置 |

**校验**

- ❌ `constraint_type='Regex'` 但 `constraint_config` 无 `pattern` 键 → 预览验证页恒通过，约束形同虚设。
- ❌ 正则里反斜杠未转义（写 `^1[3-9]\d{9}$` 而非 `\\d`）→ JSON 解析后正则失真，匹配行为与预期不符。
- ❌ `constraint_type='Enum'` 但 `enum_id` 为空 → 值域为空，选择器打开是空列表。
- ❌ `constraint_type='Length'` 且 `min > max` → 无校验，任何值都不通过。
- ⚠️ 改 `base_type` → 界面已锁死；绕过界面改会让已按旧类型落地的属性数据语义错乱。

### 4.6 `ont_enum_types` / `ont_enum_items` / `ont_enum_level_code_rule`

#### 枚举类型主表

| 列 | 必填 | 说明 |
|---|---|---|
| `api_name` | **必** | **UNIQUE**，snake_case |
| `enum_type` | **必** | `general_single` / `general_multi` / `biz_single` / `biz_multi` |
| `max_level` | **必** | 最大层级，默认 `1` |
| `top_code` | 选 | 顶级编码 |
| `status` | **必** | **文本** `active` / `inactive` |

#### 枚举项 `ont_enum_items`

| 列 | 必填 | 说明 |
|---|---|---|
| `code` | **必** | 枚举项编码，同枚举内唯一 |
| `label` | **必** | 显示名 |
| `parent_code` | 条件 | 多级枚举必填；**顶级为 NULL**（注意与分类树的 `'0'` 约定不同） |
| `level` | **必** | 层级，1-based |
| `sort_num` | 选 | 同级排序 |
| `status` | **必** | **文本** `active` / `inactive` |

真实样例（行政区划三级）：

```
{code:'110000', label:'北京市',   parent_code:null,     level:1, sort_num:1, status:'active'}
{code:'110100', label:'市辖区',   parent_code:'110000', level:2, sort_num:1, status:'active'}
{code:'110101', label:'东城区',   parent_code:'110100', level:3, sort_num:1, status:'active'}
```

#### 层次编码规则 `ont_enum_level_code_rule`

| 列 | 说明 |
|---|---|
| `code_name` | 该级码段名 |
| `rule_level` | 层级 |
| `code_len` | 本级码长 |
| `total_len` | 累计总长 |
| `code_separator` | 级间分隔符，默认 `''` |
| `fill_char` | 填充字符，默认 `'0'` |
| `fill_pos` | **`0` = 前补 / `1` = 后补** |

**校验**

- ❌ `parent_code` 指向不存在的 code → 该项及其子树在树视图里挂不上，整支消失。
- ❌ `level` 与 `parent_code` 的实际深度不一致 → 层级渲染错乱，`max_select_level` 截断到错误的层。
- ❌ `status` 写 `1` 而非 `'active'` → 该项在所有选择器里不出现（见 [2.4](#24-状态字段的三套口径)）。
- ❌ `parent_code` 形成环 → 无校验，树装配死循环。
- ⚠️ `code_len` 之和与 `total_len` 对不上 → 编码演算示例显示异常，但不阻断保存。

### 4.7 接口三表

| 表 | ID 前缀 | 关键列 |
|---|---|---|
| `ont_interface` | `interface-` | `api_name`(UNIQUE，不可改)、`interface_code`、`ns_code`、`category_code`、`icon`、`color`、`status`(1/0)、`metadata` |
| `ont_interface_property` | `interface-pro-` | `interface_id`、`api_name`、`prop_code`、`data_type`、`value_type`、**`is_required`：`0` 可选 / `1` 必填 / `2` 多值** |
| `ont_interface_class` | `interface-class-` | `interface_id` + `class_id`（实现关系） |

> ⚠️ `is_required` 在这张表是**三态**，不是布尔。

**校验**

- ❌ 把 `is_required` 当布尔写 → `2`（多值）语义丢失。
- ❌ `ont_interface_class` 同一 `(interface_id, class_id)` 重复插入 → 服务层做了幂等跳过；直接改库则产生重复行，实现数统计翻倍。

### 4.8 `ont_type_class` — 类型类与 `param_json` ★

#### 主表关键列

| 列 | 必填 | 说明 |
|---|---|---|
| `id` | **必** | `type-class-{UUID}` |
| `category_code` | **必** | 关联 `ont_type_class_category_dict` |
| `name_prefix` | **必** | **NOT NULL**，与 `category_code` 组成 **UNIQUE** |
| `name_cn_base` | **必** | 基础中文名 |
| `name_template` | 选 | 模板表达式 |
| `source_type` | 选 | 默认 `platform_built` |
| `allow_apply_types` | **必** | JSON 白名单数组：`["property","relation","action"]` |
| `allow_multi_bind` | 选 | 0/1 |
| `is_array_value` | 选 | 0/1 |
| `system_protected` | 选 | 0/1，**为 1 时不可删、关键字段不可改** |
| `param_type` | **必** | `boolean`/`rid`/`enum`/`text`/`numeric`/`json` |
| `frontend_component` | **必** | `switch`/`single_select`/`multi_select`/`text_input`/`number_input`/`json_editor`/`rid_selector` |
| `param_options_json` | 条件 | `param_type='enum'` 时的选项数组 |
| `param_validator_json` | 选 | 校验规则 |
| `param_json` | 条件 | `param_type='json'` 时的字段 Schema |
| `sort_weight` | 选 | 排序权重 |

#### `param_options_json` —— 枚举选项

```jsonc
[
  { "value": "simple",   "label_cn": "简单分词器" },
  { "value": "standard", "label_cn": "标准分词器" },
  { "value": "japanese", "label_cn": "日语分词器" }
]
```

#### `param_json` —— 多字段参数 Schema

键是字段名，值是该字段的定义。抽屉右栏据此**自动生成表单并实时预览**。

```jsonc
{
  "default_color": {
    "type": "string",
    "default": "",
    "description": "曲线默认十六进制颜色值;为空时系统自动分配配色"
  },
  "line_type": {
    "type": "enum",
    "default": "solid",
    "enum": ["solid", "dashed", "dotted"],
    "description": "曲线展示线型,用于区分主辅指标层级"
  },
  "axis_position": {
    "type": "enum",
    "default": "left",
    "enum": ["left", "right"],
    "description": "度量字段归属 Y 轴,解决多指标量级差异过大的同图展示问题"
  }
}
```

字段定义支持的键：

| 键 | 类型 | 说明 |
|---|---|---|
| `type` | string | `string` / `number` / `boolean` / `enum` |
| `default` | any | 默认值 |
| `enum` | array | `type='enum'` 时的候选值 |
| `required` | bool | 是否必填 |
| `description` | string | 说明，渲染为表单字段的提示文字 |

#### 绑定实例 `ont_type_class_bind`

| 列 | 说明 |
|---|---|
| `id` | `bind-{UUID}` |
| `env` | 默认 `prod` |
| `type_class_meta_id` | 关联 `ont_type_class.id` |
| `applicable_type` | `property` / `relation` / `action` |
| `property_owner_type` / `property_owner_id` / `property_id` | 仅属性绑定时使用 |
| `link_type_id` / `action_type_id` | 仅关系 / 动作绑定时使用 |
| `value` | 绑定的参数值（多字段时为 JSON） |
| `bind_deprecated` | 0/1，单条绑定临时弃用 |

**校验**

- ❌ `(category_code, name_prefix)` 重复 → UNIQUE 报错。
- ❌ `param_type='enum'` 但 `param_options_json` 为空 → 选择器无候选项。
- ❌ `param_json` 语法错误 → 右栏预览表单空白，且无错误提示。
- ❌ `allow_apply_types` 不是合法 JSON 数组 → 绑定时的适用类型判定失败，该类型类在所有载体上都绑不上。
- ❌ 删除 `system_protected=1` 的类型类 → 接口拒绝。
- ⚠️ `value` 与 `param_type` 不匹配（如 boolean 参数存了字符串）→ 渲染解析时按原样透出，上层解释可能出错。

### 4.9 动作类型表族 ★

主表 `ont_class_action` + 20 余张子表。这里只讲**两个 JSON 列**，表结构清单见 [9.1](#91-全表清单按层分组)。

#### 4.9.1 `ont_class_action_rule.rule_config`

规则表本身的列很少：

```
id, action_id, action_type, rule_type, rule_name,
target_param_code, link_type_code, sort, status, rule_config
```

| 列 | 说明 |
|---|---|
| `rule_type` | `1` 本体编辑类 / `2` 副作用类 |
| `sort` | **执行顺序，语义关键**——规则按此顺序依次生效 |
| `status` | `1` 启用 / `0` 停用 |
| `rule_config` | **全部类型专属配置的 JSON** |

> ⚠️ **重要**：`rule_config` 当前是**全部 8 种规则类型字段的扁平并集**（实测共 48 个键），无论规则是什么 `kind`，所有键都会写进去。详见 [8.1](#81-rule_config-是全部规则类型的扁平并集)。**消费方必须先读 `kind`，再只取该 kind 相关的键，忽略其余。**

**判别键**：

```jsonc
{ "kind": "create_object" }
```

`kind` 的 8 种取值与 `rule_type` 的对应：

| `kind` | 中文 | `rule_type` |
|---|---|---|
| `create_object` | 创建对象 | 1 |
| `modify_object` | 修改对象 | 1 |
| `delete_object` | 删除对象 | 1 |
| `create_link` | 创建链接 | 1 |
| `delete_link` | 删除链接 | 1 |
| `function` | 运行函数 | 1 |
| `notification` | 通知 | 2 |
| `webhook` | Webhook | 2 |

**各 kind 实际使用的键**：

| kind | 相关键 |
|---|---|
| `create_object` / `modify_object` / `delete_object` | `obj_class_id`、`obj_pk_property`、`obj_links[]`（`{link_type_code, peer_param}`） |
| `create_link` / `delete_link` | `link_src_param`、`link_dst_param`、`link_type_code`（在主表列上） |
| `function` | `func_code`、`func_version`、`func_autoupgrade`、`func_params[]`、`func_exec_identity`、`func_error_strategy`、`func_timeout`、`func_retry`、`func_concurrent`、`func_return_attachment`、`func_exceptions[]` |
| `notification` | `notify_title`、`notify_content`、`notify_to`、`notify_recipient_source`、`notify_recipient_object_param`、`notify_recipient_user_attr`、`notify_recipient_field`、`notify_content_mode`、`notify_func_code`、`notify_ch_push`、`notify_ch_email`、`notify_ch_sms`、`notify_link_*`、`notify_custom_html`、`notify_html_content`、`notify_permission_scope`、`notify_channel`(旧单选，已迁移) |
| `webhook` | `wh_code`、`wh_version`、`wh_subtype`、`wh_method`、`wh_url`、`wh_timing`、`wh_body`、`wh_params[]`、`wh_input_mode`、`wh_input_func`、`wh_input_func_version` |

`func_params[]` 单项结构（函数入参映射）：

```jsonc
{
  "name": "station_code",
  "param_type": "string",
  "required": 1,
  "value_source": 1,          // 见下方「值来源」
  "value_content": "stationCode"
}
```

**值来源 `value_source` 编码**（同样用在 `ont_action_rule_property_mapping`）：

| 值 | 含义 | `value_content` 存什么 |
|---|---|---|
| `1` | 来自表单参数 | 参数 `param_code` |
| `2` | 静态值 | 字面量 |
| `3` | 当前用户 | 空 |
| `4` | 系统时间 | 空 |
| `5` | 关联对象属性 | 属性标识 |

#### 4.9.2 `ont_action_form_param.config`

表列：`id, action_id, section_id, param_code, param_name, param_type, data_type, is_required, is_multi, default_value, placeholder, sort, config`

`param_type` 取值：`string` / `number` / `boolean` / `object` / `enum`

`config` 的完整结构（真实样例，键固定齐全）：

```jsonc
{
  "value_source": 1,               // 值来源
  "property_code": "stationCode",  // 对应主体对象的属性
  "section": "基础参数",            // 冗余的分区名（权威在 section_id）
  "visible": 1,
  "disabled": 0,
  "display_type": "input",         // input/number/select/radio/checkbox/date/textarea…
  "placeholder": "如 HS-0001",
  "help_text": "",
  "allow_multi": 0,
  "input_mode": "input",

  // —— 约束设置 ——
  "min_length_on": 1, "min_length": 7,
  "max_length_on": 1, "max_length": 16,
  "regex_on": 1,      "regex": "^HS-\\d{4}$",

  // —— 选项来源 ——
  "options": [],
  "allow_other": 0,
  "option_source": "manual",       // manual / objset

  // —— 从对象集获取 ——
  "objset": {
    "class_id": "",                // 起始对象类型
    "set_var": "",                 // 或引用表单里的对象参数作变量
    "filters": [],                 // [{property_code, operator, value}] 起始对象自身的属性过滤
    "links": [                     // 关联搜索：链式多跳，第 N 跳从第 N-1 跳的对端对象继续往外关联
      {
        "link_type_code": "",      // 本跳链接类型（候选只列与本跳起点对象两端相连的链接）
        "filters": []              // [{property_code, operator, value}] 本跳对端对象的属性过滤
      }
    ],
    "label_prop": "",              // 返回属性，逗号分隔多个属性编码；顺序即卡片槽位（第 1 个作标题，依次向下）
    "label_card": "title_sub"      // 下拉候选卡片模版：title_sub / title_tags / title_sub_tags / inline
  },

  // —— 默认值 ——
  "default_enabled": 0,
  "default_type": "static",        // static / obj_param
  "default_obj_param": "",         // 两级级联第一级：对象参数
  "default_obj_prop": "",          // 第二级：该参数所指类的属性，逗号分隔多个，顺序即拼接/卡片槽位
  "default_obj_card": "inline",    // 多属性回填的呈现模版，取值同 objset.label_card

  // —— 覆盖规则 ——
  "overrides": [],
  "dsp": {}
}
```

`objset.links[]` 兼容说明：早期单跳配置写的是 `link_on` / `link_type_code` / `link_filters` 三个平铺字段，前端读取时自动并成 `links[0]`，保存后统一为 `links[]`，旧字段不再写出。链上每一跳的起点 = 上一跳的对端对象，改中间某跳或删除某跳会连带截断其后续跳。

`overrides[]` 单项结构（条件 → 动作）：

```jsonc
{
  "cond": {
    "logic": "all",                // all / any
    "children": [
      { "type": "cond", "subject": "param", "field": "stationCode",
        "fieldName": "测站编码", "dataType": "string",
        "operator": "startsWith", "value": "HS-" }
    ]
  },
  "actions": [ { "type": "visible", "value": 1 } ]
}
```

#### 4.9.3 提交标准

`ont_action_submit_standard_config` + `ont_action_submit_condition_node`（`submit_node-{UUID}`）构成嵌套条件树，节点用 `all` / `any` 分组。

**校验（动作类）**

- ❌ `rule_config` 缺 `kind` 键 → 前端 `normalizeRule` 无法判定规则类型，卡片渲染为空白，实测演示库中已有一条这样的历史数据。
- ❌ `sort` 有重复值 → 执行顺序不确定，冲突校验（删除后修改、副作用前置）可能误判。
- ❌ `create_link` 的 `link_src_param` / `link_dst_param` 指向不存在的表单参数 → 试运行时值来源解析为空，链接建不出来。
- ❌ `config.regex` 里反斜杠未双写 → 正则失真。
- ❌ `objset.class_id` 与 `default_obj_prop` 所属类不一致 → 默认值取不到，界面显示黄条提示回退到动作主体对象。
- ❌ `objset.links[i].link_type_code` 的两端都不含第 i 跳的起点对象 → 该跳解析不出对端对象，其后续跳全部失效，界面下拉置灰提示「请先完成上一跳」。
- ⚠️ `config.section` 与 `section_id` 指向的分区名不一致 → 以 `section_id` 为准，`config.section` 是冗余显示字段。

---

## 5. L3 接入层：数据源与物理映射

### 5.1 `sys_data_source` — 数据库类数据源

| 列 | 必填 | 说明 |
|---|---|---|
| `id` | **必** | `datasource-{UUID}` |
| `ds_code` / `ds_name` | **必** | 编码 / 名称 |
| `ds_type` | **必** | `mysql`/`postgresql`/`oracle`/`mongodb`/`dm`/`kingbase`/`oscar`/`gbase`/`polardb`/`tdsql`/`gaussdb`/`oceanbase` |
| `jdbc_driver` / `jdbc_url` / `username` / `password` | 条件 | 非 MongoDB 必填 |
| `mongo_url` | 条件 | 仅 MongoDB |
| `category_code` | 建议 | 所属领域 |
| `status` | **必** | `1`/`0` |
| `connect_status` | 系统写 | `online` / `offline` / `risk` |
| `active_conn` / `max_conn` / `response_ms` / `collection_cnt` / `ref_count` | 系统写 | 运行期指标 |

> 🔴 **安全提示**：`password` 明文存储，且 Mapper 用 `SELECT *` 会随详情接口返回前端。详见《初步方案设计》4.3 风险 P1-5。

**校验**

- ❌ `ds_type` 不在上述 12 种内 → 驱动目录取不到驱动类名，连接池不注册，测试连接必失败。
- ❌ MongoDB 填了 `jdbc_url` 而非 `mongo_url` → 连接失败。
- ❌ `jdbc_driver` 与 `ds_type` 不匹配 → 驱动加载失败。

### 5.2 `ont_physical_table` — 物理表元数据

| 列 | 说明 |
|---|---|
| `id` | `phys-{UUID}` |
| `ds_id` | 关联 `sys_data_source.id` |
| `physical_table` | 物理表/视图名，与 `ds_id` 组成 **UNIQUE** |
| `display_name` | 中文名，**用户可改，同步时不覆盖** |
| `table_type` | `table` / `view` |
| `columns_json` | 字段清单 JSON：`[{name, type}]` |
| `column_count` | 字段数 |
| `sync_time` | 最近同步时间 |

**校验**

- ❌ `columns_json` 不是合法数组 → 属性映射时列下拉为空。
- ⚠️ 同步会覆盖 `columns_json` 与 `column_count`，但**不覆盖** `display_name`——这是有意的，用户起的中文名要保留。

### 5.3 `ont_ext_data_source` — 外部 HTTP 数据源与 `auth_config` ★

| 列 | 默认 | 说明 |
|---|---|---|
| `id` | — | `ext_ds-{UUID}` |
| `ds_code` | — | 与 `category_code` 组成 **UNIQUE** |
| `ds_type` | `http_rest` | `http_rest` / `webhook` / `graphql` |
| `read_write_type` | `1` | `1` 只读 / `2` 读写 |
| `base_url` | — | 基础地址，所有接口路径基于此拼接 |
| `default_method` | `POST` | 全局默认方法 |
| `content_type` | `application/json` | 默认请求体格式 |
| `connect_timeout` | `5000` | 连接超时 ms |
| `read_timeout` | `10000` | 读取超时 ms |
| `retry_count` | `1` | 重试次数，**最大 3** |
| `retry_interval` | `1000` | 重试间隔 ms |
| `ssl_verify` | `1` | SSL 证书校验 |
| `log_enable` | `1` | 请求日志开关 |
| `header_enable` | `0` | 全局请求头开关 |
| `global_header` | — | 全局请求头 JSON |
| `auth_type` | `none` | 见下 |
| `auth_config` | — | 鉴权参数 JSON |

#### `auth_config` 的九种形态

字段定义是**表驱动**的（`frontend/src/views/resources/datasource/authModel.js`），新增鉴权类型只在该表加一项。

| `auth_type` | `auth_config` 键 |
|---|---|
| `none` | `{}` |
| `apikey` | `key_name`(默认 `X-API-Key`)、`param_position`(`header`/`query`)、`key_value`、`key_prefix` |
| `basic` | `username`、`password`、`auto_encode`(0/1，默认 1) |
| `bearer` | `token_value`、`expire_time`(默认 7200)、`param_position`、`token_prefix`(默认 `Bearer`)、`auto_renew`(0/1)、`renew_url`(仅 `auto_renew=1` 时显示) |
| `oauth2_client` | `token_url`、`client_id`、`client_secret`、`scope`、`param_position` 等 |
| `oauth2_code` | 授权码模式相关字段 |
| `cas` | CAS 服务地址与票据字段 |
| `jwt` | `token_value`、`token_position`(`header`/`query`/`cookie`)、`token_prefix` |
| `digest` | `username`、`password` |

示例：

```jsonc
// apikey
{ "key_name": "X-API-Key", "param_position": "header",
  "key_value": "••••••", "key_prefix": "" }

// bearer + 自动续期
{ "token_value": "••••••", "expire_time": 7200,
  "param_position": "header", "token_prefix": "Bearer",
  "auto_renew": 1, "renew_url": "https://host/oauth/refresh" }
```

**校验**

- ❌ `auth_type` 与 `auth_config` 的键不匹配（如 `auth_type='bearer'` 但只给了 `username`/`password`）→ 请求发出时不带凭证，返回 401。
- ❌ `retry_count > 3` → 超出上限，行为未定义。
- ⚠️ `base_url` 末尾带 `/` 且 `api_path` 开头也带 `/` → 拼出双斜杠，部分服务端返回 404。

> 🔴 **安全提示**：`auth_config` 内的 `key_value`、`client_secret`、`token_value`、`password` 均明文存储。见《初步方案设计》4.3 风险 P1-5。

### 5.4 `ont_ext_api_interface` — 接口定义与 `request_params` ★

| 列 | 默认 | 说明 |
|---|---|---|
| `id` | — | `ext_api-{UUID}` |
| `ds_id` | — | 所属数据源 |
| `group_id` | `'0'` | 所属分组，**未分组为 `'0'` 不是 NULL** |
| `api_code` | — | 与 `ds_id` 组成 **UNIQUE**，供函数/动作引用 |
| `method` | `POST` | `GET`/`POST`/`PUT`/`DELETE` |
| `api_path` | — | 相对路径，与 `base_url` 拼接 |
| `api_status` | `debug` | `debug` 调试 / `done` 完成 |
| `request_params` | — | 请求参数全量定义 JSON |
| `response_params` | — | 响应定义 JSON |
| `override_auth` | `0` | `0` 继承数据源 / `1` 单独配置 |
| `header_inherit` | `1` | 是否继承全局 Header |
| `content_type` / `timeout` | NULL | 为空则继承数据源 |

#### `request_params` 结构

```jsonc
{
  "header": [ { "enabled": 1, "name": "X-Trace", "value": "abc", "desc": "", "type": "text" } ],
  "query":  [ { "enabled": 1, "name": "page", "value": "1", "desc": "", "type": "text" } ],
  "body": {
    "mode": "none",          // none / form-data / x-www-form-urlencoded / raw / binary
    "raw": "",               // mode=raw 时的原始文本
    "form": [                // mode=form-data / urlencoded 时的行
      { "enabled": 1, "name": "file", "value": "", "desc": "",
        "type": "file",      // text / file（仅 form-data 支持 file）
        "file": null }       // 文件只在内存，不入库
    ],
    "file": null             // mode=binary 时
  }
}
```

**校验**

- ❌ `request_params` 缺 `body` 键 → 前端 `parseRequestParams` 会补齐默认结构，不报错。
- ❌ `body.mode` 写了不在枚举内的值 → 发送时按 `none` 处理，请求体丢失。
- ⚠️ 文件字段（`type='file'` 的 `file` 值）**不入库**，仅存在于当前会话内存。保存后重开标签页需要重新选文件。上限 10MB。

### 5.5 `ont_ext_api_call_log` — 调用日志

| 列 | 说明 |
|---|---|
| `id` | `ext_log-{UUID}` |
| `trace_id` | 全链路追踪标识（`trace-{UUID}`） |
| `call_type` | `function` / `action` / `debug` |
| `call_status` | `1` 成功 / `2` 失败 / `3` 超时 / `4` 鉴权失败 |
| `http_status` | HTTP 状态码 |
| `cost_time` | 耗时 ms |
| `response_size` | 响应体字节数 |
| `request_header` | **敏感字段脱敏后存储** |

**校验**

- ⚠️ `request_body` / `response_body` 不做大小截断，大响应会撑大日志表，需要定期归档。

### 5.6 `ont_enum_sync_config` — 枚举同步配置

| 列 | 默认 | 说明 |
|---|---|---|
| `enum_id` | — | **UNIQUE**，一个枚举至多一份同步配置 |
| `data_source_id` | — | 关联 `sys_data_source.id` |
| `sync_source_type` | `table` | `table` 按表配置 / 自定义 SQL |
| `table_name` / `table_alias` | — | 真实表名 / 备注名 |
| `field_code` / `field_name` / `field_sort` / `field_status` / `field_parent` | — | 五个字段映射 |
| `filter_sql` | — | 顶级筛选表达式，**直接拼进 `WHERE`** |
| `custom_sql` | — | 自定义 SQL 模式下的完整语句 |
| `sync_mode` | `level_diff` | `level_diff` 逐级对比 / `full_overwrite` 全量覆盖 / `incremental` 增量 |
| `sync_strategy` | `once` | `once` / `daily` / `weekly` / `monthly` |

**校验**

- ❌ `field_code` 或 `field_name` 为空 → 同步取不到编码或名称，全部失败。
- ❌ 多级枚举未配 `field_parent` → 拉回来的项全是一级，层级丢失。
- ❌ `filter_sql` 语法错误 → 同步执行时 SQL 报错，日志记 `fail_count`。
- ⚠️ `sync_strategy` 的 `daily`/`weekly`/`monthly` **当前不生效**——系统无定时调度，全部为手动触发。
- 🔴 `filter_sql` 直接拼入 WHERE，见《初步方案设计》4.3 风险 P1-8。

---

## 6. L4 消费层：对外数据契约

### 6.1 统一响应 `R<T>`

```jsonc
// 成功 —— 注意 code 是 0 不是 200
{ "code": 0, "msg": "ok", "data": { } }

// 失败 —— HTTP 状态码仍为 200
{ "code": 400, "msg": "共享属性被 3 个对象类型引用，无法删除" }
```

| 项 | 值 |
|---|---|
| 成功码 | **`0`** |
| 参数错误 | `400`（`IllegalArgumentException` 兜底） |
| 服务端错误 | `500` |
| HTTP 状态 | **恒为 200**（含失败） |

前端拦截器按 `body.code !== 0` 判失败并弹提示，返回 `body.data`。

**校验**

- ⚠️ 下游若按 HTTP 状态码判成败，**会把所有失败当成功**。必须解 body 的 `code`。

### 6.2 对象类型聚合详情

`GET /api/resource/classes/{id}` —— 下游取元数据的主入口，一次返回五段：

```jsonc
{
  "id": "class-…", "api_name": "HydrologyStation", "display_name": "水文测站",
  "category_code": "dom_hydrology", "icon": "station", "color": "#165DFF", "status": 1,
  "properties":  [ /* ont_class_property 行 */ ],
  "links":       [ /* 关联的链接类型 */ ],
  "actions":     [ /* 挂在该类上的动作 */ ],
  "interfaces":  [ /* 实现的接口 */ ],
  "datasources": [ /* 物理表绑定 */ ]
}
```

配套：`GET /api/resource/classes?aggregate=true` 列表带统计；`GET /api/resource/discover/overview` 返回 15 项全局统计（支持行业/领域过滤）。

### 6.3 实例数据形态 🟡

> ⚠️ **当前为确定性模拟数据**（`InstanceMockService`）。按对象类型的属性定义生成，种子固定、重启一致、内存缓存。**不是业务数据。**

**单条实例**是一个平铺的 Map：

```jsonc
{
  "id": "…", "classId": "class-…", "className": "水文测站",
  "code": "…", "title": "黄河上游示范测站", "rid": "…", "createdAt": "…",
  "stationCode": "HS-0001",       // 属性按 api_name 平铺进来
  "waterLevel": 128.4,
  "rel::class-river-01::riverName": "黄河"   // 跨关联对象的字段
}
```

**关联字段键前缀**：`rel::{关联对象类型id}::{关联属性api_name}`

**列表响应**：

```jsonc
{ "total": 90, "page": 1, "size": 20,
  "rows": [ /* 实例数组 */ ],
  "columns": [ /* 列定义 */ ] }
```

**校验**

- ❌ 对象类型无属性 → 生成不出实例，列表恒空。
- ❌ 对象类型 `status=0` → 不在可探索列表里。
- ⚠️ 实例 id 在重启后保持一致（确定性），但**新增属性会改变生成结果**——属性定义变了，实例内容随之变。

### 6.4 `ont_explore_design.config` — 探索看板 ★

| 列 | 说明 |
|---|---|
| `id` | `design-{UUID}` |
| `class_id` | 所属对象类型 |
| `kind` | 设计类型，如 `query` |
| `name` | **命名设计有值；每类型的默认看板为空串** |
| `config` | 看板配置 JSON，前端产生、后端只存不解析 |

> ⚠️ **`config` 有两种形态**，消费方需要都容错，见 [8.2](#82-explore_designconfig-有两种不同的顶层形态)。

**命名设计**（`name` 非空）：

```jsonc
{
  "name": "2测试新增图表", "kind": "query", "desc": "", "visibility": "private",
  "classId": "class-…", "className": "水文测站", "icon": "station", "color": "#165DFF",
  "kw": "", "pills": [], "sort": "", "order": "asc", "viewMode": "charts",
  "instanceIds": [],
  "layoutConfig": { "layout": [ /* 栅格项 */ ] }
}
```

**默认看板**（`name` 为空串）——**直接就是 layout，没有外层包装**：

```jsonc
{ "layout": [ { "isChart": true, "x": 0, "y": 0, "w": 4, "h": 9,
                "i": "…", "id": "…", "tabList": [ … ] } ] }
```

栅格项关键键：`x` / `y` / `w` / `h`（位置尺寸）、`i` / `id`（标识）、`isChart`、`isTabLayout`、`tabList[]`（图表配置，含 `type` 如 `BKPieChart`、`title`、`explainConfig` 等）。

### 6.5 类型类渲染解析

`GET /api/tc-render/resolve?classId=…`（或 `linkTypeId=…`）—— 一次拿回该载体上全部类型类绑定与参数值，供上层决定渲染方式。批量版：`/api/tc-render/category-map?categoryCode=…`。

**这是图表、图谱做"按配置渲染"的地基**，下游不应逐条查 `ont_type_class_bind`。

---

## 7. 数据自检清单

### 7.1 建模数据自检

| # | 检查项 | 不通过的后果 |
|---|---|---|
| 1 | 每个对象类型的 `category_code` 指向存在的分类节点 | 左树里点不出来 |
| 2 | 每个对象类型至少有 1 条属性 | 实例探索空白 |
| 3 | `prop_type='object'` 的属性都填了 `range_class_id` | 图谱断链 |
| 4 | 链接两端的 `l_object_type_id`/`r_object_type_id` 都存在 | 列表显示空白实体 |
| 5 | `is_data_source_rel=1` 的链接都填了 `rel_data_table` 与 `join_table_column` | 取数失败 |
| 6 | 复合键映射的 `seq` 从 1 连续 | join 键序错乱 |
| 7 | `constraint_type='Enum'` 的值类型都填了 `enum_id` | 值域为空 |
| 8 | 多级枚举项的 `parent_code` 都能在同枚举内找到 | 整支子树消失 |
| 9 | 枚举项 `level` 与 `parent_code` 深度一致 | 层级截断错误 |
| 10 | 动作规则的 `sort` 无重复 | 执行顺序不确定 |
| 11 | 动作规则的 `rule_config` 都有 `kind` 键 | 规则卡片空白 |

### 7.2 JSON 列自检

对每个 JSON 列跑一遍"能否解析 + 必需键是否齐全"：

```sql
-- 找出解析不了的 rule_config（近似判断）
SELECT id, rule_name FROM ont_class_action_rule
WHERE rule_config IS NOT NULL AND rule_config <> ''
  AND rule_config NOT LIKE '{%}';

-- 找出缺 kind 的规则
SELECT id, rule_name FROM ont_class_action_rule
WHERE rule_config IS NULL OR rule_config = '' OR rule_config NOT LIKE '%"kind"%';

-- 找出 Enum 值类型漏配枚举的
SELECT id, api_name FROM ont_value_types
WHERE constraint_type = 'Enum' AND (enum_id IS NULL OR enum_id = '');

-- 找出挂了数据源但没填物理列的属性
SELECT id, api_name FROM ont_class_property
WHERE class_ds_id IS NOT NULL AND class_ds_id <> ''
  AND (physical_column IS NULL OR physical_column = '');
```

### 7.3 双方言自检

| # | 检查项 |
|---|---|
| 1 | 新迁移脚本在 `sqlite/` 与 `postgresql/` 都有对应版本（或确认放在 `common/` 且语法通用） |
| 2 | 无 Java 代码给时间戳列传字符串 |
| 3 | 元数据查询（表清单、列结构）按方言分支，未硬编码 `sqlite_master` / `PRAGMA` |
| 4 | 布尔用 `INTEGER 0/1`，未用 `BOOLEAN` |
| 5 | 删库后两种方言各跑一次冷启动，均出现 `Successfully applied N migrations` |

### 7.4 快速验证命令

```bash
# 后端就绪
curl -s http://localhost:8088/bontolink/api/health

# 逐模块 smoke（成功码是 0）
for p in category/tree resource/classes link-types action-types shared-properties \
         value-types enum-types interface type-classes datasource ext-datasource \
         dict instance/object-types tool/status; do
  echo -n "$p -> "; curl -s "http://localhost:8088/bontolink/api/$p" | head -c 60; echo
done
```

```python
# 演示库计数快照（SQLite）
import sqlite3
c = sqlite3.connect('backend/bontolink.db')
for t, in c.execute("select name from sqlite_master where type='table' order by name"):
    n = c.execute('select count(*) from "%s"' % t).fetchone()[0]
    if n: print(t, n)
```

---

## 8. 已知数据问题

这一节记录**当前数据形态里确实存在的不一致**，不是需求，是现状。接数据、写消费方代码时必须知道。

### 8.1 `rule_config` 是全部规则类型的扁平并集

**现象**：`ont_class_action_rule.rule_config` 无论 `kind` 是什么，都会写入全部 8 种规则类型的键。实测演示库中共出现 **48 个键**——一条 `create_object` 规则里带着 `notify_*`、`wh_*`、`func_*` 的全套默认值。

**影响**：
- JSON 体积是实际所需的数倍。
- 无法凭键的存在判断规则类型，**必须先读 `kind`**。
- 无法用 SQL 检索（现有的规则维度计数只能 `LIKE '%"kind":"xxx"%'`）。

**消费方约定**：先取 `kind`，按 [4.9.1](#491-ont_class_action_rulerule_config) 的对照表只取该 kind 的键，**忽略其余全部键**。

**另有一条历史数据缺 `kind`**（规则名"属性映射"，`rule_config` 为空），前端渲染为空白卡片。清理时可按 [7.2](#72-json-列自检) 的 SQL 定位。

### 8.2 `explore_design.config` 有两种不同的顶层形态

命名设计是 `{name, kind, classId, …, layoutConfig:{layout:[…]}}`，默认看板直接是 `{layout:[…]}`。两者顶层结构不同，消费方需要按 `config.layout ?? config.layoutConfig?.layout` 兼容取值。

### 8.3 `value_type` 列的注释与实际存储不符

`ont_class_property.value_type` 的 DDL 注释写的是"值类型 RID"，但**实际存的是 `ont_value_types.id`**（实测值 `value-types-text`、`value-types-area`）。`ont_shared_properties.value_type` 的注释是"关联值类型 id"，与实际一致。

**约定以实际为准：存 `id`。** 建议后续修正 DDL 注释。

### 8.4 时间戳列名不统一

多数表用 `create_time` / `update_time`，类型类等较新的表用 `created_at` / `updated_at`。写通用工具时不能假定列名。

### 8.5 ID 前缀有历史遗留的不一致

代码生成的前缀（`class-property-`）与部分种子数据的前缀（`cp-1`）不同源。同一张表里可能并存两种形态。**只做等值比较，不解析前缀。**

### 8.6 演示库与迁移脚本存在漂移

`backend/bontolink.db` 是长期迭代的开发库，与迁移脚本的当前状态不完全一致——例如 V31 引入的 `ont_ext_data_source` 三表在该库中不存在（Flyway 已应用 25 个版本，但该库建立时间早于部分迁移）。

**结论**：**以迁移脚本为准，不以现有 db 文件为准。** 需要干净环境时删库重启，让 Flyway 从头建。

### 8.7 `sync_strategy` 定义了但不生效

`ont_enum_sync_config.sync_strategy` 有 `once/daily/weekly/monthly` 四值，但系统无定时调度，全部为手动触发。填 `daily` 不会有任何自动行为。

---

## 9. 附录

### 9.1 全表清单（按层分组）

**L1 归属层（5）**
`ont_biz_category`、`ont_biz_namespace`、`ont_biz_namespace_version`、`ont_biz_group`、`ont_biz_group_class`

**L2 定义层 · 对象与关系（10）**
`ont_class`、`ont_class_property`、`ont_class_link`、`ont_class_ds`、`ont_class_group`、`ont_class_disjoint_union`、`ont_property_equivalent`、`ont_property_disjoint`、`ont_link_types`、`ont_link_mappings`

**L2 定义层 · 动作（21）**
`ont_class_action`、`ont_class_action_rule`、`ont_action_rule_condition`、`ont_action_rule_property_mapping`、`ont_action_form_section`、`ont_action_form_param`、`ont_action_form_param_display`、`ont_action_form_global_config`、`ont_action_form_display_string`、`ont_action_form_display_number`、`ont_action_form_display_boolean`、`ont_action_form_display_object`、`ont_action_form_override_block`、`ont_action_form_override_item`、`ont_action_override_condition_group`、`ont_action_override_condition_item`、`ont_action_submit_standard_config`、`ont_action_submit_condition_node`、`ont_action_function_rule_config`、`ont_action_function_param_mapping`、`ont_action_function_exception_map`、`ont_action_link_rule_config`、`ont_action_link_prop_mapping`、`ont_action_notification_rule_config`、`ont_action_webhook_rule_config`、`ont_action_webhook_input_mapping`、`ont_action_execution`

**L2 定义层 · 类型与约束（12）**
`ont_shared_properties`、`ont_struct_types`、`ont_struct_items`、`ont_value_types`、`ont_valuetypes_usage_config`、`ont_enum_types`、`ont_enum_items`、`ont_enum_level_code_rule`、`ont_interface`、`ont_interface_property`、`ont_interface_class`、`ont_property_format`

**L2 定义层 · 类型类（4）**
`ont_type_class`、`ont_type_class_category_dict`、`ont_type_class_bind`、`ont_dic_type_class`

**L3 接入层（10）**
`sys_data_source`、`ont_physical_table`、`ont_ext_data_source`、`ont_ext_api_group`、`ont_ext_api_interface`、`ont_ext_api_call_log`、`ont_enum_sync_config`、`ont_enum_sync_log`

**L4 消费与支撑（10）**
`ont_explore_design`、`ont_dict_def`、`ont_dict_item`、`icon_lib_group`、`icon_lib_icon`、`ont_ontology_version`、`ont_domain_term`、`sys_synonym_dict`、`sys_synonym_candidate`、`sys_stopwords`、`ont_class_expansion`、`ont_class_hierarchy`、`sys_query_log`

**迁移元数据（1）**：`flyway_schema_history`

### 9.2 ID 前缀速查（52 个）

代码中 `UUID.randomUUID()` 的全部前缀（权威来源：`bontolink-admin` 各 Controller / Service）：

| 前缀 | 表 | 前缀 | 表 |
|---|---|---|---|
| `class-` | ont_class | `enum-types-` | ont_enum_types |
| `class-property-` | ont_class_property | `enum-item-` | ont_enum_items |
| `class-ds-` | ont_class_ds | `enum_level_code_rule-` | ont_enum_level_code_rule |
| `class-group-` | ont_class_group | `enum-sync-config-` | ont_enum_sync_config |
| `class-disjoint-union-` | ont_class_disjoint_union | `enum-sync-log-` | ont_enum_sync_log |
| `prop-equivalent-` | ont_property_equivalent | `enum-groups-` | （枚举分组，stub） |
| `prop-disjoint-` | ont_property_disjoint | `interface-` | ont_interface |
| `link-types-` | ont_link_types | `interface-pro-` | ont_interface_property |
| `link-mappings-` | ont_link_mappings | `interface-class-` | ont_interface_class |
| `class_action-` | ont_class_action | `type-class-` | ont_type_class |
| `action_rule-` | ont_class_action_rule | `bind-` | ont_type_class_bind |
| `action_form_section-` | ont_action_form_section | `tcdic-` | ont_dic_type_class |
| `action_form_param-` | ont_action_form_param | `datasource-` | sys_data_source |
| `submit_standard-` | ont_action_submit_standard_config | `phys-` | ont_physical_table |
| `submit_node-` | ont_action_submit_condition_node | `ext_ds-` | ont_ext_data_source |
| `action_exec-` | ont_action_execution | `ext_group-` | ont_ext_api_group |
| `rule_prop-` | ont_action_rule_property_mapping | `ext_api-` | ont_ext_api_interface |
| `shared-properties-` | ont_shared_properties | `ext_log-` | ont_ext_api_call_log |
| `struct-types-` | ont_struct_types | `trace-` | （调用链路 trace_id） |
| `struct-items-` | ont_struct_items | `dict-` | ont_dict_def |
| `sp-gc-` | 共享属性分组绑定 | `dict-item-` | ont_dict_item |
| `value-types-` | ont_value_types | `design-` | ont_explore_design |
| `vt-usage-config-` | ont_valuetypes_usage_config | `ig-` / `ii-` | icon_lib_group / icon |
| `category-` / `group-` | ont_biz_category | `syn-` | sys_synonym_dict |
| `namespace-` | ont_biz_namespace | `dt-` | ont_domain_term |
| `namespace-v-` | ont_biz_namespace_version | `gc-` | ont_biz_group_class |
| `group-ref-` | 分组引用 | | |

### 9.3 枚举值速查

| 字段 | 合法值 |
|---|---|
| `ont_biz_category.category_type` | `1` 行业 / `2` 领域 / `3` 分组 |
| `ont_class_property.prop_type` | `data` / `object` / `annotation` / `struct` |
| `ont_link_types.l_cardinality` / `r_cardinality` | `one` / `many` |
| `ont_link_types.l_visibility` / `r_visibility` | `normal` / `prominent` / `hidden` |
| `ont_link_types.status` | `experimental` / `active` / `deprecated` |
| `ont_link_mappings.side` | `left` / `right` |
| `ont_link_types.is_data_source_rel` | `0` 字段映射 / `1` 中间表 |
| `ont_class.class_expr_type` | `NULL`/`0` / `union` / `intersection` / `complement` / `enumeration` |
| `ont_value_types.base_type` | `String` / `Integer` / `Decimal` / `Boolean` / `DateTime` |
| `ont_value_types.constraint_type` | `RID` / `UUID` / `Length` / `Regex` / `Enum` |
| `ont_valuetypes_usage_config.display_format` | `label` / `code` / `code_label` / `full_label` |
| `ont_enum_types.enum_type` | `general_single` / `general_multi` / `biz_single` / `biz_multi` |
| `ont_enum_types.status`、`ont_enum_items.status` | `active` / `inactive` |
| `ont_enum_level_code_rule.fill_pos` | `0` 前补 / `1` 后补 |
| `ont_enum_sync_config.sync_mode` | `level_diff` / `full_overwrite` / `incremental` |
| `ont_enum_sync_config.sync_strategy` | `once` / `daily` / `weekly` / `monthly`（后三者不生效） |
| `ont_interface_property.is_required` | `0` 可选 / `1` 必填 / `2` 多值 |
| `ont_type_class.param_type` | `boolean` / `rid` / `enum` / `text` / `numeric` / `json` |
| `ont_type_class.frontend_component` | `switch` / `single_select` / `multi_select` / `text_input` / `number_input` / `json_editor` / `rid_selector` |
| `ont_type_class_bind.applicable_type` | `property` / `relation` / `action` |
| `ont_class_action.status` | `0` 草稿 / `1` 已发布 / `2` 已停用 |
| `ont_class_action_rule.rule_type` | `1` 本体编辑 / `2` 副作用 |
| `rule_config.kind` | `create_object` / `modify_object` / `delete_object` / `create_link` / `delete_link` / `function` / `notification` / `webhook` |
| 值来源 `value_source` | `1` 表单参数 / `2` 静态值 / `3` 当前用户 / `4` 系统时间 / `5` 关联对象属性 |
| `ont_action_form_param.param_type` | `string` / `number` / `boolean` / `object` / `enum` |
| `sys_data_source.ds_type` | `mysql` / `postgresql` / `oracle` / `mongodb` / `dm` / `kingbase` / `oscar` / `gbase` / `polardb` / `tdsql` / `gaussdb` / `oceanbase` |
| `sys_data_source.connect_status` | `online` / `offline` / `risk` |
| `ont_physical_table.table_type` | `table` / `view` |
| `ont_ext_data_source.ds_type` | `http_rest` / `webhook` / `graphql` |
| `ont_ext_data_source.auth_type` | `none` / `apikey` / `basic` / `bearer` / `oauth2_client` / `oauth2_code` / `cas` / `jwt` / `digest` |
| `ont_ext_api_interface.method` | `GET` / `POST` / `PUT` / `DELETE` |
| `ont_ext_api_interface.api_status` | `debug` / `done` |
| `request_params.body.mode` | `none` / `form-data` / `x-www-form-urlencoded` / `raw` / `binary` |
| `ont_ext_api_call_log.call_status` | `1` 成功 / `2` 失败 / `3` 超时 / `4` 鉴权失败 |
| `ont_ext_api_call_log.call_type` | `function` / `action` / `debug` |

### 9.4 演示数据快照（2026-08-03）

| 维度 | 计数 |
|---|---|
| 数据表 / 已应用迁移 | 76 / 25 |
| 分类节点 / 命名空间 / 分组 / 成员绑定 | 18 / 22 / 17 / 43 |
| 对象类型 / 属性 / 简单类链接 / 物理表绑定 | 48 / 78 / 20 / 3 |
| 链接类型 / 字段映射 | 4 / 9 |
| 动作 / 规则 / 表单分区 / 表单参数 / 属性映射 / 提交节点 / 提交配置 | 9 / 15 / 8 / 85 / 67 / 18 / 9 |
| 共享属性 / 结构属性 / 结构条目 | 23 / 6 / 15 |
| 值类型 / 使用配置 | 5 / 1 |
| 枚举类型 / 枚举项 / 编码规则 / 同步配置 / 同步日志 | 4 / 18 / 5 / 1 / 2 |
| 接口 / 接口属性 / 实现绑定 | 9 / 11 / 13 |
| 类型类 / 分类 / 绑定 / 枚举字典 | 44 / 11 / 4 / 23 |
| 数据源 | 14 |
| 系统字典 / 字典条目 | 38 / 156 |
| 图标分组 / 自定义图标 | 16 / 131 |
| 同义词 / 停用词 / 领域术语 | 16 / 20 / 9 |
| 属性格式化规则 / 探索设计 | 9 / 4 |

### 9.5 相关文档

| 文件 | 内容 |
|---|---|
| `需求规格说明书.md` | 功能清单与验收标准 |
| `初步方案设计.md` | 架构 / 模块 / 技术 / 安全 |
| `使用帮助.html` | 操作手册 |
| `API_NAME_CODING_STANDARD.md` / `API_NAME_CHEATSHEET.md` | 编码规范与决策树 |
| `backend/.../db/migration/` | **权威 DDL**（以此为准，不以现有 db 为准） |
| `frontend/src/api/index.js` | 全部 API 客户端定义 |
| `_idx/` | 八个核心模块的预索引 |
| `tools/` | api_name 冲突检测工具 |

### 9.6 文档版本

| 版本 | 日期 | 内容 |
|---|---|---|
| V1.0 | 2026-08-03 | 初版。四层数据分层、通用规范、约 76 张表的字段规范、7 个关键 JSON 列的结构定义、自检清单、7 项已知数据问题 |
