# BOntoLink 动态本体管理系统 · 需求规格说明书（SRS）

| 项 | 内容 |
|---|---|
| 产品名称 | BOntoLink 博智联 · 动态本体管理系统 |
| 文档版本 | **V1.0** |
| 编制日期 | 2026-08-03 |
| 阅读对象 | 需求方 / 产品评审 / 研发 / 测试 |
| 定位 | 面向水利等行业的动态本体（Ontology）建模与元数据管理平台 |
| 下游消费方 | 智能报表、实例探索、知识问答等按本体语义取数的应用 |

> **状态标注**：✅ 已实现并可实测　🟡 已实现但有已知限制　🔜 规划中　🚫 **明确不做**（附理由，界定范围）
>
> 本文既是需求依据，也是**验收依据**：每组功能后附「验收」，给的是可复现的实测口径（接口、页面、库表计数）。
>
> **口径说明**：✅/🟡 依据当前代码与演示库实测；🚫 条目是本文建议的范围边界，需在评审时确认。文中的库表计数取自 2026-08-03 的演示库 `backend/bontolink.db` 快照。

---

## 1. 引言

### 1.1 目的

界定本产品的范围、功能需求与验收标准，作为需求方、研发与测试的共同依据。

**与《使用帮助》的分工**：本文回答「要做什么、做到什么算合格」；`使用帮助.html` 回答「怎么操作」。两者不重复，验收时对照本文，使用时对照帮助。

### 1.2 产品定位

给行业业务数据建立一层**可管理、可复用、可被机器理解的语义模型**。用户在界面上定义对象类型、它们之间的链接、属性与取值约束，并把这些定义映射到已有的物理表或外部接口上。

产出物是**元数据**，不是业务数据本身：谁有哪些字段、字段什么含义、取值从哪来、和别的实体怎么关联。上层应用（报表、问答、图谱、探索）读这层元数据去取数、出图、生成 SQL。

**不是** 数据仓库，**不是** ETL 调度平台，**不是** BI 报表工具，**不是** 业务表单系统。

### 1.3 术语

| 术语 | 含义 |
|---|---|
| 本体 Ontology | 一套形式化的概念模型：类、属性、关系及其约束 |
| **对象类型** Object Type | 本体的核心实体（对应 OWL Class）。一个对象类型 ≈ 一类业务实体，如「水文测站」 |
| **属性** Property | 对象类型上的字段。分数据属性 / 对象属性 / 注解属性 |
| **链接类型** Link Type | 两个对象类型之间的关系定义，带基数（1:1 / 1:N / N:1 / N:N）与双向命名 |
| **动作类型** Action Type | 挂在对象类型上的可执行操作：表单参数 + 一组有序规则 + 提交标准 |
| **共享属性** Shared Property | 跨对象类型复用的属性定义，一处改动全局生效 |
| **结构属性** Struct Type | 若干共享属性组合成的复合结构 |
| **值类型** Value Type | 属性取值的语义包装：基础类型 + 约束（长度/正则/RID/UUID/枚举） |
| **枚举类型** Enum Type | 有限取值字典，支持多级层次与外部库同步 |
| **接口** Interface | 纯抽象契约：一组属性约定，供对象类型去「实现」 |
| **类型类** Type Class | 给属性/关系/动作挂的元数据标签，带参数与前端组件映射，驱动上层渲染 |
| **行业分类** Category | 行业 → 领域 → 分组 三层归属树，全系统的筛选与归属维度 |
| **命名空间** Namespace | 领域绑定的 URI 与版本，本体术语的作用域 |
| **api_name** | 实体的机器可读唯一编码，全局命名规范见《API_NAME_CODING_STANDARD》 |
| **RID** | 资源标识符，格式 `ri.ont.{module}.{code}` |
| **物理表映射** | 对象类型/属性到真实数据库表列的绑定关系 |
| **实例** Individual | 对象类型的具体数据行。本版为**确定性模拟数据**，见 CON-02 |

---

## 2. 总体描述

### 2.1 用户角色

| 角色 | 能力 |
|---|---|
| **本体管理员** | 全部功能：分类/命名空间、类型类、字典、数据源等全局配置 |
| **业务建模师** | 建模主线：对象类型、链接、动作、属性、值类型、枚举、接口 |
| **数据接入工程师** | 数据源接入、物理表同步与字段映射、外部接口定义与调试、枚举同步 |
| **消费者** | 总览 / 图谱 / 实例探索 / 知识浏览器只读查看；下游系统走 REST 取元数据 |

本版**不含权限体系**，四类角色能力在界面上不做隔离（见 2.4、FR-SYS-06）。

### 2.2 运行环境

| 层 | 选型 |
|---|---|
| 前端 | Vue 3 `<script setup>` + Vite 5 + vue-router(hash) + Pinia；**原生 CSS，无 UI 框架** |
| 后端 | Java 21 + Spring Boot 3.3 + MyBatis（注解式 SQL） |
| 数据库 | 默认 **SQLite**（单文件，启动即用）；同步支持 **PostgreSQL**（切 profile） |
| 迁移 | Flyway，`db/migration/{common,sqlite,postgresql}/Vn__*.sql` |
| 语义层 | Apache Jena（本体模型 / SPARQL）+ 向量库（pgvector / Milvus） |
| 部署 | 后端 `localhost:8088`，context-path `/bontolink`；前端 dev `5173` 代理 `/api` |

后端为四模块 Maven 工程：`bontolink-data`（Mapper/Entity/Flyway）→ `bontolink-base`（动态数据源与连接池）→ `bontolink-admin`（管理后台，端口 8088）/ `bontolink-ontology`（本体服务，端口 8089）。

### 2.3 约束

| 编号 | 约束 |
|---|---|
| CON-01 | 单体部署，两种方言（SQLite / PostgreSQL）必须同时可用；所有 schema 变更走 Flyway 增量脚本，不改已发布版本 |
| CON-02 | **系统不存储真实对象实例**。实例探索、动作执行均基于按对象类型属性定义**确定性生成**的模拟数据（`InstanceMockService`），重启后一致 |
| CON-03 | 前端不引入 Element / Antd / Tailwind 等 UI 框架，组件全部自研；每模块 CSS 独立 namespace 前缀 |
| CON-04 | `api_name` 全局唯一且遵循统一编码规范；创建后不可修改（改了会打断下游引用） |
| CON-05 | 时间戳列在 PostgreSQL 为 `TIMESTAMP`，禁止从 Java 传字符串；统一 `CURRENT_TIMESTAMP` 或 `java.sql.Timestamp` |
| CON-06 | 外部数据源的凭证以明文/可逆方式存库，仅适用于内网可信环境；生产落地前需接入密钥托管 |

### 2.4 明确不做（控范围）🚫

| 项 | 理由 |
|---|---|
| 业务实例的增删改存储 | 本产品管的是元数据。实例数据留在各业务系统的物理库，本产品只做映射与读取（见 CON-02、7.2） |
| ETL / 数据同步调度 | 属数据集成平台职责。本产品只做「枚举字典同步」这一项元数据级同步 |
| 报表设计与出图 | 由下游《智能报表》承担，本产品向其提供本体语义层 |
| 工作流 / 审批流引擎 | 动作类型只定义「做什么」，不定义「谁批、几级审」 |
| 多租户与租户隔离 | 单实例单组织部署 |
| 本体自动学习 / 文本抽取建模 | AI 助手页仅为交互占位，自动建模不在本版范围 |

---

## 3. 典型用例

六个用例覆盖第 4、5 章的全部功能分组。每个用例给出角色、前置、主流程、涉及功能编号与完成标志。

### UC-01 从零建一个业务对象（业务建模师）

| 项 | 内容 |
|---|---|
| 角色 | 业务建模师 |
| 前置 | 行业分类里已有「水利 → 水文」领域及命名空间 |

**主流程**

1. 进 **对象类型**，左侧分类树选中「水利 · 水文」，点 **新建**，走创建向导：填编码 / 显示名 / api_name / 图标颜色，选所属领域。（FR-OT-01、FR-CT-05）
2. 抽屉切「属性」页签，逐条加属性：编码、XSD 数据类型、是否主键/必填/多值，需要语义约束的挂 **值类型**。（FR-OT-05、FR-VT-03）
3. 有些属性是全系统通用的（如「行政区划代码」），不在这里重复定义，直接引用 **共享属性**。（FR-SP-01）
4. 取值有限的属性挂 **枚举类型**（经值类型的 Enum 约束）。（FR-ET-01、FR-VT-04）
5. 切「数据源」页签，绑定物理表，把属性逐个映射到表列。（FR-OT-10、FR-DS-06）
6. 需要对外声明契约时，在「接口」页签勾选它实现的接口。（FR-IF-05）

**完成标志**：总览页对象类型计数 +1；`GET /api/resource/classes/{id}` 返回带属性、链接、数据源绑定的完整结构；实例探索里该对象类型可打开并出数。

---

### UC-02 把两个对象连起来（业务建模师）

| 项 | 内容 |
|---|---|
| 角色 | 业务建模师 |
| 前置 | 「水文测站」与「河流」两个对象类型已建好 |

**主流程**

1. 进 **链接**，点新建。上下两栏对称填源端 / 目标端：各选对象类型、基数（one/many）、正反向显示名与复数名。（FR-LK-01/02/03）
2. 关系落在哪：字段映射（两表各出一个字段对上）还是中间表（`is_data_source_rel=1` + 指定中间表）。复合主键就多加几行映射。（FR-LK-04/05）
3. 需要给这条关系挂元数据标签时，切「类型类」页签绑定。（FR-TC-05）
4. 保存后在「链接关系图」里核对方向与基数是否画对。（FR-OT-07）

**完成标志**：列表行显示 `源实体 —基数标签→ 目标实体`，映射数非 0；对象类型详情的链接关系图里两端都能看到这条边。

---

### UC-03 给对象配一个可执行动作（业务建模师）

| 项 | 内容 |
|---|---|
| 角色 | 业务建模师 |
| 前置 | 主体对象类型已建好，需要一个「新建监测样地」的操作入口 |

**主流程**

1. 进 **动作类型**，走五步新建向导确定主体对象、编码、名称与基本配置。（FR-AT-01）
2. 进详情工作台的 **表单** 页：按分区加参数，逐个设数据类型、显示类型、默认值、校验规则；对象引用类参数配「从对象集获取」的筛选规则。（FR-AT-04/05/06）
3. 进 **规则** 页：按执行顺序排规则卡片。本体编辑类有创建/修改/删除对象、创建/删除链接、运行函数；副作用类有通知、Webhook。（FR-AT-07/08）
4. 每条规则配属性映射：目标属性 ← 值来源（表单参数 / 对象参数属性 / 静态值 / 当前用户或时间）。（FR-AT-09）
5. 进 **安全与提交**：配提交标准条件树（all/any 嵌套）。（FR-AT-11）
6. 管理页点 ▶ **试运行**，填参数看「将写入的记录」与「将触发的副作用」，确认逻辑对了再发布。（FR-AT-12）

**完成标志**：规则冲突校验无红条；试运行产出可预期的执行计划并落一条执行记录；状态从「草稿」切到「已发布」。

---

### UC-04 接一个外部系统的数据（数据接入工程师）

| 项 | 内容 |
|---|---|
| 角色 | 数据接入工程师 |
| 前置 | 对方给了一个数据库连接串，或一组 HTTP 接口 + 鉴权方式 |

**主流程（数据库类）**

1. 进 **数据源** 新建，选数据库类型（12 种可选），填 JDBC 驱动 / URL / 凭证，点**测试连接**。（FR-DS-01/03）
2. 切「物理表」页签点同步，把对端的表结构拉进来。（FR-DS-06）
3. 回对象类型的「数据源」页签做表与字段映射。（FR-OT-10）
4. 用「监控」页签看连接池实时状态，必要时热刷新或动态调整最大连接数。（FR-DS-04/05）

**主流程（HTTP 接口类）**

1. 新建**外部数据源**，填基址与鉴权（9 种鉴权类型，含 OAuth2 两种模式、CAS、JWT）。（FR-EX-01/02）
2. 进**接口管理器**（类 Postman 的独立标签页）：左侧目录建分组与接口，地址栏选方法填路径，配 Header / Query / Body，点发送真实调用。（FR-EX-03/04/05）
3. 调通后在「日志」页签看调用记录与耗时，「监控」页签看成功率趋势。（FR-EX-06/07）

**完成标志**：数据库类连接状态为 online 且物理表列表非空；HTTP 类接口能真实发起请求并落调用日志。

---

### UC-05 查数据：从对象类型钻到具体记录（消费者）

| 项 | 内容 |
|---|---|
| 角色 | 消费者（不建模，只看数） |

**主流程**

1. 进 **实例探索**，顶部选领域范围，或直接用全局搜索找对象类型（支持分词 / 引号短语 / AND-OR / 通配符）。（FR-IX-01/02）
2. 打开一个对象类型 → 列表视图，配显示列（可跨关联对象取列）、加筛选条件、排序、分页。（FR-IX-03/04）
3. 切图表视图看分组聚合，或用推荐图表快速出图；配置存成该对象类型的默认看板。（FR-IX-05/06）
4. 点某行开详情抽屉，看全部属性与关联对象，顺着关联跳到下一个对象。（FR-IX-07）

**完成标志**：全程不进任何建模页面；看板配置刷新后仍在（落库而非 localStorage）。

---

### UC-06 核对本体是否真的进了语义层（本体管理员）

| 项 | 内容 |
|---|---|
| 角色 | 本体管理员 |
| 前置 | 建模改动已保存，需确认 Jena 模型与向量库同步 |

**主流程**

1. 进 **知识浏览器**，顶部三张卡片看 Jena / 向量库 / 数据库是否就绪及规模。（FR-KB-01）
2. Jena 页签逐项核对：类、属性、成员个体、资源页（出站/入站三元组下钻）、目录、类树。（FR-KB-02/03/04）
3. 用 SPARQL 页签跑查询模板或自己写的语句验证推理结果。（FR-KB-05）
4. 向量库页签按命名空间浏览已向量化的类，用一段自然语言做相似度检索验证召回。（FR-KB-06/07）
5. 需要重建时走 `POST /api/ontology/rebuild` 或同义词的 Milvus 重建入口。（FR-KB-08）

**完成标志**：三张状态卡全绿；SPARQL 能查到刚建的类；向量检索能召回它。

---

### 用例与功能需求的覆盖关系

| 用例 | 主要涉及 |
|---|---|
| UC-01 建对象 | FR-OT-01/05/10、FR-VT-03、FR-SP-01、FR-ET-01、FR-CT-05 |
| UC-02 建链接 | FR-LK-01~05、FR-TC-05、FR-OT-07 |
| UC-03 建动作 | FR-AT-01~12 |
| UC-04 接数据 | FR-DS-01~06、FR-EX-01~07、FR-OT-10 |
| UC-05 查数据 | FR-IX-01~07、FR-SYS-01 |
| UC-06 核对语义层 | FR-KB-01~08 |

> 未被任何用例覆盖的需求，须在评审时说明存在的理由。

---

## 4. 功能需求 —— 本体建模主线

### 4.1 行业分类与命名空间（FR-CT）

分类树是全系统的**归属与筛选维度**：每个本体资源都挂在某个分类节点下，六个资源模块共用同一棵树做左侧过滤。

| 编号 | 需求 | 状态 |
|---|---|---|
| FR-CT-01 | 三层分类树：**行业 → 领域 → 分组**，`category_type` 1/2/3，父子经 `parent_id` 自关联 | ✅ |
| FR-CT-02 | 节点 CRUD；删除前做级联校验（有下级或有资源引用时拒绝） | ✅ |
| FR-CT-03 | 树 / 列表双视图，展开折叠、统计徽标、右键菜单、快速添加悬浮菜单 | ✅ |
| FR-CT-04 | 搜索支持**中文 / 拼音 / 首字母** | ✅ |
| FR-CT-05 | 领域绑定**命名空间**：ns_code / URI / 当前版本 / 层次路径 / JSON 元数据 | ✅ |
| FR-CT-06 | 命名空间**版本历史**（`ont_biz_namespace_version`） | 🟡 有表与列表，版本切换与差异比对未做 |
| FR-CT-07 | 分组**成员管理**：加入/移出对象类型，拖拽排序 | ✅ |
| FR-CT-08 | 节点统计：单点 `/{id}/stats`、批量 `stats-batch?ids=`、关联图 `/{id}/graph` | ✅ |
| FR-CT-09 | 按任意编码反解所属领域 `resolve-domain?code=` | ✅ |
| FR-CT-10 | 全局概览：统计卡片（行业/领域/类/链接/动作/接口/属性）+ 行业柱状图 + 行业卡片列表 | ✅ |
| FR-CT-11 | 节点显示配置：图标（业务图库自定义 SVG + 18 组内置图标）+ 主题色 | ✅ |
| FR-CT-12 | RDF 语义字段：`rdfs:label / comment / seeAlso / isDefinedBy` | ✅ |
| FR-CT-13 | 分类树导出 | 🔜 按钮已在，导出未接 |

**验收**：`GET /api/category/tree` 返回嵌套三层结构；演示库 18 个分类节点 / 22 个命名空间 / 17 个分组 / 43 条分组成员绑定；六个资源页左侧树的选中态各自独立持久化（store-key 区分）。

### 4.2 对象类型（FR-OT）

本体的核心。一个对象类型 = 类定义 + 属性集 + 关系 + 约束 + 物理映射 + 业务应用。

| 编号 | 需求 | 状态 |
|---|---|---|
| FR-OT-01 | 对象类型 CRUD + 状态启停；创建走分步向导（类 + 数据源 + 属性字段映射，事务） | ✅ |
| FR-OT-02 | 三种视图：列表 / 卡片 / 图谱；行业·领域·状态筛选 + 搜索 + 分组折叠 | 🟡 图谱视图为占位 |
| FR-OT-03 | 详情抽屉左侧 **4 组 15 个页签**：基础信息(概览/属性)、关系(链接关系/对象图谱)、规则约束(等价类/不相交类/互斥并集/等价属性/不相交属性/接口)、业务应用(动作/函数/被引用/数据源/使用情况/时序图表/事件时间轴) | 🟡 函数页签依赖未上线的函数模块 |
| FR-OT-04 | 抽屉支持**二级嵌套**（最多 8 个 tab 的栈）、左缘拖拽宽度并持久化、最大化 | ✅ |
| FR-OT-05 | 属性行内编辑：编码 / 显示名 / 属性种类(data/object/annotation) / XSD 类型 / 值类型 / 主键·必填·派生·多值 / 值域类 / 父属性 / 排序 | ✅ |
| FR-OT-06 | 属性携带 **7 个 OWL 特性** + **6 个 XSD 约束**；拖拽排序 | ✅ |
| FR-OT-07 | **链接关系图**：以本类为中心的关系画布，可直接在图上编辑链接（复用 LinkTypeEditor） | ✅ |
| FR-OT-08 | **对象图谱**：7 维度关联视图 | ✅ |
| FR-OT-09 | 类关系约束：等价类 / 不相交类 / 互斥并集 / 等价属性 / 不相交属性，各自独立 CRUD 与去重校验 | ✅ |
| FR-OT-10 | **物理表绑定**：主表 / 附表（`rel_type` 1/2），属性到表列的映射；解绑时级联清空字段映射 | ✅ |
| FR-OT-11 | 类表达式（`class_expr_type` / `class_expr_content`）与特殊类标记（Thing / Nothing / 通用类） | 🟡 存得下、显示得出，无表达式求值与推理 |
| FR-OT-12 | 删除保护：有属性/链接/动作/接口引用时拒绝并给出引用清单 | ✅ |
| FR-OT-13 | 时序图表 / 事件时间轴：按类型类配置渲染业务时序视图 | 🟡 依赖类型类绑定，示例级 |
| FR-OT-14 | 属性画布视图（`TabPropsCanvas` / X6 版） | 🟡 实验特性，未接入默认路径 |

**验收**：演示库 48 个对象类型 / 78 条属性 / 20 条简单类链接 / 3 条物理表绑定；`GET /api/resource/classes/{id}` 一次返回属性、链接、动作、接口、数据源五段嵌套结构；`GET /api/resource/discover/overview` 返回 15 项统计且支持行业/领域过滤。

### 4.3 链接类型（FR-LK）

| 编号 | 需求 | 状态 |
|---|---|---|
| FR-LK-01 | 链接 CRUD + 批量删除 + 状态切换（experimental / active / deprecated） | ✅ |
| FR-LK-02 | **对称双端建模**：源端与目标端各有对象类型、基数(one/many)、显示名、复数名、api_name、可见性(normal/prominent/hidden)、启停开关 | ✅ |
| FR-LK-03 | 四种基数组合 1:1 / 1:N / N:1 / N:N，列表可按基数筛选 | ✅ |
| FR-LK-04 | 两种落地方式：**字段映射**（两端对象属性对齐）或 **中间表**（`is_data_source_rel=1` + `rel_data_table`） | ✅ |
| FR-LK-05 | **复合主键映射**：每端可配多行映射，按 `seq` 排序 | ✅ |
| FR-LK-06 | 编辑器支持只读 / 编辑模式切换；可脱离 tab 嵌入对象类型的关系图中复用 | ✅ |
| FR-LK-07 | 绑定类型类（`applicable_type='relation'`） | ✅ |
| FR-LK-08 | 删除级联清理 mappings 与类型类绑定 | ✅ |
| FR-LK-09 | 链接上的属性（关系自身带字段） | 🔜 中间表已可承载，UI 未开放 |

**验收**：演示库 4 条链接类型 / 9 条字段映射；列表 JOIN 出源目标端的显示名、图标、颜色与映射数；`PUT /api/link-types/{id}` 对 mappings 做整体覆盖（先删后插），不产生孤儿行。

### 4.4 动作类型（FR-AT）

挂在对象类型上的可执行操作，是本体从「描述」走向「可操作」的部分。

| 编号 | 需求 | 状态 |
|---|---|---|
| FR-AT-01 | 动作 CRUD + 批量删除 + 状态（0 草稿 / 1 已发布 / 2 已停用）；创建走**五步向导** | ✅ |
| FR-AT-02 | 管理页三栏 13 列列表，按主体对象、状态、规则维度筛选 | ✅ |
| FR-AT-03 | 编辑详情为**三栏工作台抽屉**：左导航（概览/表单/规则/安全与提交，表单与规则可展开成下级树）+ 中内容区 + 右预览 | ✅ |
| FR-AT-04 | **表单设计**：分区（section）+ 参数（param），跨分区拖拽排序，分区重命名/删除 | ✅ |
| FR-AT-05 | 参数详情四标签：**值 / 显示 / 覆盖 / 详情**；类型含 string / number / boolean / object / enum，显示类型（输入框/下拉/单选/多选/日期…）自适应 | 🟡 「覆盖」的完整 If-Then 条件树未做 |
| FR-AT-06 | **对象集筛选**（`ObjectSetFilter`）：起始对象集（全部对象 / 现有对象集变量）→ 按属性过滤 → 关联搜索；两种形态复用（字符串多选取值、对象引用参数筛选） | ✅ |
| FR-AT-07 | **8 种规则**：本体编辑类（创建对象 / 修改对象 / 删除对象 / 创建链接 / 删除链接 / 运行函数）+ 副作用类（通知 / Webhook） | ✅ |
| FR-AT-08 | 规则为**有序折叠卡片**，拖拽排序、单条启停、折叠态摘要；新增走类型选择弹框并按冲突禁用不可选项 | ✅ |
| FR-AT-09 | **属性映射**：目标属性 ← 四类值来源（来自参数 / 对象参数属性 / 静态值 / 当前用户·系统时间）；「取值配置」随来源自适应 | ✅ |
| FR-AT-10 | 规则专属完整编辑页：函数（入参映射 + 类型不匹配标红 + 代码预览 + 异常映射）、通知（收件人 + 内容 + 多选渠道 + 链接 + 高级）、Webhook（回写/副作用 + 版本 + 输入参数）、对象/链接（目标对象参数选择器 + 属性映射表 + 关联链接配置） | ✅ |
| FR-AT-11 | **提交标准**：嵌套条件树（all / any 分组） | ✅ |
| FR-AT-12 | **执行引擎**：`POST /{id}/execute`（含 `dry_run`）→ 校验必填 → 解析值来源 → 产出「将写入的实例记录」与「将触发的副作用」→ 落 `ont_action_execution`；`GET /{id}/executions` 查历史 | 🟡 **模拟执行**，不写物理表，见 CON-02 |
| FR-AT-13 | 规则冲突校验：重复创建、删除后修改、副作用前置；入口即禁用 + 顶部红条 + 保存拦截 | ✅ |
| FR-AT-14 | 选择函数 / Webhook / 代码仓库的跳转 | 🔜 当前为占位提示，待函数模块上线 |

**验收**：演示库 9 个动作 / 15 条规则 / 8 个表单分区 / 85 个表单参数 / 67 条属性映射 / 18 个提交条件节点 / 9 条提交标准配置。演示动作 `class_action-demo-create-01` 含 8 参数 + 4 规则（创建对象/创建链接/通知/Webhook）+ 5 节点嵌套提交树，可作为完整样例逐屏对照。

### 4.5 共享属性与结构属性（FR-SP）

| 编号 | 需求 | 状态 |
|---|---|---|
| FR-SP-01 | 共享属性 CRUD + 批量删除；`prop_code` 唯一且创建后不可改 | ✅ |
| FR-SP-02 | 详情抽屉 5 页签：基础信息 / OWL 特性 / 约束 / 引用 / 注释；只读↔编辑切换 | ✅ |
| FR-SP-03 | 携带 7 个 `owl_*` 特性、6 个 `xsd_*` 约束、4 个 `rdfs_*` 语义字段 + JSON 元数据 | ✅ |
| FR-SP-04 | **引用保护**：`ref_count > 0` 时拒绝删除；批量删除自动跳过被引用项 | ✅ |
| FR-SP-05 | 反查引用清单（哪些对象类型属性用了它） | ✅ |
| FR-SP-06 | 新建向导三步：元数据 → 配置 → 保存位置 | ✅ |
| FR-SP-07 | **结构属性**：多个共享属性组合成复合结构，条目可拖拽重排；左右双栏视图 | ✅ |
| FR-SP-08 | 属性格式化规则配置（单条 / 批量），`property_scope='shared'` | ✅ |
| FR-SP-09 | 共享属性变更向已引用它的对象类型属性**传播** | 🚫 现为引用关系而非继承覆盖，改共享属性不回写已落地的类属性；需要统一口径时应在建模阶段引用而非事后同步 |

**验收**：演示库 23 条共享属性 / 6 个结构属性 / 15 条结构条目 / 9 条属性格式化规则；对 `ref_count>0` 的共享属性调 `DELETE` 返回 400 且行数不变。

### 4.6 值类型（FR-VT）

| 编号 | 需求 | 状态 |
|---|---|---|
| FR-VT-01 | 值类型 CRUD；`api_name` 与基础类型保存后不可改 | ✅ |
| FR-VT-02 | 五种**基础类型**：String / Integer / Decimal / Boolean / DateTime | ✅ |
| FR-VT-03 | 五种**约束**：RID / UUID / Length / Regex / Enum，参数存 `constraint_config` JSON | ✅ |
| FR-VT-04 | Enum 约束绑定枚举类型，含枚举选择面板与**使用配置**（最大选择层级 / 是否允许非叶子 / 显示格式 label·code·code_label·full_label） | ✅ |
| FR-VT-05 | 三页签：元数据 / 约束配置 / **预览验证**（输入测试值实时校验 + 配置概要） | ✅ |
| FR-VT-06 | 多级枚举的树形测试选择器 | ✅ |
| FR-VT-07 | 使用配置可复用，可设系统默认 | ✅ |
| FR-VT-08 | 删除时清理分组引用 | ✅ |
| FR-VT-09 | 复合/嵌套值类型（值类型引用值类型） | 🚫 复合结构由「结构属性」承担，不在值类型层再做一套 |

**验收**：演示库 5 个值类型 / 1 条使用配置；预览页对 Regex 约束输入不匹配串时给出失败原因，对多级枚举按 `max_select_level` 截断可选层。

### 4.7 枚举类型（FR-ET）

| 编号 | 需求 | 状态 |
|---|---|---|
| FR-ET-01 | 枚举类型 CRUD；四种类别 `general_single / general_multi / biz_single / biz_multi`，带最大层级 | ✅ |
| FR-ET-02 | 枚举项 CRUD：code / label / parent_code / level / 排序 / 状态；**树视图与表格视图**双模式行内编辑 | ✅ |
| FR-ET-03 | **层次编码规则**：每级的码段名、码长、总长、分隔符、填充字符与填充位置（前补/后补），带示例演算 | ✅ |
| FR-ET-04 | **外部库同步**：数据源 → 表 → 字段 三级联动配置（编码/名称/排序/状态/父级字段 + 过滤 SQL） | ✅ |
| FR-ET-05 | 三种同步模式：**全量覆盖 / 逐级对比（以业务系统为准）/ 增量更新** | ✅ |
| FR-ET-06 | 同步前测试连接、预览 SQL；同步后落日志（新增/更新/删除/失败计数 + 错误信息 + 操作人） | ✅ |
| FR-ET-07 | 引用查询：哪些对象属性 / 接口属性用了本枚举 | ✅ |
| FR-ET-08 | 枚举分组 | 🟡 API 为 stub，未接实际分组 |
| FR-ET-09 | 同步定时调度 | 🚫 属调度平台职责，见 2.4；现为手动触发 |

**验收**：演示库 4 个枚举类型 / 18 个枚举项 / 5 条层次编码规则 / 1 条同步配置 / 2 条同步日志；`POST /{enumId}/sync-test` 在数据源不可达时返回明确错误而非超时挂起。

### 4.8 接口（FR-IF）

| 编号 | 需求 | 状态 |
|---|---|---|
| FR-IF-01 | 接口 CRUD + 启停切换（active↔inactive）；`api_name` 创建后不可改 | ✅ |
| FR-IF-02 | 四页签：概览（编码/语义定义/分类归属/规则配置 JSON）、显示（图标+颜色+RID）、属性、实现 | ✅ |
| FR-IF-03 | **接口属性**行内编辑：编码 / 名称 / XSD 类型 / 值类型引用 / 必填标记(0 可选 / 1 必填 / 2 多值) / 格式化 / 注释 | ✅ |
| FR-IF-04 | 属性批量格式化（复用 PropertyFormatModal） | ✅ |
| FR-IF-05 | **实现关系**：绑定/解绑对象类型，幂等（已关联跳过） | ✅ |
| FR-IF-06 | 列表显示属性数与实现数；批量删除 / 批量启用 / 批量禁用 | ✅ |
| FR-IF-07 | 删除级联清理属性与实现绑定 | ✅ |
| FR-IF-08 | **实现一致性校验**：对象类型是否真的具备接口声明的全部属性 | 🔜 当前只记录绑定关系，不校验属性覆盖 |

**验收**：演示库 9 个接口 / 11 条接口属性 / 13 条实现绑定；重复绑定同一对象类型不产生第二条记录。

### 4.9 类型类（FR-TC）

给属性 / 关系 / 动作挂的元数据标签，驱动上层的渲染与解释。

| 编号 | 需求 | 状态 |
|---|---|---|
| FR-TC-01 | 类型类 CRUD + 废弃切换；`UNIQUE(category_code, name_prefix)` | ✅ |
| FR-TC-02 | **一级分类字典**（Kind）独立 CRUD，内置 11 个分类，带图标 / 颜色 / 适用类型 / 排序 | ✅ |
| FR-TC-03 | 参数定义：`param_type`（boolean / rid / enum / text / numeric / json）× `frontend_component`（switch / 单选 / 多选 / 文本 / 数字 / JSON 编辑器 / RID 选择器） | ✅ |
| FR-TC-04 | 多字段参数走 **JSON Schema**（`param_json`），抽屉右栏按 Schema **自动生成表单并实时预览** | ✅ |
| FR-TC-05 | **绑定实例**：按适用类型（property / relation / action）绑到载体上并存参数值；支持按载体反查 | ✅ |
| FR-TC-06 | `allow_multi_bind` / `is_array_value` / `system_protected`（受保护不可删改） | ✅ |
| FR-TC-07 | **渲染解析**：`/api/tc-render/resolve` 按对象类型或链接聚合出全部绑定与参数值，供图表/图谱真实渲染 | ✅ |
| FR-TC-08 | 类型类枚举字典（6 个枚举名）独立管理与排序 | ✅ |
| FR-TC-09 | 引用页签：统计卡片 + 可筛选绑定列表 | ✅ |

**验收**：演示库 44 个类型类 / 11 个分类 / 4 条绑定实例 / 23 条类型类枚举项；对 `system_protected=1` 的类型类调 `DELETE` 被拒绝。

### 4.10 数据源 —— 数据库类（FR-DS）

| 编号 | 需求 | 状态 |
|---|---|---|
| FR-DS-01 | 数据源 CRUD + 启停 + 批量操作；**12 种数据库类型**（MySQL / PostgreSQL / Oracle / MongoDB / 达梦 / 人大金仓 / 神通 / GBase / PolarDB / TDSQL / GaussDB / OceanBase） | ✅ |
| FR-DS-02 | JDBC 驱动目录按类型分组下发（`/drivers`），选类型自动带出驱动类名 | ✅ |
| FR-DS-03 | **测试连接**（DriverManager 直连），结果写回连接状态（online / offline / risk）与响应时间 | ✅ |
| FR-DS-04 | **连接池监控**：2×2 面板 —— 全局状态（负载条）/ 基础配置（最小空闲、最大连接）/ 实时状态（活跃、等待）/ 响应时间 | ✅ |
| FR-DS-05 | 连接池运维：**热刷新**、**动态调整** maxPoolSize（HikariCP MXBean） | ✅ |
| FR-DS-06 | **物理表同步**：从数据源拉表/视图元数据入 `ont_physical_table`，可改显示名、可删、可分页 | ✅ |
| FR-DS-07 | 概览统计：总量 / 正常 / 风险 + 按类型分布 | ✅ |
| FR-DS-08 | 删除时级联销毁连接池与物理表记录 | ✅ |
| FR-DS-09 | 凭证加密存储 / 密钥托管 | 🔜 见 CON-06 |

**验收**：演示库 14 条数据源记录；`GET /{id}/monitor` 在数据源离线时返回 offline 而非抛异常；`POST /{id}/pool/resize` 后 `GET /{id}/pool` 的 maxPoolSize 随之变化。

### 4.11 外部数据源 —— HTTP 接口类（FR-EX）

与数据库类数据源在列表页合并展示，但走独立的表与接口。

| 编号 | 需求 | 状态 |
|---|---|---|
| FR-EX-01 | 外部数据源 CRUD + 批量删除 + 连通性测试；抽屉式详情（不是整页） | ✅ |
| FR-EX-02 | **9 种鉴权类型**：无鉴权 / API Key / Basic / Bearer / OAuth2 客户端模式 / OAuth2 授权码模式 / CAS 单点登录 / JWT / Digest；表驱动渲染，字段可按其它字段值联动显示 | ✅ |
| FR-EX-03 | **接口目录**：分组 + 接口两级树，各自 CRUD | ✅ |
| FR-EX-04 | **接口管理器**（独立标签页，类 Postman）：左目录 + 多标签 + 地址栏（方法按色高亮 GET绿/POST橙/PUT蓝/DELETE红）+ 请求区（设置/鉴权/Header/Query/Body）+ 响应区（实时响应/请求头/响应头/成功响应示例），横竖分栏均可拖拽 | ✅ |
| FR-EX-05 | Body 五种模式：none / form-data / x-www-form-urlencoded / raw / binary；form-data 行可为文本或**文件**（上限 10MB，文件只在内存不入库） | ✅ |
| FR-EX-06 | **真实发送**：`POST /interfaces/{apiId}/send` 实际发起请求并落调用日志；接口级鉴权可覆盖数据源级 | ✅ |
| FR-EX-07 | **调用日志**与**监控**：日志列表（筛选栏单行）+ 日志详情；按天的成功率与耗时趋势 | ✅ |
| FR-EX-08 | 接口状态：调试 / 完成，目录圆点按状态着色 | ✅ |
| FR-EX-09 | 接口响应到对象类型属性的**结构映射**（把外部接口当作对象类型的数据来源） | 🔜 当前只到「接口可调通」，未接入建模侧取数 |

**验收**：新建一个指向公开 HTTP 服务的外部数据源，在接口管理器发送一次 GET，`GET /{dsId}/logs` 能查到该次调用及耗时；未保存的标签页显示脏标记圆点。

### 4.12 字典管理（FR-DC）

系统级通用字典（区别于业务枚举类型）。

| 编号 | 需求 | 状态 |
|---|---|---|
| FR-DC-01 | 字典定义 CRUD（编码 / 名称 / 说明 / 状态），左侧卡片列表 + 搜索 | ✅ |
| FR-DC-02 | 字典条目 CRUD，支持平铺与**树形**两种取用（`/items` 与 `/items/tree`） | ✅ |
| FR-DC-03 | 按编码直取 `/dict/code/{code}`，供前端 `DictLabel` 组件回显 | ✅ |
| FR-DC-04 | **缓存重载**：全量「发布重载」与单字典重载 | ✅ |
| FR-DC-05 | 缓存实现可切换（本地 / 远程两种 `DictCacheService` 实现） | ✅ |

**验收**：演示库 38 个字典定义 / 156 条字典条目；改一条条目后点「发布重载」，页面回显立即变化。

---

## 5. 功能需求 —— 工作区、检索与全局能力

### 5.1 总览（FR-WS）

| 编号 | 需求 | 状态 |
|---|---|---|
| FR-WS-01 | **15 项统计卡片**三行布局（4 / 6 / 5）：行业·领域·分组·数据源 / 对象类型·关系类型·动作类型·函数·类型类·接口 / 属性·枚举·值类型·结构属性·共享属性 | ✅ |
| FR-WS-02 | 每张卡片显示 `启用数 \| 总数`、中英文名、图标水印、一句话说明；点击跳到对应模块 | ✅ |
| FR-WS-03 | 顶部**行业 / 领域两级 chip 筛选**（横向滚动，可全选可清空），统计随筛选联动 | ✅ |
| FR-WS-04 | 面包屑显示当前统计范围，一键清除筛选 | ✅ |

**验收**：`GET /api/resource/discover/overview` 带行业/领域参数时返回收窄后的 15 项计数；不带参数时与各模块列表总数一致。

### 5.2 图谱（FR-GR）

| 编号 | 需求 | 状态 |
|---|---|---|
| FR-GR-01 | **双画布**（G6）：左侧行业层级树（行业/领域/子领域/分组）+ 右侧对象本体图，中缝可拖拽分配宽度、可单侧折叠 | ✅ |
| FR-GR-02 | 每侧独立：关键字搜索定位、重置、导出图片 | ✅ |
| FR-GR-03 | 悬浮工具条：适配视图、放大缩小、回到初始布局、单侧全屏 | ✅ |
| FR-GR-04 | 树布局可切换（多种 G6 布局），右下角下拉选择 | ✅ |
| FR-GR-05 | 图例可折叠，按节点类型着色 | ✅ |
| FR-GR-06 | 图上直接编辑（增删节点/边） | 🚫 图谱为只读视图，建模在各资源页完成，避免两套编辑口径 |

**验收**：左右画布节点数与 `/api/category/tree`、`/api/graph` 返回一致；导出的 PNG 覆盖当前视口全部节点。

### 5.3 实例探索（FR-IX）

| 编号 | 需求 | 状态 |
|---|---|---|
| FR-IX-01 | 浏览器式**多标签**工作区：一个标签一个探索会话，可新建/关闭；顶部返回与回首页 | ✅ |
| FR-IX-02 | **全局搜索**语法：默认分词（任一命中）、`"引号"` 精确短语、`AND / OR` 含括号、`?` 单字符与 `*` 任意长度通配；带语法帮助浮层与实时建议下拉 | ✅ |
| FR-IX-03 | **范围选择**：双栏抽屉按行业/领域圈定对象类型范围 | ✅ |
| FR-IX-04 | 列表视图：分页 + 关键词 + 多条件筛选（JSON filter）+ 排序；**显示列可跨关联对象取列**（`rel::{classId}::{prop}`） | ✅ |
| FR-IX-05 | 图表视图：分组聚合、统计、矩阵；**图表推荐**弹框 | ✅ |
| FR-IX-06 | **看板持久化**：每个对象类型一份默认看板落库（`ont_explore_design`），另支持命名设计的增删改 | ✅ |
| FR-IX-07 | 详情抽屉：全部属性 + 关联对象，可顺着关联继续跳转 | ✅ |
| FR-IX-08 | 对象类型关系图视图（在探索内查看类型间关系） | ✅ |
| FR-IX-09 | 实例数据来源 | 🟡 **确定性模拟数据**（见 CON-02）：按属性定义生成、内存缓存、重启一致；接真实存储时替换 `InstanceMockService` 即可 |
| FR-IX-10 | 实例的新增 / 修改 / 删除 | 🚫 见 2.4，本产品不承担业务数据写入 |

**验收**：演示库 4 条已保存的探索设计；同一关键词在重启前后返回相同结果集（确定性种子）；筛选 + 排序 + 翻页三者叠加时总数保持一致。

### 5.4 知识浏览器与语义检索（FR-KB）

| 编号 | 需求 | 状态 |
|---|---|---|
| FR-KB-01 | **三源状态卡**：Jena 本体（类数/属性数/三元组数）、向量库（类型/维度/向量条数/表是否初始化）、数据库（产品/方言/表数） | ✅ |
| FR-KB-02 | Jena 七个子页签：类 / 属性 / 成员·个体 / 资源页 / 目录 / 类树 / SPARQL | ✅ |
| FR-KB-03 | **资源页**（仿 Fuseki）：任意 URI 的出站/入站三元组，分页下钻 | ✅ |
| FR-KB-04 | 成员浏览：某个类的个体（实例与枚举值通用），关键词 + 分页 | ✅ |
| FR-KB-05 | **SPARQL 查询**：内置查询模板 + 自由编写，多格式返回 | ✅ |
| FR-KB-06 | 向量库浏览：按命名空间分页列出已向量化的类，查看单条向量详情 | ✅ |
| FR-KB-07 | **向量相似度检索**：输入自然语言，按余弦相似度返回 topK（可设阈值） | ✅ |
| FR-KB-08 | 本体重建与统计：`POST /api/ontology/rebuild`、`GET /api/ontology/stats`、`/health` | ✅ |
| FR-KB-09 | **语义扩展链路**：同义词词典 CRUD + 批量导入、领域术语、停用词、查询扩展（`/api/tool/semantic-expand`）、子图抽取（`/api/tool/subgraph`） | ✅ |
| FR-KB-10 | **同义词自动生成**：单条/批量生成，Milvus 重建 / 从词典同步 / flush / load | 🟡 依赖外部 Milvus 与生成模型可用性 |
| FR-KB-11 | 语义匹配服务：`/api/ontology/preview-expansion`、`/api/ontology/match` 把自然语言映射到本体术语 | ✅ |
| FR-KB-12 | 统一查询入口 `/api/tool/query`：`source = jena / vector / db` 三选一 | ✅ |
| FR-KB-13 | 本体查询转 SQL（自然语言 → 结构化取数） | 🔜 方案已成文（`docs/本体查询转SQL方案.md`），未落地 |

**验收**：演示库 16 条同义词 / 20 条停用词 / 9 条领域术语；`GET /api/tool/status` 三源各自返回可用性；Jena 未就绪时页面显示「未就绪」而非报错白屏。

### 5.5 全局能力（FR-SYS）

| 编号 | 需求 | 状态 |
|---|---|---|
| FR-SYS-01 | **全局搜索**（顶栏 ⌘K）：跨对象 / 关系 / 属性 / 数据源 / 其它，输入即出燕尾下拉 | ✅ |
| FR-SYS-02 | **常用领域选择器**：侧栏顶部聚合显示「行业 \| 领域1/领域2」，作为全局筛选上下文 | ✅ |
| FR-SYS-03 | **深浅主题**：跟随系统 / 手动切换，另有主题色（accent）可选 | ✅ |
| FR-SYS-04 | 侧栏四组导航（工作区 / 资源 / 辅助工具 / 系统配置），分组折叠状态持久化；侧栏可收起 | ✅ |
| FR-SYS-05 | **业务图库**：自定义 SVG 图标分组管理（上传 / 批量删）+ 18 组内置图标，供分类、对象类型、接口、类型类共用 | ✅ |
| FR-SYS-06 | **权限安全**：角色 / 资源范围 / 操作权限 | 🔜 当前为静态占位页，无后端 |
| FR-SYS-07 | **导入导出**：OWL / RDF / Turtle / JSON 快照的导入与导出 | 🔜 当前为占位页，按钮未接实现 |
| FR-SYS-08 | **AI 助手**：自然语言交互式建模 | 🔜 当前为占位对话页，未接推理引擎 |
| FR-SYS-09 | **函数模块** | 🔜 占位页；动作类型的函数规则已预留接入点（FR-AT-14） |
| FR-SYS-10 | 全局反馈规范：`BL.success/warning/error/info`、`BL.confirm`、`BL.prompt`，全系统统一 | ✅ |
| FR-SYS-11 | 三栏资源页统一骨架：PageHeader + CategoryTreeFilter + 列表 + 右侧抽屉（可拖宽、可最大化、无蒙层、宽度持久化） | ✅ |
| FR-SYS-12 | 批量操作栏统一配色：删除红 `#f53f3f` / 启用绿 `#00b42a` / 禁用灰 `#86909c`，均为 outline 风格 | ✅ |

**验收**：演示库 16 个图标分组 / 131 个自定义图标；深色模式下全部资源页与抽屉无对比度失效；任一资源页的抽屉宽度调整后刷新保持。

---

## 6. 非功能需求

| 编号 | 需求 | 指标 |
|---|---|---|
| NFR-01 | 双方言等价 | 同一套 Flyway 迁移在 SQLite 与 PostgreSQL 下均能从空库建起并灌入种子；两库跑同一组接口 smoke 结果一致 |
| NFR-02 | 冷启动 | 删库后启动自动建表 + 灌种子，日志出现 `Successfully applied N migrations`，无需人工干预 |
| NFR-03 | 列表响应 | 演示规模（几十至数百条元数据）下主列表接口 < 300ms；实例列表分页 < 500ms |
| NFR-04 | 前端构建 | `npx vite build` 必须通过，作为每次大改的底线验证 |
| NFR-05 | 后端编译 | `mvn -DskipTests clean compile` 必须通过（JDK 21） |
| NFR-06 | 无外部 UI 依赖 | 前端不引入 UI 组件库；`mvn dependency:tree -Dincludes=com.alibaba:fastjson` 须为空（只留 fastjson2） |
| NFR-07 | 降级可用 | Jena / 向量库 / 外部数据源任一不可用时，相关页面显示明确的「未就绪 / 不可用」而非白屏或超时挂起 |
| NFR-08 | 引用完整性 | 被引用的元数据（共享属性、值类型、对象类型…）删除必须被拒绝并给出引用清单，不产生悬挂引用 |
| NFR-09 | 编码一致性 | `api_name` 全局唯一，遵循《API_NAME_CODING_STANDARD》；提供冲突检测工具（SQL / PowerShell / Java 三种，见 `tools/`） |
| NFR-10 | 视觉一致性 | 新模块须对齐《UI规范》19 章：CSS 前缀命名、抽屉与模态 z-index 分层（抽屉 1010 / 大模态 ≥1200）、章节标题、批量操作栏 |
| NFR-11 | 主题适配 | 所有页面在深浅两色主题下均可读，颜色取 `tokens.css` 变量而非硬编码 |
| NFR-12 | 确定性 | 实例模拟数据的种子固定，重启后同一查询返回同一结果，便于回归比对 |

---

## 7. 已知限制与路线

### 7.1 🔜 规划中

| 项 | 说明 |
|---|---|
| 权限体系（FR-SYS-06） | 角色、资源范围、操作权限；当前四类角色在界面上无隔离 |
| 导入导出（FR-SYS-07） | OWL / RDF / Turtle / JSON 快照双向；跨环境迁移与备份的核心手段 |
| 函数模块（FR-SYS-09、FR-AT-14） | 动作的函数规则、对象类型的函数页签、总览的函数计数都在等它 |
| 外部接口接入建模侧（FR-EX-09） | 接口响应结构 → 对象类型属性的映射，使 HTTP 数据源与数据库数据源等价可用 |
| 本体查询转 SQL（FR-KB-13） | 自然语言 → 本体术语 → 结构化 SQL，方案已成文待落地 |
| 命名空间版本管理（FR-CT-06） | 版本切换、差异比对、按版本发布 |
| 接口实现一致性校验（FR-IF-08） | 绑定时校验对象类型是否覆盖接口声明的全部属性 |
| 参数覆盖条件树（FR-AT-05） | 动作表单参数的完整 If-Then 覆盖规则 |
| 凭证加密（FR-DS-09、CON-06） | 数据源与外部接口凭证接入密钥托管 |
| AI 助手（FR-SYS-08） | 接推理引擎，做交互式建模建议 |
| 分类树导出（FR-CT-13） | 按钮已在位，导出逻辑待接 |

### 7.2 🚫 明确不做及理由

| 项 | 理由 |
|---|---|
| **业务实例的写入存储** | 本产品是元数据平台，实例数据的权威副本在各业务系统。强行落一份会立刻面临一致性、同步、权限三个问题，而这三个问题的解法都在数据集成层而不在建模层。实例探索的模拟数据只服务于「验证建模是否可用」这一目的 |
| **ETL / 同步调度** | 枚举同步是元数据级的、手动触发的例外；成体系的表级同步与调度属数据集成平台 |
| **报表与出图** | 下游《智能报表》以本产品为语义层，两者分工明确：这边定义「有什么」，那边决定「怎么呈现」 |
| **工作流引擎** | 动作类型定义「做什么、按什么顺序、触发什么副作用」，不定义审批链路与流转状态机 |
| **图谱上直接建模**（FR-GR-06） | 建模入口若有两套（表单一套、画布一套），校验规则与冲突检测就得写两遍。图谱定位为核对与浏览 |
| **共享属性变更向下传播**（FR-SP-09） | 引用关系而非继承。需要统一口径应在建模时引用共享属性，而不是事后批量回写已落地的类属性——后者会静默改变已在生产使用的对象定义 |
| **复合值类型**（FR-VT-09） | 复合结构由结构属性承担，不在值类型层再做一套嵌套模型 |
| **多租户** | 单实例单组织部署 |

---

## 8. 附录

### 8.1 模块与路由清单

| 分组 | 路由 | 页面 | 主数据表 |
|---|---|---|---|
| 工作区 | `/workspace/discover` | `Discover.vue` | —（聚合统计） |
| 工作区 | `/workspace/graph` | `GraphG6.vue` | —（聚合） |
| 工作区 | `/workspace/instances` | `Instances.vue` + `instance/*` | `ont_explore_design`（+ 模拟实例） |
| 资源 | `/resources/object-types` | `ObjectTypes.vue` + `objecttype/*` | `ont_class` `ont_class_property` `ont_class_link` `ont_class_ds` `ont_class_group` `ont_class_disjoint_union` `ont_property_equivalent` `ont_property_disjoint` |
| 资源 | `/resources/link-types` | `LinkTypes.vue` + `linktype/LinkTypeEditor.vue` | `ont_link_types` `ont_link_mappings` |
| 资源 | `/resources/action-types` | `ActionTypes.vue` + `actiontype/*` | `ont_class_action` `ont_class_action_rule` `ont_action_form_*` `ont_action_submit_*` `ont_action_execution` 等 |
| 资源 | `/resources/value-types` | `config/ValueTypes.vue` | `ont_value_types` `ont_valuetypes_usage_config` |
| 资源 | `/resources/enum-types` | `config/EnumTypes.vue` | `ont_enum_types` `ont_enum_items` `ont_enum_level_code_rule` `ont_enum_sync_config` `ont_enum_sync_log` |
| 资源 | `/resources/shared-props` | `SharedProperties.vue` + `sharedproperty/*` | `ont_shared_properties` `ont_struct_types` `ont_struct_items` |
| 资源 | `/resources/functions` | `PlaceholderList.vue` | —（占位） |
| 资源 | `/resources/interfaces` | `Interfaces.vue` | `ont_interface` `ont_interface_property` `ont_interface_class` |
| 资源 | `/resources/datasources` | `Datasources.vue` + `datasource/*` | `sys_data_source` `ont_physical_table` `ont_ext_data_source` `ont_ext_api_group` `ont_ext_api_interface` `ont_ext_api_call_log` |
| 资源 | `/resources/datasources/:dsId/apis` | `datasource/ApiManager.vue` | 同上（外部接口三表） |
| 辅助工具 | `/tools/ai` | `AiAssistant.vue` | —（占位） |
| 辅助工具 | `/tools/import-export` | `ImportExport.vue` | —（占位） |
| 辅助工具 | `/tools/knowledge` | `KnowledgeBrowser.vue` | Jena 模型 / 向量库 / 本库 |
| 系统配置 | `/config/category` | `config/Category.vue` | `ont_biz_category` `ont_biz_namespace` `ont_biz_namespace_version` `ont_biz_group` `ont_biz_group_class` |
| 系统配置 | `/config/type-classes` | `config/TypeClasses.vue` | `ont_type_class` `ont_type_class_category_dict` `ont_type_class_bind` `ont_dic_type_class` |
| 系统配置 | `/config/security` | `config/Security.vue` | —（占位） |
| 系统配置 | `/config/dict-manager` | `config/DictManager.vue` | `ont_dict_def` `ont_dict_item` |

### 8.2 REST 接口分组

| 前缀 | 职责 |
|---|---|
| `/api/category` `/api/namespace` `/api/group` `/api/group-ref` | 分类树、命名空间、分组与成员 |
| `/api/class-meta` | 对象类型及其属性、类关系约束、数据源绑定的写侧 |
| `/api/resource` | 对象类型的读侧聚合（列表、详情、全局统计） |
| `/api/link-types` | 链接类型与字段映射 |
| `/api/action-types` | 动作类型、表单、规则、提交标准、执行 |
| `/api/shared-properties` `/api/struct-types` | 共享属性与结构属性 |
| `/api/value-types` `/api/enum-types` | 值类型与枚举类型（含同步） |
| `/api/interface` | 接口、接口属性、实现关系 |
| `/api/type-classes` `/api/tc-category` `/api/tc-bind` `/api/tc-render` `/api/tc-enum` | 类型类全家桶 |
| `/api/datasource` `/api/physical-tables` | 数据库类数据源与物理表 |
| `/api/ext-datasource` | 外部 HTTP 数据源、接口目录、发送、日志、监控 |
| `/api/dict` | 系统字典与缓存重载 |
| `/api/instance` `/api/explore-design` | 实例探索与看板持久化 |
| `/api/search/global` | 全局搜索 |
| `/api/graph` | 图谱数据 |
| `/api/icon-lib` | 业务图库 |
| `/api/property-format` | 属性格式化规则 |
| `/api/tool/*` | Jena / 向量 / 数据库统一查询、语义扩展、子图、同义词 |
| `/api/semantic-search` `/api/synonym-dict` `/api/domain-term` | 语义检索与词典 |
| `/api/ontology/*` | 本体服务（端口 8089）：匹配、重建、统计、类/属性/枚举/值类型只读视图 |
| `/api/health` | 健康检查（smoke test 用） |

### 8.3 数据规模（2026-08-03 演示库快照）

| 维度 | 计数 |
|---|---|
| 数据表总数 | 76（迁移脚本累计 CREATE TABLE 83 次，含跨版本重建） |
| Flyway 已应用迁移 | 25 |
| 分类节点 / 命名空间 / 分组 / 分组成员 | 18 / 22 / 17 / 43 |
| 对象类型 / 属性 / 简单类链接 / 物理表绑定 | 48 / 78 / 20 / 3 |
| 链接类型 / 字段映射 | 4 / 9 |
| 动作 / 规则 / 表单分区 / 表单参数 / 属性映射 / 提交条件节点 | 9 / 15 / 8 / 85 / 67 / 18 |
| 共享属性 / 结构属性 / 结构条目 | 23 / 6 / 15 |
| 值类型 / 使用配置 | 5 / 1 |
| 枚举类型 / 枚举项 / 层次编码规则 / 同步配置 / 同步日志 | 4 / 18 / 5 / 1 / 2 |
| 接口 / 接口属性 / 实现绑定 | 9 / 11 / 13 |
| 类型类 / 分类 / 绑定 / 枚举字典 | 44 / 11 / 4 / 23 |
| 数据源 | 14 |
| 系统字典 / 字典条目 | 38 / 156 |
| 图标分组 / 自定义图标 | 16 / 131 |
| 同义词 / 停用词 / 领域术语 | 16 / 20 / 9 |
| 探索设计（看板） | 4 |

### 8.4 验收执行清单

改动落地后按此清单逐项核验：

1. `mvn -DskipTests clean compile`（JDK 21）通过。
2. `npx vite build` 通过。
3. 删除 `backend/bontolink.db*` 后启动，日志出现 `Successfully applied N migrations`，无 Flyway 校验失败。
4. `curl http://localhost:8088/bontolink/api/health` 返回成功。
5. 逐模块 smoke：`/api/category/tree`、`/api/resource/classes`、`/api/link-types`、`/api/action-types`、`/api/shared-properties`、`/api/value-types`、`/api/enum-types`、`/api/interface`、`/api/type-classes`、`/api/datasource`、`/api/ext-datasource`、`/api/dict`、`/api/instance/object-types`、`/api/tool/status` 均返回 `code=0`（`R.ok` 的成功码是 **0** 不是 200；失败时 HTTP 状态仍为 200，错误码在 body 的 `code` 里）。
6. 引用保护：对有引用的共享属性 / 受保护类型类调删除，返回 400 且数据未变。
7. 深浅两色主题各巡一遍改动页面。
8. PostgreSQL profile 下重跑第 3~6 步。

### 8.5 相关文档

| 文件 | 内容 |
|---|---|
| `使用帮助.html` | **操作层文档** —— 回答「怎么操作」 |
| `UI规范.md` | 完整视觉 / 交互 / 组件规范（19 章） |
| `API_NAME_CODING_STANDARD.md` / `API_NAME_CHEATSHEET.md` | 全局 api_name 编码规范与速查 |
| `CLAUDE.md` | 项目协作指南（技术栈、约定、踩坑清单） |
| `_idx/` | 八个核心模块的预索引（页面 / API / 数据表定位） |
| `tools/` | api_name 冲突检测工具（SQL / PowerShell / Java） |
| `docs/` | 专题方案：Jena 与向量库集成、本体查询转 SQL、语义向量化与同义词生成、知识浏览器优化 |
| `开发文档/` | 原始需求 PDF：动作类型、外部数据源、总览等 |
| `backend/README.md` / `backend/模块化改造总结.md` | 后端四模块架构说明 |

### 8.6 文档版本

| 版本 | 日期 | 内容 |
|---|---|---|
| V1.0 | 2026-08-03 | 初版。覆盖 12 个建模模块 + 工作区 + 语义检索 + 全局能力，共 6 组用例、150 余条功能需求、12 条非功能需求 |
