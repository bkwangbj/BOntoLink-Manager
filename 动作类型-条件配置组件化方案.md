# 动作类型 — 条件配置组件化方案

> 目标:把「配一个条件」这件事在动作类型模块里的 4 套实现收敛成 1 套组件 + 1 份数据模型。
>
> 状态:待评审 · 未实施

---

## 一、现状盘点

「选属性 + 选运算符 + 填值」这件事,模块里有四处实现,三套交互范式:

| 位置 | 文件 | 交互形态 | 数据结构 |
|---|---|---|---|
| 提交标准(向导 step4 / 详情「安全与提交」) | `ConditionGroup.vue` + `ConditionPicker.vue` | chip 点击 → **全屏 Modal 选属性** → 再弹一次 **Modal 选运算符** | `{subject, field, fieldName, dataType, operator, value}` |
| 参数覆盖规则 | `OverrideCondGroup.vue` + `OverrideModal.vue` | 条件行点击选中 → **右侧 40% 面板线性四步**(模板→属性→运算符→值) | 同上,逐字相同 |
| 对象集按属性过滤 | `ObjectSetFilter.vue` | **三个内联下拉平铺**,无分组无嵌套 | `{property_code, operator, value}` ← 字段名不同 |
| 规则内属性映射 | `ActionDetailWorkspace.vue` 内联表格 | 表格行 + BlSelect | `{property_code, prop_operator, value_source, value_content}` |

### 三个真问题

1. **运算符表分裂成三份,key 命名不一致**
   - `ConditionPicker`:`eq ne gt lt ge le contains in regex empty notempty`(11)
   - `overrideModel`:`eq ne contains startsWith gt lt empty notEmpty`(8)
   - `ObjectSetFilter`:`eq ne contains startsWith empty notEmpty`(6)

   同一个「不为空」存成 `notempty` / `notEmpty` 两种字符串。**这是正在产生脏数据的分裂**,后端解析必须兼容两套写法,新增运算符要改三处且容易漏。

2. **兼容性判断各写各的**
   `ConditionPicker` 有 `comparable()` + `numLike()`,`overrideModel` 有 `opsFor()` + 自己的 `numLike()`,`ObjectSetFilter` 干脆不判断——给字符串字段也能选「大于」。

3. **交互范式三套,认知成本高**
   同一个动作详情页里,配提交条件要弹两次全屏框,配覆盖条件要在右侧走四步向导,配对象集过滤又变成一行下拉。用户学三遍。

---

## 二、目标架构

三层结构,自下而上:

```
conditionModel.js          单一事实源: 主体 / 运算符 / 类型兼容 / 摘要 / 迁移
      ↓
ConditionRow.vue           一条条件 = [属性▾] [运算符▾] [值] 一行内联搞定
      ↓
ConditionGroupEditor.vue   条件组: 逻辑(all/any/none) + 嵌套 + 拖拽排序
```

配套一个 `FieldPicker.vue`(锚定式下拉面板,非全屏 Modal)供 `ConditionRow` 选属性用。

### 1. `conditionModel.js`

单一事实源,合并现有三份常量:

```js
export const SUBJECTS = [           // 主体: 对象 / 用户属性 / 表单参数
  { key:'object', label:'对象',     icon:'box',  color:'#165DFF' },
  { key:'user',   label:'用户属性', icon:'user', color:'#722ED1' },
  { key:'param',  label:'表单参数', icon:'edit', color:'#00B42A' },
]

export const OPERATORS = [          // 合并三份的并集, key 统一驼峰
  { key:'eq', label:'等于', dt:'any' },  { key:'ne', label:'不等于', dt:'any' },
  { key:'gt', label:'大于', dt:'num' },  { key:'lt', label:'小于', dt:'num' },
  { key:'ge', label:'大于等于', dt:'num' }, { key:'le', label:'小于等于', dt:'num' },
  { key:'contains', label:'包含', dt:'str' }, { key:'startsWith', label:'开头是', dt:'str' },
  { key:'in', label:'包含于', dt:'any' }, { key:'regex', label:'匹配正则', dt:'str' },
  { key:'empty', label:'为空', dt:'any', noValue:true },
  { key:'notEmpty', label:'不为空', dt:'any', noValue:true },
]

opsFor(dataType)      // 按类型过滤可用运算符
needValue(op)         // 是否需要比较值
comparable(dataType)  // 能否用于条件比较(排除对象引用/结构)
dtLabel(dataType)     // 数据类型中文名
emptyCond() / emptyGroup(logic)
condText(c) / condReady(c) / groupSummary(g)
normalizeCond(c)      // 迁移老 key: notempty→notEmpty, property_code→field
serializeGroup(g)     // 存库前剔除 _k
```

**运算符 key 以驼峰 `notEmpty` 为准**(覆盖规则和对象集过滤已经这么存,占多数)。

### 2. `ConditionRow.vue`

一条条件的完整编辑器,**一行内联完成,不弹全屏框**:

```
[🔵 测站编码 ▾] [等于 ▾] [值输入/下拉] [×]
```

- 属性:点击展开 `FieldPicker` 锚定面板(主体 tab + 搜索 + 类型标注)
- 运算符:BlSelect,选项由 `opsFor(cond.dataType)` 动态给出——**选了字符串字段就不会出现「大于」**
- 值:按 `dataType` 决定控件——枚举给 BlSelect、布尔给开关、日期给日期选择、`noValue` 运算符直接隐藏

props:`cond`、`subjects`(允许哪些主体)、`fields`(各主体的字段表)、`dense`

### 3. `ConditionGroupEditor.vue`

合并现在的 `ConditionGroup` + `OverrideCondGroup`:

props:
- `node` — 条件组
- `logics` — 允许的逻辑,`['all','any']` 或 `['all','any','none']`
- `subjects` / `fields` — 透传给 `ConditionRow`
- `nested` — 是否允许嵌套子组(对象集过滤关掉)
- `hint` — 顶部约束提示文案(如覆盖规则的「只能引用上方字段」)

### 4. `FieldPicker.vue`

取代 `ConditionPicker` 的全屏 Modal:锚定在触发元素下方的下拉面板,内含主体 tab + 搜索框 + 「仅显示兼容选项」+ 带类型标注的列表。不遮挡上下文,不打断心流。

---

## 三、调用点改造

| 调用点 | 改法 |
|---|---|
| 详情「安全与提交」 | `ConditionGroupEditor`,`subjects=[object,user,param]`,`logics=[all,any]`,`nested=true` |
| 向导 step4 提交标准 | 同上,与详情页共用同一组件(现在是两份相似代码) |
| `OverrideModal` | 左侧换 `ConditionGroupEditor`,`subjects=[user,param]`,`logics=[all,any,none]`;**右侧四步面板整块删除**,行内编辑取代,弹框可从 60/40 双栏收成单栏 |
| `ObjectSetFilter` 按属性过滤 | `ConditionGroupEditor` 扁平模式:`nested=false`、隐藏逻辑选择器、`subjects=[object]` |

**净收益**:删除 `ConditionPicker.vue`(172) + `ConditionGroup.vue`(163) + `OverrideCondGroup.vue`(90) + `OverrideModal` 右侧面板(~90) ≈ **-515 行**;新增四个文件 ≈ **+560 行**。行数基本持平,但**实现从 4 份变 1 份**。

---

## 四、数据兼容

老数据落在两处:`ont_action_submit_node`(提交标准) 和参数 config JSON 里的 `overrides`。

- 前端读取时统一走 `normalizeCond()`:`notempty` → `notEmpty`,`property_code` → `field`,缺失的 `dataType` 按字段表回填
- 写入一律用新 key
- **不需要写 SQL 迁移**——`normalizeCond` 在读取路径上兜住,存回去时自然就是新格式
- 后端若有解析运算符的逻辑,需同步兼容两种写法一个版本周期(排查点:`ActionRuleService` / 提交校验相关)

---

## 五、分阶段实施

建议分三批,每批独立可验证、可单独合并:

**阶段 1 — 只做数据层(低风险,高收益)**
新建 `conditionModel.js`,三处调用点改成 import 它的常量,`normalizeCond` 接到读取路径上。UI 完全不动。
→ 先把运算符分裂这个**正在产生脏数据**的问题止血。

**阶段 2 — 条件行组件化**
落地 `ConditionRow` + `FieldPicker`,先替换 `ConditionGroup` 内部的 chip + 双 Modal。提交标准两处先用起来,观察一轮。

**阶段 3 — 条件组统一 + 清理**
`ConditionGroupEditor` 上线,改造 `OverrideModal` 和 `ObjectSetFilter`,删除三个旧组件。

---

## 六、待定决策

1. **属性选择:锚定下拉面板 vs 保留全屏 Modal?**
   建议前者。对象属性再多也能靠搜索解决,全屏框打断上下文——与「表单预览去掉全屏浮层」是同一个判断。

2. **`ObjectSetFilter` 要不要支持逻辑分组?**
   现在多条过滤是隐式 AND。统一到 `ConditionGroupEditor` 后天然支持 all/any,但会改变现有语义和存储结构,需确认对象集查询后端是否支持 OR。**保守做法:先只统一行的样式和运算符表,逻辑分组留空。**

3. **运算符全集要不要收敛?**
   合并后是 12 个。`in`(包含于) 和 `regex`(匹配正则) 目前只有提交标准在用,是否推广到覆盖规则和对象集过滤,取决于后端执行引擎是否都支持。

---

## 七、顺带发现(不在本次范围)

模块内还有**五套弹框外壳**各写各的:`cp-`(ConditionPicker)、`osp-`(对象集选择)、`rlp-`(添加规则)、`rlm-`(入参映射/通用)、`ovm-`(覆盖规则)。z-index、遮罩透明度、圆角、头尾布局各不相同。

建议后续抽一个 `BlModal.vue` 基座(遮罩 + 头/体/尾插槽 + 尺寸档位 + z-index 分层),但**与本方案解耦**,不要混在一批做。
