# BOntoLink 动态本体管理系统 · 初步方案设计

| 项 | 内容 |
|---|---|
| 文档版本 | **V1.0** |
| 编制日期 | 2026-08-03 |
| 阅读对象 | 技术评审 / 架构师 / 研发 / 运维 / 安全 |
| 文档性质 | 方案设计（架构 + 模块 + 技术选型 + 安全评估） |

> **与其它文档的分工**
>
> | 文档 | 回答什么 |
> |---|---|
> | 《需求规格说明书》 | 要做什么、做到什么算合格 |
> | 《使用帮助》 | 怎么操作 |
> | **本文** | **怎么实现、为什么这么实现、有什么风险** |
>
> **口径说明**：第一~三章描述的是当前代码的**实际架构**（已实现部分）与**建议演进**（明确标注 🔜）；第四章的风险项均给出了代码证据位置，可逐条复核。

---

## 目录

- [（一）整体框架设计](#一整体框架设计)
- [（二）功能模块设计](#二功能模块设计)
- [（三）技术方案设计](#三技术方案设计)
- [（四）安全性分析](#四安全性分析)

---

# （一）整体框架设计

## 1.1 设计目标与原则

本系统是一层**语义中间件**：向下对接各业务系统的物理数据，向上给报表、问答、探索等应用提供统一的"业务对象"视图。架构设计围绕五条原则展开。

| 原则 | 含义 | 在架构上的体现 |
|---|---|---|
| **元数据与业务数据分离** | 系统只存"定义"，不存业务数据行 | 无实例存储表；对象类型经映射指向外部物理表；实例探索用确定性模拟数据验证模型 |
| **依赖单向下沉** | 低层不依赖高层 | 后端四模块严格单向：data → base → admin / ontology |
| **重负载可隔离** | 语义推理不能拖垮元数据 CRUD | Jena 推理与向量检索独立进程（8089），OOM 或死锁不影响管理后台（8088） |
| **配置驱动而非代码驱动** | 扩展优先改配置 | 鉴权类型表驱动、类型类参数走 JSON Schema、驱动目录集中登记、迁移脚本增量叠加 |
| **一致性优先于个性化** | 新模块对齐既有模块 | 统一三栏骨架、统一响应包装、统一 CSS 前缀命名、统一批量操作栏 |

## 1.2 分层架构

```
┌──────────────────────────────────────────────────────────────────────┐
│  接入层                                                                │
│  Nginx :80   ── /api/          → admin:8088/bontolink                │
│               ── /api/ontology/ → ontology:8089/bontolink-ontology   │
│               ── /             → SPA 静态资源                          │
└──────────────────────────────────────────────────────────────────────┘
                                  ▼
┌──────────────────────────────────────────────────────────────────────┐
│  展现层  Vue 3 SPA（hash 路由，单页多模块）                             │
│  ├── 工作区：总览 / 图谱 / 实例探索                                     │
│  ├── 资源：对象类型 / 链接 / 动作 / 值类型 / 枚举 / 共享属性 / 接口 / 数据源 │
│  ├── 辅助工具：知识浏览器 / 导入导出🔜 / AI 助手🔜                       │
│  └── 系统配置：行业分类 / 类型类 / 字典 / 权限安全🔜                     │
└──────────────────────────────────────────────────────────────────────┘
                                  ▼  axios /api，统一解包 R<T>
┌──────────────────────────────────────────────────────────────────────┐
│  应用层  bontolink-admin (:8088)          bontolink-ontology (:8089)  │
│  ├── 34 个 REST Controller                ├── OntologyMatchController │
│  ├── 22 个 Service                        ├── Jena 模型管理           │
│  └── 统一响应 R / 全局异常 / CORS          └── 向量检索 / 语义匹配      │
└──────────────────────────────────────────────────────────────────────┘
                                  ▼
┌──────────────────────────────────────────────────────────────────────┐
│  基础设施层  bontolink-base                                            │
│  ├── DynamicDataSourceRegistry  外部数据源连接池注册表（HikariCP）       │
│  ├── DataSourceConnector        连接池 / 直连两种取连接模式              │
│  └── CaseInsensitiveMapObjectFactory  跨方言列名大小写归一               │
└──────────────────────────────────────────────────────────────────────┘
                                  ▼
┌──────────────────────────────────────────────────────────────────────┐
│  数据访问层  bontolink-data                                            │
│  ├── 23+ MyBatis Mapper（注解式 SQL，返回 Map<String,Object>）          │
│  ├── Entity（少量，多数模块直接用 Map）                                 │
│  └── Flyway 迁移：common / sqlite / postgresql 三目录                   │
└──────────────────────────────────────────────────────────────────────┘
                                  ▼
┌──────────────────────────────────────────────────────────────────────┐
│  存储层                                                                │
│  ├── 主库：PostgreSQL（生产）/ SQLite（开发单机）  ~76 张表             │
│  ├── 语义库：Jena 内存模型（可切 TDB2 / Fuseki）                        │
│  ├── 向量库：pgvector / Milvus（可关闭）                                │
│  ├── 缓存：Caffeine 本地（可切 Redis 二级）                             │
│  └── 外部：各业务系统的物理库、外部 HTTP 接口                            │
└──────────────────────────────────────────────────────────────────────┘
```

## 1.3 部署拓扑

### 当前：单机双进程

一台服务器承载全栈，但**以两个独立进程运行**，为拆分预留位置。

| 组件 | 端口 | 职责 | 进程 |
|---|---|---|---|
| Nginx | 80 | 反向代理 + SPA 静态资源 | 独立 |
| bontolink-admin | 8088 | 元数据 CRUD 主服务，context-path `/bontolink` | systemd |
| bontolink-ontology | 8089 | Jena 推理 / SPARQL / 向量检索，context-path `/bontolink-ontology` | systemd |
| PostgreSQL | 5432 | 共享主库，同一 Schema | 独立 |

**为什么双进程而不是单进程**：语义推理与向量计算是重负载且不稳定（内存占用大、可能长时间阻塞）。放在同一进程里，一次 OOM 会连带打掉所有建模操作。拆开之后各自 `-Xmx`、各自启停、各自升级，拆分成本降到"改一行 `proxy_pass`"。

### 演进路径

三步走，**每一步都是配置变更，不动代码**：

| 阶段 | 变化 | 改动点 |
|---|---|---|
| **Step 1** 语义层外迁 | ontology 搬到独立机器（配 Jena TDB2 + pgvector） | Nginx 一行 `proxy_pass` |
| **Step 2** 水平扩展 | admin 无状态，多实例 + 负载均衡；SPA 上 CDN | Nginx upstream |
| **Step 3** 微服务化 | 网关分流 admin / ontology / search / rag | API Gateway 路由表 |

> admin 目前**无状态**（会话不落地，缓存可外置 Redis），这是 Step 2 能成立的前提。引入登录态之后须同步把会话外置，否则水平扩展会退化。

## 1.4 后端模块划分

```
bontolink-data  ──►  bontolink-base  ──►  bontolink-admin
   (JAR)               (JAR)                (Spring Boot :8088)
                                        └►  bontolink-ontology
                                             (Spring Boot :8089)
```

| 模块 | 打包 | 职责 | 边界约束 |
|---|---|---|---|
| **bontolink-data** | JAR | Mapper、Entity、Flyway 迁移脚本 | 只做数据访问，不含业务逻辑 |
| **bontolink-base** | JAR | 动态数据源、连接池、MyBatis 扩展 | 与本体无关的通用基础设施 |
| **bontolink-admin** | Boot | 元数据管理全部业务 | 编译期依赖 ontology 的 service（如同义词扩展） |
| **bontolink-ontology** | Boot | Jena 模型、SPARQL、向量匹配 | 不反向依赖 admin |

包名对应：`...bontolink.data.*` / `...bontolink.base.*` / `...bontolink.*` / `...bontolink.ontology.*`。

> **已知的架构张力**：admin 在编译期直接依赖 ontology 的 jar 并调用其 Java service，同时运行期又通过 HTTP 调 ontology 的 REST。两条调用路径并存会让"ontology 到底在哪个进程执行"变得不确定。**建议**：明确二选一——要么统一走 HTTP（真隔离，但要处理网络失败），要么统一走 jar 内调用（简单，但失去进程隔离的意义）。当前混用的状态在 Step 1 外迁时会暴露问题。

## 1.5 前端架构

| 维度 | 方案 |
|---|---|
| 框架 | Vue 3 `<script setup>` + Vite 5 |
| 路由 | vue-router **hash 模式**（部署无需服务端 rewrite） |
| 状态 | Pinia 仅存全局态（主题、侧栏、已选领域）；**业务状态一律留在组件本地** |
| 样式 | **原生 CSS**，无 UI 框架；CSS 变量 tokens 支撑深浅双主题 |
| 组件 | 全部自研：`BlSelect` / `PageHeader` / `CategoryTreeFilter` / 各类 PickerModal / `DraggableHandles` 等 |
| HTTP | 单例 axios，`baseURL=/api`，响应拦截器自动解包 `R<T>`、失败统一弹提示 |
| 命名隔离 | 每模块独立 CSS 前缀（`ot-` / `lk-` / `vt-` / `et-` / `sp-` / `if-` / `ds-` / `tc-` / `cat-` / `at-`…） |

**为什么不用 UI 框架**：本系统的核心界面（可拖拽双向嵌套抽屉、三栏动作工作台、双画布图谱、类 Postman 接口管理器）没有一个是组件库的标配形态，用框架反而要大量 `::v-deep` 覆盖。代价是自研组件的完备性需要靠《UI规范》与代码评审来兜。

**统一骨架**：所有资源页都是 `PageHeader + 分类树 + 列表 + 右侧抽屉`。抽屉左缘可拖拽、宽度持久化到 localStorage、无蒙层（允许背景切换记录）、可最大化。层级约定：抽屉 `z-index:1010`，大模态 `≥1200`。

## 1.6 三条核心数据流

**① 建模写入链路**
```
前端表单 → axios /api/xxx → Controller 参数校验 → Service 事务编排
        → Mapper 写主表 + 子表（整体覆盖策略）→ 引用完整性校验 → 返回最新态
```
关键设计：子表（属性映射、规则、表单参数）一律**先删后插的整体覆盖**，避免增量 diff 的复杂度与孤儿行。

**② 元数据消费链路**
```
下游应用 → GET /api/resource/classes/{id}
        → 一次返回 属性 + 链接 + 动作 + 接口 + 数据源 五段嵌套
        → 应用按 tc-render/resolve 拿到类型类绑定决定渲染方式
```
关键设计：消费侧走**聚合读接口**而非多次往返，`ResourceController`（读）与 `ClassMetaController`（写）读写分离。

**③ 语义检索链路**
```
自然语言 → 分词/停用词 → 同义词扩展 → 领域术语归一
        → 向量召回（余弦 topK）+ Jena 结构查询（SPARQL）
        → 子图抽取 → 返回命中的本体术语
```

## 1.7 系统边界

| 交互方 | 方向 | 方式 | 边界 |
|---|---|---|---|
| 业务物理库 | 读 | JDBC 连接池 | **只读**：同步表结构、同步枚举字典；不写业务数据 |
| 外部 HTTP 系统 | 读 | HttpClient + 9 种鉴权 | 当前只到"接口可调通 + 落日志" |
| 智能报表等下游 | 被读 | REST 元数据接口 | 下游只读消费，不回写本体 |
| 嵌入模型服务 | 调用 | SiliconFlow / DeepSeek HTTP API | 用于向量化与同义词生成，可切 mock |
| Milvus / pgvector | 读写 | SDK / SQL | 可整体关闭（`vector.type: none`） |

---

# （二）功能模块设计

## 2.1 模块全景与依赖

```
                        ┌─────────────────┐
                        │  行业分类/命名空间 │  ← 所有模块的归属与筛选维度
                        └────────┬────────┘
                                 │
   ┌────────────┬────────────────┼────────────────┬─────────────┐
   ▼            ▼                ▼                ▼             ▼
┌──────┐   ┌────────┐      ┌──────────┐    ┌─────────┐   ┌─────────┐
│枚举类型│──►│ 值类型  │──►   │ 共享属性  │──► │对象类型 │◄──│  接口   │
└──────┘   └────────┘      └────┬─────┘    └────┬────┘   └─────────┘
                                │  结构属性      │
                                ▼               ├──► 链接类型
                          ┌──────────┐          ├──► 动作类型
                          │ 类型类    │─────────►┤
                          └──────────┘          └──► 物理表映射
                                                        ▲
                                                        │
                                          ┌─────────────┴─────────────┐
                                          │  数据源（DB 类 / HTTP 类）  │
                                          └───────────────────────────┘
```

依赖方向**自左向右、自下而上**，没有环。这决定了建模的推荐顺序：先分类 → 再枚举/值类型 → 再共享属性 → 再对象类型 → 最后链接与动作。

## 2.2 元数据内核

### 2.2.1 行业分类与命名空间

**定位**：全系统的坐标系。每个资源必须落在某个分类节点下，六个资源模块共用同一棵树做过滤。

| 设计点 | 方案 | 理由 |
|---|---|---|
| 层级 | 行业 → 领域 → 分组，`category_type` 1/2/3，`parent_id` 自关联 | 三层足够表达业务归属；无限层会让统计与面包屑复杂化 |
| 命名空间挂载点 | **挂在领域上**，不挂行业也不挂分组 | 领域是本体术语的自然作用域，粒度过粗会撞名，过细会碎片化 |
| 树状态隔离 | 六个模块各用独立 `store-key` 记住选中节点 | 在对象类型选了"水文"，切到链接不该跟着变——两者是独立的工作上下文 |
| 统计 | 单点 `/{id}/stats` + 批量 `stats-batch?ids=` | 树节点展开时批量取，避免 N 次往返 |
| 搜索 | 中文 / 拼音 / 拼音首字母三路匹配 | 分类多了以后打 `sw` 比翻树快 |

### 2.2.2 对象类型

**定位**：本体核心实体，是所有下游消费的入口。

**详情抽屉的信息架构**——4 组 15 个页签，按颜色分组，这是把"一个类的全部信息"组织进有限屏幕的关键：

| 组 | 页签 | 回答什么问题 |
|---|---|---|
| 基础信息（蓝） | 概览 / 属性 | 它是什么、有哪些字段 |
| 关系（橙） | 链接关系 / 对象图谱 | 它和谁有关系 |
| 规则约束（紫） | 等价类 / 不相交类 / 互斥并集 / 等价属性 / 不相交属性 / 接口 | 它遵守什么 OWL 约束 |
| 业务应用（绿） | 动作 / 函数 / 被引用 / 数据源 / 使用情况 / 时序图表 / 事件时间轴 | 它被谁用、落在哪 |

**二级嵌套抽屉**：在链接关系图上点开一条链接，会叠出第二层抽屉（最多 8 层栈）。这解决了"编辑 A 时需要顺手改 B"却不想丢失 A 的上下文的问题。

**创建走向导 + 事务**：`ObjectTypeCreateService.create()` 用 `@Transactional` 一次建齐"类 + 数据源绑定 + 属性字段映射"，中途失败整体回滚，不留半截数据。

### 2.2.3 链接类型

**核心设计是对称双栏**——上源端、下目标端，字段完全对等（对象类型 / 基数 / 显示名 / 复数名 / api_name / 可见性 / 启停）。

关系本身是对称的，正反两个方向都需要独立命名（"所属河流" vs "下辖测站"），把它做成对称布局比做成"主从"更贴合心智。

**两种落地方式**：

| 方式 | 开关 | 映射内容 | 适用 |
|---|---|---|---|
| 字段映射 | `is_data_source_rel=0` | 两端对象属性对齐 | 1:1 / 1:N / N:1，等价外键 |
| 中间表 | `is_data_source_rel=1` + `rel_data_table` | 额外填中间表列名 | N:N |

复合主键经 `ont_link_mappings.seq` 支持多行映射。保存时映射**整体覆盖**（先删后插）。

### 2.2.4 属性体系

属性是建模里最细的一层，一行属性承载四类信息：

| 类别 | 字段 |
|---|---|
| 基本 | 编码 / 显示名 / `prop_type`(data·object·annotation) / XSD 数据类型 / 排序 |
| 语义约束 | 值类型引用 / 值域类 / 父属性 / 7 个 `owl_*` 特性 / 6 个 `xsd_*` 约束 |
| 业务标记 | 主键 / 必填 / 派生 / 多值 |
| 物理映射 | `class_ds_id` / `physical_table` / `physical_column` |

**object 属性与链接类型的分工**：object 属性只是"声明这个字段指向那个类"；要在图谱上成为一条边、要能跨对象取数，必须建链接类型。两者不是替代关系。

## 2.3 类型与约束体系

这五个模块共同构成"取值该长什么样"的约束层，是本系统区别于普通元数据表的核心。

### 2.3.1 值类型 —— 约束的最小单元

```
基础类型（5 种）        ×        约束（5 种）
String / Integer                RID / UUID / Length
Decimal / Boolean / DateTime    Regex / Enum
```

约束参数存 `constraint_config` JSON，Enum 约束额外绑 `enum_id` 与**使用配置**（最大选择层级 / 是否允许非叶子 / 显示格式四选一）。

**为什么把使用配置独立成表**：同一个"行政区划"枚举，在 A 场景只能选到市级、显示"编码+名称"，在 B 场景可选到县级、只显示名称。使用配置独立后可复用、可设系统默认，不必为每种用法复制一份值类型。

**不可变字段**：`api_name` 与 `base_type` 保存后锁死——已经有属性按这个类型落地了，改了会静默破坏已有数据。

### 2.3.2 枚举类型 —— 可同步的业务字典

| 能力 | 设计 |
|---|---|
| 四种类别 | `general_single / general_multi / biz_single / biz_multi` |
| 层次编码规则 | 每级配码段名、码长、总长、分隔符、填充字符、填充位置（前补/后补），带实时示例演算 |
| 双视图 | 树视图看层次、表格视图批量改 |
| 外部同步 | 数据源 → 表 → 字段 三级联动 + 过滤 SQL |

**三种同步模式的取舍**：

| 模式 | 语义 | 适用 |
|---|---|---|
| 全量覆盖 | 清空重灌 | 首次同步 / 本地已改乱 |
| 逐级对比 | 以业务系统为准，逐级 diff 增删改 | 常规，对方是权威源 |
| 增量更新 | 只加不改不删 | 本地有人工补充，不能被冲掉 |

同步为**手动触发**，不做定时调度——调度属数据集成平台职责（见 SRS 2.4）。

### 2.3.3 共享属性 / 结构属性

**共享属性是引用，不是继承**。这是一个刻意的决策：

| 方案 | 后果 |
|---|---|
| 继承（改共享属性自动回写所有引用方） | 一次修改可能静默改变几十个已在生产使用的对象定义，且无法为个别对象保留例外 |
| ✅ 引用（改动不回写） | 要统一口径就在建模时引用；已落地的定义保持稳定 |

**结构属性**是共享属性的组合包（"联系方式" = 姓名 + 电话 + 邮箱），经 `ont_struct_items` 做 M:N 关联并可拖拽排序。

**引用保护**：`ref_count > 0` 拒绝删除，批量删除自动跳过被引用项并回报跳过清单。

### 2.3.4 接口 —— 抽象契约

纯声明，不落地。`ont_interface_class` 记录哪个对象类型实现了它。当前只记录绑定关系，**不校验对象类型是否真的具备接口声明的全部属性**（一致性校验列入路线图）。

### 2.3.5 类型类 —— 驱动上层渲染的元数据标签

三级结构：**一级分类（Kind，内置 11 个）→ 类型类定义 → 绑定实例**。

参数配置有两档：

| 复杂度 | 方案 |
|---|---|
| 单值参数 | `param_type`（boolean/rid/enum/text/numeric/json）× `frontend_component`（switch/单选/多选/文本/数字/JSON编辑器/RID选择器） |
| 多字段参数 | `param_json` 写 **JSON Schema**，抽屉右栏按 Schema 自动生成表单并实时预览 |

**渲染解析接口** `/api/tc-render/resolve` 是关键：上层不逐条查绑定，而是传对象类型或链接一次拿回全部绑定与参数值。这是图表、图谱做"按配置渲染"的地基。

## 2.4 动作引擎

动作是本系统从"描述"走向"可操作"的部分，也是模块复杂度最高的一块（表结构约 26 张）。

### 2.4.1 三段式模型

```
表单（收集输入）  →  规则（有序执行）  →  提交标准（准入判断）
   分区 + 参数        8 种规则卡片          all/any 嵌套条件树
```

### 2.4.2 规则设计

| 类别 | 规则 | rule_type |
|---|---|---|
| 本体编辑 | 创建对象 / 修改对象 / 删除对象 / 创建链接 / 删除链接 / 运行函数 | 1 |
| 副作用 | 通知 / Webhook | 2 |

**关键决策：规则是有序列表而非分类分组**。因为规则的语义依赖执行顺序（先创建对象才能创建指向它的链接；副作用必须在本体编辑之后）。做成一个可拖拽的有序列表，顺序即语义。

**类型专属配置全部序列化进 `rule_config` JSON**，`rule_type` 由规则种类反推。好处是新增规则类型不用改表结构；代价是这些字段无法用 SQL 直接检索（现有的规则维度计数只能用 `LIKE '%"kind":"xxx"%'` 兜底，是一处已知的技术债）。

**冲突校验分两级**：

| 层级 | 拦什么 | 表现 |
|---|---|---|
| 入口拦截 | 能静态判定的（已有创建对象规则则禁用该卡片；函数规则与其它本体编辑规则互斥） | 添加规则弹框里直接禁用 |
| 保存拦截 | 顺序类冲突（删除排在修改前、副作用前置） | 顶部红条 + 阻止保存 |

### 2.4.3 值来源解析

每条本体编辑规则的核心是属性映射表：**目标属性 ← 值来源**，四类来源：

| 来源 | 取值配置控件 |
|---|---|
| 来自参数 | 表单参数下拉 |
| 对象参数属性 | 两级级联：对象参数 → 该参数所指类的属性 |
| 静态值 | 文本框 |
| 当前用户 / 系统时间 | 自动 |

"取值配置"这一格随所选来源动态换控件，避免了配一堆永远用不上的字段。

### 2.4.4 执行引擎

```
校验必填参数 → 解析各规则的值来源 → 产出「将写入的实例记录」+「将触发的副作用」
             → 落 ont_action_execution → 返回执行计划
```

**当前是模拟执行，不写物理表**。这不是偷懒，而是 CON-02 的直接推论：系统没有真实实例存储，"写入"无处可写。执行引擎的价值在于**验证配置是否自洽**（必填齐不齐、值来源解析得出来吗、规则顺序对不对）。将来接入真实实例存储时，把"模拟 apply"换成真写即可，前面的校验与解析逻辑不变。

## 2.5 数据接入层

### 2.5.1 数据库类数据源

| 能力 | 实现 |
|---|---|
| 12 种数据库 | 驱动目录集中登记在 `DataSourceService.driverCatalog()`，选类型自动带出驱动类名 |
| 连接测试 | DriverManager 直连（不占用池），结果回写 `connect_status` + `response_ms` |
| 连接池 | HikariCP，经 `DynamicDataSourceRegistry` 按数据源 ID 动态注册/销毁 |
| 池运维 | 热刷新（改配置不重启）、动态调整 maxPoolSize（MXBean） |
| 物理表同步 | 拉表/视图元数据入 `ont_physical_table`，可改显示名 |

**国产库支持**（达梦 / 人大金仓 / 神通 / GBase / GaussDB / OceanBase / PolarDB / TDSQL）是面向政企交付的必要项，通过驱动目录 + JDBC 标准接口统一处理，无需为每种库写适配代码。

### 2.5.2 外部 HTTP 数据源

**鉴权表驱动**是这里最值得说的设计：9 种鉴权类型的字段定义集中在一张表（`authModel.js` 的 `AUTH_FIELDS`），表单按配置动态渲染，字段间支持 `showIf` 联动（Bearer 的"自动续期"打开后才出现"刷新令牌地址"）。**新增第 10 种鉴权只需在这张表里加一项**，不动表单代码。

**接口管理器**是一个类 Postman 的独立工作台：左目录树 + 多标签 + 地址栏 + 请求区（设置/鉴权/Header/Query/Body）+ 响应区（实时响应/请求头/响应头/成功响应示例），横竖分栏可拖拽。做成独立标签页而非抽屉，是因为接口调试是一个需要长时间停留、反复试错的场景，塞进抽屉会挤。

**接口级鉴权覆盖数据源级**：大部分接口用数据源默认，个别特殊的单独配。

## 2.6 语义服务

```
建模数据（PostgreSQL）
      │
      ├──► Jena 模型构建（类 / 属性 / 层次 / 约束 → 三元组）
      │         └─► SPARQL 查询、类树、资源页（出站/入站三元组）
      │
      └──► 向量化（嵌入模型）
                └─► 向量库（pgvector / Milvus）→ 余弦相似度 topK 召回
```

**语义扩展链路**（服务于自然语言问答）：

```
用户输入 → 停用词过滤 → 同义词扩展（sys_synonym_dict）
        → 领域术语归一（ont_domain_term）→ 向量召回 + 结构查询 → 子图抽取
```

**同义词自动生成**：调用嵌入/生成模型批量产出候选（`sys_synonym_candidate`），人工确认后入词典，并可同步到 Milvus。

**降级设计**：`jena-enabled`、`vector.type`、`embedding.provider` 三个开关可独立关闭，任一不可用时知识浏览器显示"未就绪"而非白屏——语义层是增强能力，不能成为建模功能的硬依赖。

## 2.7 消费侧

| 模块 | 定位 | 关键设计 |
|---|---|---|
| **总览** | 入口地图 | 15 项统计三行布局（4/6/5），行业·领域两级 chip 筛选联动全部数字 |
| **图谱** | 核对与浏览 | 双画布（行业层级树 ↔ 对象本体图）中缝可拖拽；**只读**，建模入口只留资源页一处，避免校验规则写两遍 |
| **实例探索** | 验证模型可用性 | 浏览器式多标签；搜索支持分词/引号短语/AND-OR/通配符；显示列可跨关联对象取；看板落库而非 localStorage |
| **知识浏览器** | 语义层核对 | 三源状态卡 + Jena 七子页 + 向量检索 |
| **全局搜索** | 跨模块定位 | ⌘K，跨对象/关系/属性/数据源，燕尾下拉而非弹框 |

**实例探索的数据来源是刻意的取舍**：`InstanceMockService` 按对象类型的属性定义**确定性生成**实例池（内置水利领域词库，种子固定，重启一致），懒加载缓存在内存。它的目的是回答"这个模型建得能不能用"——列配得对吗、筛选逻辑对吗、聚合出得来吗，而不是提供业务数据。

## 2.8 平台支撑

| 组件 | 作用 |
|---|---|
| **字典管理** | 系统级通用字典（界面下拉、状态码），两级缓存（本地 Caffeine / 可切 Redis），支持全量与单字典重载 |
| **业务图库** | 自定义 SVG 分组管理 + 18 组内置图标，供分类/对象/接口/类型类共用 |
| **属性格式化** | 统一的属性显示规则（小数位/单位/前后缀/日期格式），支持批量应用 |
| **统一响应** | `R<T> = {code, msg, data}`，成功码 **0** |
| **全局异常** | `IllegalArgumentException` → 400（warn，无堆栈）；其它 → 500（error，全堆栈），日志带"方法 + 路径" |

## 2.9 关键设计决策汇总

| 决策 | 备选方案 | 为什么这么选 |
|---|---|---|
| Mapper 返回 `Map<String,Object>` 而非 Entity | 每表一个 POJO | 元数据表字段多（属性表 40+ 列）且频繁增改，Entity 维护成本高于收益；代价是缺少编译期类型检查 |
| 子表整体覆盖（先删后插） | 增量 diff | 避免 diff 的复杂度与孤儿行；代价是并发编辑会互相覆盖（见 4.3 风险 12） |
| 规则配置存 JSON | 每种规则一张表 | 新增规则类型不动 schema；代价是无法 SQL 检索（现用 LIKE 兜底） |
| 图谱只读 | 图上可编辑 | 建模入口只留一处，校验规则不用写两遍 |
| 实例用模拟数据 | 接真实库 / 不做探索 | 在无实例存储的前提下仍能验证模型可用性 |
| 无 UI 框架 | Element / Antd | 核心界面形态均非组件库标配，覆盖成本高于自研 |
| hash 路由 | history 模式 | 部署无需服务端 rewrite 配置 |
| 双进程部署 | 单进程 | 语义推理 OOM 不拖垮元数据 CRUD |

---

# （三）技术方案设计

## 3.1 技术选型

| 层 | 选型 | 版本 | 选型理由 |
|---|---|---|---|
| 后端框架 | Spring Boot | 3.3.5 | LTS 生态，与 JDK 21 匹配 |
| 语言 | Java | 21 | 虚拟线程、模式匹配、record；LTS |
| 持久层 | MyBatis Spring Boot Starter | 3.0.3 | 注解式 SQL，对多方言的手工控制力强于 JPA |
| 主库 | PostgreSQL / SQLite | 42.7.4 / 3.45.3 | PG 生产（JSON、pgvector），SQLite 开发零部署 |
| 迁移 | Flyway | 10.10.0 | 版本化增量，天然支持多方言目录 |
| 缓存 | JetCache（Caffeine / Redis） | 2.7.7 | 本地与远程二级缓存切换只改配置 |
| 语义 | Apache Jena | 5.2.0 | OWL 推理与 SPARQL 的事实标准 |
| 向量 | Milvus SDK / pgvector | 2.2.16 | 二选一，可整体关闭 |
| JSON | **FastJSON2** | 2.0.51 | 已排除 fastjson v1 全部传递依赖 |
| 前端 | Vue 3 + Vite | 5.x | — |
| 图可视化 | AntV G6 | — | 双画布图谱、关系图 |

## 3.2 数据库设计策略

### 3.2.1 命名与主键规范

| 项 | 规范 | 示例 |
|---|---|---|
| 表名 | `ont_{模块}_{对象}`，小写下划线 | `ont_class_property` |
| 字段 | `snake_case` | `category_code` / `prop_type` |
| 主键 | `id TEXT PRIMARY KEY`，格式 `{前缀}-{UUID}` | `class-a1b2…` / `link-types-…` |
| RID | `ri.ont.{module}.{code}` | `ri.ont.interface.…` |
| 状态 | 整数 `1/0`，或文本 `active/inactive/deprecated` | 见下 |

**TEXT 主键而非自增整数**的理由：跨环境迁移（导入导出、多库合并）时不会撞 ID；前缀自带类型信息，日志里一眼看出是什么资源。代价是索引体积与 join 成本略高，在元数据这个数据量级可忽略。

### 3.2.2 双方言兼容

这是整个数据层最需要小心的地方。

| 差异点 | SQLite | PostgreSQL | 统一方案 |
|---|---|---|---|
| 时间戳列 | `TEXT` | `TIMESTAMP` | 建表各写各的；**Java 侧禁止传字符串** |
| 时间戳赋值 | 接受 `'2026-07-30 10:00:00'` | 拒绝隐式转换 | SQL 直接写 `CURRENT_TIMESTAMP`，或传 `java.sql.Timestamp` |
| 列名大小写 | 保持原样 | 结果集可能全小写 | `CaseInsensitiveMapObjectFactory` 归一 |
| 元数据查询 | `sqlite_master` / `PRAGMA` | `information_schema` | 服务层按方言分支 |

**迁移目录结构**：
```
db/migration/
├── common/       两方言通用（种子数据为主）
├── sqlite/       SQLite 方言 DDL
└── postgresql/   PostgreSQL 方言 DDL
```
配置 `locations: classpath:db/migration/common,classpath:db/migration/{vendor}`，`out-of-order: true` 以适配双目录版本号交错。

**铁律**：已发布的 `Vn` 脚本永不修改，改动一律新建 `Vn+1`。

### 3.2.3 引用完整性

系统**不依赖数据库外键**，而在 Service 层做引用检查。理由：SQLite 的外键约束默认关闭且行为与 PG 有差异；更重要的是删除时需要给用户**可读的引用清单**（"这个共享属性被 3 个对象类型的 5 个属性引用"），而不是一个约束冲突异常。

## 3.3 接口设计规范

### 3.3.1 统一响应

```java
R.ok(data)          → { "code": 0,   "msg": "ok",  "data": {...} }
R.error(400, msg)   → { "code": 400, "msg": "..." }
```

**成功码是 0 不是 200**。失败时 HTTP 状态码仍为 200，错误信息在 body 的 `code` 里。

> **权衡说明**：这种"HTTP 恒 200 + 业务码"的风格便于前端统一拦截，但会让 HTTP 层的监控（Nginx 日志、APM 的错误率统计、网关的熔断规则）全部失效——所有失败在基础设施眼里都是成功。**建议演进**：4xx/5xx 场景同步设置对应的 HTTP 状态码，body 结构保持不变，前端拦截器兼容两种。

### 3.3.2 路径规范

```
/api/{资源}                    GET 列表 / POST 新建
/api/{资源}/{id}               GET 详情 / PUT 更新 / DELETE 删除
/api/{资源}/batch-delete       POST 批量删除
/api/{资源}/{id}/{子资源}       子资源 CRUD
```

**读写分离的特例**：对象类型的写侧在 `/api/class-meta`，读侧聚合在 `/api/resource`。因为读侧需要一次返回五段嵌套结构，与写侧的细粒度 CRUD 是两种完全不同的形态。

### 3.3.3 前端 API 客户端

`frontend/src/api/index.js` 集中定义全部 29 个 `xxxApi` 对象，标准方法名 `list / get / create / update / remove / batchRemove`。集中定义的好处是接口变更时有唯一改动点，且能一眼看出系统的完整 API 面。

## 3.4 前端工程方案

### 3.4.1 状态管理

| 状态类型 | 存放位置 |
|---|---|
| 全局（主题、侧栏折叠、已选领域） | Pinia `useAppStore` |
| 跨会话偏好（抽屉宽度、树展开、导航分组折叠） | localStorage |
| 业务数据（列表、表单、筛选） | **组件本地 ref** |

**刻意不把业务数据放 Pinia**：资源页之间没有共享业务状态的需求，放进全局 store 只会带来生命周期管理负担与内存驻留。

### 3.4.2 层级约定

| 元素 | z-index |
|---|---|
| 详情抽屉 | 1000 / 1010（二级） |
| 大模态弹框 | ≥ 1200 |
| 全局提示 | 更高 |

大弹框必须高于抽屉，否则在抽屉里打开的选择器会被遮住。

### 3.4.3 主题

CSS 变量集中在 `tokens.css`，深浅两套 + 三种主题色（accent）。组件一律取变量不硬编码颜色。深色模式经 `:root[data-theme="dark"]` 覆盖。

### 3.4.4 构建

`npx vite build` 是每次改动的底线验证。已知问题：Windows + 杀软实时扫描下 esbuild 偶发 `Access is denied`，重跑即过，非代码问题。

## 3.5 缓存方案

| 缓存 | 实现 | 失效策略 |
|---|---|---|
| 字典 | JetCache（Caffeine，可切 Redis 二级） | 手动"发布重载"，支持全量与单字典 |
| 实例模拟数据 | `ConcurrentHashMap` 进程内 | 懒加载，进程生命周期内不失效（种子确定性，无需失效） |
| 枚举项（语义扩展用） | 服务内缓存 | 提供 `clearEnumCache` 接口 |
| OAuth2 令牌 | 内存 Map，key = `tokenUrl\|clientId` | 按过期时间 |

`bontolink.cache.type` 三档：`local`（仅 Caffeine）/ `remote`（+Redis 二级）/ `none`。多实例部署时**必须切 remote**，否则各实例字典缓存不一致。

## 3.6 动态数据源与连接池

```
sys_data_source 表  ──►  DynamicDataSourceRegistry
                              │  按 ds_id 注册 HikariDataSource
                              ├─► DataSourceConnector（池化取连接）
                              └─► DriverManager 直连（仅连接测试用）
```

池参数集中在 `bontolink.datasource.pool`（默认 maxPoolSize=5 / minIdle=2 / connectionTimeout=30s / idleTimeout=5min / maxLifetime=30min）。单个数据源可在界面上动态调整 maxPoolSize 与热刷新。删除数据源时级联销毁池。

MongoDB 走独立的 `mongo_url` 路径，不进 JDBC 池。

## 3.7 语义层技术方案

| 配置项 | 当前默认 | 可选 | 说明 |
|---|---|---|---|
| `jena-enabled` | `false` | true | 关闭时知识浏览器 Jena 页显示未就绪 |
| `storage-mode` | `memory` | tdb2 | 生产建议 TDB2 持久化 |
| `reasoner-type` | `owl` | rdfs / none | 推理级别影响性能 |
| `vector.type` | `none` | pgvector / milvus | — |
| `dimension` | 768 | — | 与嵌入模型对齐（bge-base-zh-v1.5） |
| `embedding.provider` | `siliconflow` | mock | mock 用于离线开发 |
| `fuseki.enabled` | `false` | true | 独立 SPARQL 端点 |

**内存模式的边界**：当前 `storage-mode: memory` 意味着 Jena 模型进程重启即失，需要 `POST /api/ontology/rebuild` 重建。元数据规模（几十个类）下重建很快，但类数上千后应切 TDB2。

## 3.8 性能设计

| 场景 | 措施 |
|---|---|
| 列表查询 | 计数用子查询而非 N+1；分页在 SQL 层 |
| 树与统计 | 批量统计接口 `stats-batch?ids=` 一次取多节点 |
| 详情聚合 | `/api/resource/classes/{id}` 一次返回五段嵌套，避免多次往返 |
| 类型类渲染 | `tc-render/resolve` 按载体聚合，`category-map` 批量取 |
| 字典 | 两级缓存，界面回显不打库 |
| MyBatis | `default-fetch-size: 100`，`default-statement-timeout: 30` |
| 前端 | 路由级懒加载；大列表分页；图谱按需渲染 |

**目标**：元数据列表 < 300ms，实例列表分页 < 500ms。

## 3.9 可观测性与运维

| 项 | 现状 | 建议 🔜 |
|---|---|---|
| 日志 | `com.beiktech.bontolink: DEBUG`，全局异常带"方法 + 路径" | 生产降到 INFO；接结构化日志 + 集中采集 |
| 健康检查 | `/api/health` | 补 Actuator，暴露 `/health` `/metrics` 给探针 |
| 指标 | 连接池 MXBean 指标（界面可见） | 接 Micrometer + Prometheus |
| 链路追踪 | 无 | 微服务化前接入 |
| 审计 | **无**（见 4.3 风险 9） | 元数据变更审计表 |

## 3.10 环境与交付

| 环境 | 数据库 | 部署方式 |
|---|---|---|
| 本地开发 | SQLite（单文件，删了自动重建） | 前端 `vite dev :5173` 代理 → 后端 `:8088` |
| 生产（Jean） | PostgreSQL | Nginx + 两个 systemd 服务 |

**构建验证**：`mvn -DskipTests clean compile`（JDK 21）+ `npx vite build`，两者必过。

**部署脚本**：`deploy/deploy-jean.sh`（全栈）、`deploy/ontology/deploy-ontology.sh`（语义服务单独）。

---

# （四）安全性分析

## 4.1 总体结论

> **当前系统处于"无任何访问控制"的状态，且存在多个可被直接利用的高危入口。它目前只适合运行在完全可信的隔离内网中；在接入任何非受控网络之前，4.3 节的 P0 项必须全部整改。**

这不是危言耸听，而是可以逐条复核的事实：后端不存在任何认证过滤器、鉴权注解或 Spring Security 依赖（`grep -rn "SecurityFilterChain\|OncePerRequestFilter\|@PreAuthorize"` 全项目零命中），意味着**任何能访问到 8088 端口的人，都拥有系统的全部权限**——包括执行任意 SELECT、读取所有外部数据库的明文口令、通过服务端发起任意 HTTP 请求。

## 4.2 资产与威胁面

### 需要保护的资产

| 等级 | 资产 | 说明 |
|---|---|---|
| **高** | 外部数据源凭证 | 12 类业务库的地址、账号、口令；泄露 = 业务库沦陷 |
| **高** | 第三方 API 密钥 | DeepSeek / SiliconFlow 的 key，可被盗刷 |
| **高** | 外部接口鉴权配置 | 9 种鉴权的 token、client_secret、证书信息 |
| **中** | 本体元数据 | 业务模型是组织的知识资产，泄露可推断业务结构 |
| **中** | 物理表结构 | 表名、列名、类型——SQL 注入的地图 |
| **低** | 模拟实例数据 | 非真实业务数据 |

### 暴露面

| 入口 | 暴露程度 |
|---|---|
| `/api/**`（8088，34 个 Controller） | **完全匿名可访问** |
| `/api/ontology/**`（8089） | **完全匿名可访问** |
| 前端 SPA | 无登录页，直接进主界面 |
| 服务端出站请求 | 可由用户输入完全控制目标 URL |
| 主库 | 经两个原始 SQL 接口间接可读 |

## 4.3 风险清单

> 等级定义：**P0** = 上线前必须修复，当前状态下可被直接利用；**P1** = 有实际危害，需在正式交付前解决；**P2** = 应改进，风险相对可控。

---

### 🔴 P0-1　全站无认证、无授权

| 项 | 内容 |
|---|---|
| **证据** | 全项目无 `spring-boot-starter-security` 依赖；无 `HandlerInterceptor` / `OncePerRequestFilter` / `SecurityFilterChain` / `@PreAuthorize` 实现；`views/config/Security.vue` 为静态占位页，无对应后端 |
| **影响** | 任何能路由到服务端口的主体（含内网横向移动的攻击者、误连的测试机）拥有全部权限：读写全部本体、读取所有数据源明文口令、删除任意元数据 |
| **利用难度** | 极低——直接 `curl` 即可 |
| **建议** | ① 立即接入认证（JWT 或对接企业统一身份/CAS）；② 按《SRS》FR-SYS-06 落地 RBAC：角色 → 资源范围 → 操作权限；③ 在认证落地前，用 Nginx 层的 IP 白名单 + Basic Auth 做临时兜底；④ 引入登录态后须把会话外置（Redis），否则破坏 admin 的无状态性与水平扩展前提 |

---

### 🔴 P0-2　第三方 API 密钥硬编码并已提交到版本库

| 项 | 内容 |
|---|---|
| **证据** | `backend/bontolink-admin/src/main/resources/application.yml` 中 `bontolink.deepseek.api-key` 与 `bontolink.siliconflow.api-key` 为明文真实密钥；`git ls-files` 确认该文件**已被 Git 跟踪**；`.gitignore` 仅忽略 `.env`，未覆盖此文件 |
| **影响** | 密钥已进入版本历史，所有拿到仓库副本的人（含离职人员、外包、代码托管平台）均可提取并盗刷，产生直接费用损失；即使现在删除，历史提交中仍然存在 |
| **建议** | ① **立即到两个平台后台吊销并轮换这两个密钥**——这是最紧急的一步，删代码不能替代吊销；② 改为从环境变量注入 `${DEEPSEEK_API_KEY}`，配置文件只留占位符；③ 用 `git filter-repo` 或 BFG 清理历史（需协调所有已克隆方重新克隆）；④ 引入密钥扫描（gitleaks / trufflehog）进 CI，防止再次发生 |

---

### 🔴 P0-3　两个可执行任意 SQL 的匿名接口

| 项 | 内容 |
|---|---|
| **证据** | `DebugSqlController.java` → `POST /api/debug/query`，接收 `{sql}` 直接 `jdbcTemplate.queryForList(sql)`，唯一校验是 `upperSql.startsWith("SELECT")`；`DatabaseToolService.java:65` → `POST /api/tool/db/query` 同样只做 `startsWith("SELECT")` 检查。前端 `toolApi.dbQuery` 已把这个能力接到界面上 |
| **影响** | 可读取主库**全部**数据，包括 `sys_data_source` 表里所有外部业务库的明文口令、`ont_ext_data_source` 的鉴权配置。`startsWith("SELECT")` 不构成防护：`SELECT ... ; DROP TABLE ...`（取决于驱动的多语句开关）、`WITH x AS (...) SELECT`、PostgreSQL 的 `SELECT ... FROM pg_read_file(...)` 等均可绕过或造成额外危害 |
| **补充** | `DebugSqlController.getSchema()` 与 `DatabaseToolService.schema()` 用字符串拼接构造 `PRAGMA table_info(" + table + ")`，表名未做白名单校验 |
| **建议** | ① `DebugSqlController` **直接删除**，或用 `@Profile("dev")` 隔离且绝不打进生产包；② `/api/tool/db/query` 若必须保留（知识浏览器依赖），则加：强制鉴权 + 管理员角色 + 只读数据库账号 + 语句白名单/解析校验（用 JSqlParser 判定确为单条 SELECT）+ 强制 LIMIT + 查询超时 + 审计日志；③ schema 接口的表名改为对照 `information_schema` / `sqlite_master` 的白名单校验 |

---

### 🔴 P0-4　CORS 完全放开且允许携带凭证

| 项 | 内容 |
|---|---|
| **证据** | `CorsConfig.java`：`allowedOriginPatterns("*")` + `allowCredentials(true)` + 全部方法 + 全部头 |
| **影响** | 任意站点的 JS 都能带着用户凭证跨域调用本系统接口。**这条与 P0-1 组合起来尤其危险**：一旦将来加了基于 Cookie 的登录态，任何被诱导访问的恶意页面都能以管理员身份操作本系统（CSRF 的加强版，因为 CORS 还允许读取响应） |
| **建议** | 改为显式白名单：`allowedOrigins("https://内网域名")`；开发环境单独 profile 放开 localhost。`allowCredentials(true)` 与通配 origin 本就是被规范禁止的组合，Spring 用 `allowedOriginPatterns` 绕过了这个限制，等于主动放弃了浏览器的保护 |

---

### 🟠 P1-5　数据源凭证明文存储且回传前端

| 项 | 内容 |
|---|---|
| **证据** | `SysDataSourceMapper.java:17,21` 为 `SELECT * FROM sys_data_source`，`password` 列随详情接口一并返回前端；写入侧 `insert`/`update` 直接存明文；外部数据源的 `client_secret`、`token_value` 同理存在 `ont_ext_data_source` 的配置 JSON 中 |
| **影响** | ① 数据库文件/备份泄露 = 所有业务库口令泄露；② 口令出现在前端内存、浏览器开发者工具的网络面板、以及任何中间代理的日志里；③ 与 P0-3 组合，一条 SELECT 即可批量收割 |
| **建议** | ① 存储侧用 KMS / Jasypt 加密，或接入密钥托管服务；② 读接口**永不返回**密码字段，改为返回掩码 `******`；更新时若前端传回掩码则保留原值；③ 明确 Mapper 的 `SELECT` 列清单，禁止 `SELECT *` |

---

### 🟠 P1-6　服务端请求伪造（SSRF）

| 项 | 内容 |
|---|---|
| **证据** | `POST /api/ext-datasource/interfaces/{apiId}/send` 会以服务端身份请求用户填写的任意 URL；动作类型的 Webhook 规则同样可配任意 URL；OAuth2 的 `token_url` 亦由用户填写（`ExtAuthService.java:107-127`） |
| **影响** | 攻击者可让服务器探测内网（端口扫描）、访问仅内网可达的管理接口、读取云厂商元数据服务（`169.254.169.254` → 窃取实例角色临时凭证）、经 `file://` 等协议读本地文件（取决于 HttpClient 配置） |
| **建议** | ① 出站 URL 白名单（域名/网段），或至少黑名单屏蔽私有网段 `10/8`、`172.16/12`、`192.168/16`、`127/8`、`169.254/16`；② 只允许 `http`/`https` 协议；③ 禁用或限制重定向跟随（防绕过）；④ DNS 解析后再校验 IP（防 DNS rebinding）；⑤ 出站流量走独立的受限网络出口 |

---

### 🟠 P1-7　自定义 SVG 图标未消毒导致存储型 XSS

| 项 | 内容 |
|---|---|
| **证据** | `IconPickerField.vue:365-389` 的 `parseSvgText()` 仅做 fill/stroke 颜色归一，**不过滤 `<script>`、`on*` 事件属性、`<foreignObject>`**；`lib/bl.js:398` 把 `custom.content` 原样插入 `<svg>` 模板；全系统图标一律经 `v-html` 渲染。后端 `IconLibService` 亦无消毒逻辑 |
| **影响** | 上传一个含 `<image href=x onerror="...">` 的 SVG 即可植入持久化脚本。该图标会出现在分类树、对象类型列表、图谱、抽屉等几乎所有页面，**任何打开这些页面的用户都会中招**。结合 P0-1（无鉴权），攻击者可直接调用图标上传接口而无需任何账号 |
| **建议** | ① 服务端消毒（权威侧）：用 OWASP Java HTML Sanitizer 或 SVG 专用白名单，只保留 `path/circle/rect/g/polygon` 等安全标签与几何属性；② 前端 `parseSvgText` 同步剥离 `on*` 属性、`<script>`、`<foreignObject>`、`href`/`xlink:href` 中的 `javascript:`；③ 更彻底的方案：图标以 `<img src="data:image/svg+xml,...">` 渲染而非内联，浏览器会隔离其脚本执行；④ 加 CSP 头作为纵深防御 |

---

### 🟠 P1-8　枚举同步的过滤条件直接拼入 SQL

| 项 | 内容 |
|---|---|
| **证据** | `EnumSyncService.java:245`：`if (!filter.isBlank()) sql.append(" WHERE ").append(filter);`，`filter_sql` 来自用户在界面上的自由输入 |
| **影响** | 该 SQL 在**外部业务数据源**上执行。虽然当前配置者已是"全权限"用户（P0-1），但一旦 RBAC 落地，低权限的建模人员将能借此对生产业务库执行任意语句——这是一条越权通道 |
| **建议** | ① 改为结构化条件构造（字段 + 运算符 + 参数化值），不接受自由 SQL；② 若产品上确需自由 WHERE，则限定为只读数据库账号 + 语法解析校验（拒绝子查询、分号、注释符）+ 仅管理员可配 |

---

### 🟠 P1-9　无操作审计

| 项 | 内容 |
|---|---|
| **证据** | 全库无审计表；仅有应用日志，且不记录"谁"（因为没有身份体系） |
| **影响** | 本体是组织的知识资产，一旦被误删或恶意篡改，**无法回溯是谁、什么时候、改了什么**，也无法据此恢复 |
| **建议** | 认证落地后同步建审计表：操作人 / 时间 / 资源类型 / 资源 ID / 动作 / 变更前后快照（JSON diff）。至少覆盖删除与状态变更这两类不可逆操作 |

---

### 🟠 P1-10　缺少请求限流与体积限制

| 项 | 内容 |
|---|---|
| **证据** | 无限流组件；接口管理器的文件上传以 dataURL 形式塞进 JSON body（前端限 10MB，`apiModel.js` 的 `MAX_UPLOAD_BYTES`），**后端未见对应限制**；`/api/tool/db/query`、向量检索等重接口无频率约束 |
| **影响** | 单个客户端即可通过大 body 或高频重查询打满内存/CPU，造成拒绝服务 |
| **建议** | ① Nginx 层 `client_max_body_size` + 限流；② Spring 层配 `spring.servlet.multipart.max-request-size`；③ 重接口（SQL 查询、向量检索、同步执行）加令牌桶限流与并发上限 |

---

### 🟡 P2-11　错误信息回显内部细节

| 项 | 内容 |
|---|---|
| **证据** | `GlobalExceptionHandler.unknown()` 把 `e.getMessage()` 直接放进响应；`DebugSqlController` 返回 `"SQL 执行失败: " + e.getMessage()` |
| **影响** | 向调用方泄露表名、列名、SQL 片段、驱动版本，为进一步攻击提供信息 |
| **建议** | 生产环境返回通用文案 + 错误追踪 ID，详细堆栈只进服务端日志 |

---

### 🟡 P2-12　无并发控制，编辑互相覆盖

| 项 | 内容 |
|---|---|
| **证据** | 子表采用"先删后插"整体覆盖；主表更新无版本号/乐观锁 |
| **影响** | 两人同时编辑同一个对象类型的属性表，后保存者静默覆盖前者的全部改动，且无任何提示 |
| **建议** | 主表加 `version` 或 `updated_at` 乐观锁，保存时比对，冲突则提示"该记录已被他人修改，请刷新" |

---

### 🟡 P2-13　日志级别过高可能记录敏感数据

| 项 | 内容 |
|---|---|
| **证据** | `application.yml`：`com.beiktech.bontolink: DEBUG` |
| **影响** | DEBUG 级别下可能打印含口令、token 的请求参数到日志文件，日志的访问控制通常弱于数据库 |
| **建议** | 生产降为 INFO；对凭证类字段做日志脱敏 |

---

### 🟡 P2-14　依赖供应链无扫描

| 项 | 内容 |
|---|---|
| **证据** | 无 SCA / 依赖漏洞扫描环节；Milvus SDK 2.2.16、Jena 5.2.0 等第三方库版本固定但无跟踪机制 |
| **建议** | CI 接入 `mvn dependency-check` 或 Snyk / Trivy，定期扫描并跟进 CVE |

---

## 4.4 风险汇总

| 等级 | 编号 | 风险 | 核心影响 |
|---|---|---|---|
| 🔴 P0 | 1 | 无认证无授权 | 全系统任意访问 |
| 🔴 P0 | 2 | 密钥硬编码入库 | 第三方账号被盗刷 |
| 🔴 P0 | 3 | 任意 SQL 接口 | 主库全量读取（含所有业务库口令） |
| 🔴 P0 | 4 | CORS 全开 + 凭证 | 跨站以用户身份操作 |
| 🟠 P1 | 5 | 凭证明文存储回传 | 业务库口令泄露 |
| 🟠 P1 | 6 | SSRF | 内网探测、云元数据窃取 |
| 🟠 P1 | 7 | SVG 存储型 XSS | 全站持久化脚本注入 |
| 🟠 P1 | 8 | 同步 SQL 拼接 | 对外部业务库越权执行 |
| 🟠 P1 | 9 | 无审计 | 篡改无法追溯 |
| 🟠 P1 | 10 | 无限流 | 拒绝服务 |
| 🟡 P2 | 11 | 错误信息泄露 | 信息收集 |
| 🟡 P2 | 12 | 无并发控制 | 数据静默覆盖 |
| 🟡 P2 | 13 | 日志敏感数据 | 凭证泄露 |
| 🟡 P2 | 14 | 供应链无扫描 | 已知漏洞长期潜伏 |

## 4.5 整改路线

### 第一阶段：止血（建议 1~2 周，上线前必须完成）

| 序 | 动作 | 对应风险 |
|---|---|---|
| 1 | **吊销并轮换 DeepSeek / SiliconFlow 密钥**，改环境变量注入 | P0-2 |
| 2 | 删除 `DebugSqlController`；`/api/tool/db/query` 加管理员鉴权 + 只读账号 + 语句校验 | P0-3 |
| 3 | CORS 改显式白名单 | P0-4 |
| 4 | 接入认证（JWT 或企业 SSO），全部 `/api/**` 默认拒绝 | P0-1 |
| 5 | 数据源密码读接口掩码化，Mapper 去掉 `SELECT *` | P1-5 |
| 6 | SVG 上传服务端消毒 | P1-7 |
| 7 | Nginx 层加 IP 白名单与请求体大小限制（临时兜底） | P0-1 / P1-10 |

### 第二阶段：加固（交付前）

| 序 | 动作 | 对应风险 |
|---|---|---|
| 8 | RBAC 落地：角色 → 资源范围 → 操作权限（对齐 FR-SYS-06） | P0-1 |
| 9 | 凭证加密存储（KMS / Jasypt） | P1-5 |
| 10 | 出站 URL 白名单 + 私有网段屏蔽 | P1-6 |
| 11 | 枚举同步过滤条件改结构化构造 | P1-8 |
| 12 | 操作审计表 + 变更快照 | P1-9 |
| 13 | 接口限流与并发上限 | P1-10 |
| 14 | 生产日志降级 + 敏感字段脱敏 + 错误信息通用化 | P2-11 / P2-13 |
| 15 | 清理 Git 历史中的密钥 | P0-2 |

### 第三阶段：体系化（演进期）

| 序 | 动作 |
|---|---|
| 16 | 乐观锁与并发冲突提示 |
| 17 | CI 接入密钥扫描（gitleaks）+ 依赖漏洞扫描（SCA） |
| 18 | CSP / HSTS / X-Frame-Options 等安全响应头 |
| 19 | 全链路 HTTPS，内部服务间 mTLS |
| 20 | 定期渗透测试与安全评审进入发布流程 |

## 4.6 安全基线（长期规范）

新增功能时对照检查：

| # | 基线 |
|---|---|
| 1 | 新增接口默认**拒绝匿名访问**，需显式声明所需角色 |
| 2 | 任何用户可控内容进入 SQL，一律参数化；确需动态片段则走白名单枚举 |
| 3 | 任何用户可控内容进入 `v-html` / `innerHTML`，一律先消毒 |
| 4 | 任何用户可控 URL 参与服务端出站请求，一律先校验目标 |
| 5 | 凭证类字段：**存加密、传掩码、日志脱敏、不入版本库** |
| 6 | 删除与状态变更类操作必须留审计 |
| 7 | 错误响应不含内部实现细节 |
| 8 | 新增配置项若含密钥，必须走环境变量，配置文件只留占位符 |

---

## 附：文档关系

| 文档 | 位置 |
|---|---|
| 需求规格说明书 | `需求规格说明书.md` |
| 使用帮助 | `使用帮助.html` |
| UI 与交互规范 | `UI规范.md` |
| 部署方案 | `deploy/README.md` |
| 后端模块说明 | `backend/README.md`、`backend/模块化改造总结.md` |
| api_name 编码规范 | `API_NAME_CODING_STANDARD.md`、`API_NAME_CHEATSHEET.md` |
| 专题方案 | `docs/`（Jena 与向量库集成、本体查询转 SQL、语义向量化与同义词生成） |

| 版本 | 日期 | 内容 |
|---|---|---|
| V1.0 | 2026-08-03 | 初版。整体框架 / 功能模块 / 技术方案 / 安全性分析四章，含 14 项风险与三阶段整改路线 |
