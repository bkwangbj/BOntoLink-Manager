# 2026-07-24

## 项目不足全面排查
对 BOntoLink-Manager 做了全量只读审查（backend / backend-old / frontend / bk-analysis-maker-vue3 / 工程化），产出报告：`docs/项目不足排查报告-2026-07-24.md`。

关键结论（已人工验证）：
- 严重安全：DebugSqlController 无鉴权任意 SQL 执行；EnumSyncService 用户 SQL 直达外部数据源；全系统零认证 + CORS `*` 带凭据；application-postgresql.yml 明文 postgres/postgres（dev.beiktech.com:9523）；前端 BL.icon/confirm/Instances 三处 XSS；maker 库 10+ 处 eval/new Function。
- 严重工程：Flyway V3/V4 版本冲突（工作区必崩，新克隆缺 7 张语义表）；backend-old 两个 25MB .db 被 git 跟踪；frontend/package-lock.json 被 .gitignore 忽略未入库；README 与实际四模块双服务架构严重不符；ontology 模块无法独立运行。
- 中等：零测试、零参数校验、零 CI/CD、无 LICENSE、依赖过旧（vite5/monaco0.33/g6v4）、Category.vue 3079 行等超长文件、两套 UI 框架并存。

## 功能设计专项排查
产出 `docs/功能设计问题报告-2026-07-24.md`。核心结论：
- 最严重：mock 污染生产——GraphController.ontology() 用 Random(42) 向真实响应编造 eq/dis/union/link 假关系边（已验证）；/api/instance/* 全走 InstanceMockService；AI 助手 setTimeout 假对话（已验证）。
- 核心模型缺陷：ont_class_link（图谱读，无 INSERT API）与 ont_link_types（CRUD 写）双模型互不同步——界面建的链接类型永远不进图谱。
- ontology(8089) 是死服务（已验证）：模块激活 sqlite profile，但 application-sqlite.yml 仅存在于 admin 模块且 ontology 不依赖 admin → classpath 无数据源配置，按现状无法启动连库；前端零调用、Jena 零使用。项目方认可此结论（双服务架构成本零收益）。
- 语义扩充模块（同义词/领域术语 CRUD+匹配）：**非"无需求依据自建"**——项目方澄清其为新增功能、需求文档尚未同步、���能仍在测试中。其 admin 写/ontology 读/前端无入口/PG 下无表属于测试阶段未完成项，待稳定后补文档对齐。报告已据该澄清修订（见功能设计报告第二节"修订说明"与第六节第 2 点）。
- 需求缺口：OWL 语义层、版本快照/回滚、OWL 导出、实例真实查询、动作类型 CRUD、数据源监控、属性格式化生效链路、函数模块、权限、导入导出均未实现。
- 前端：全局领域选择器 selectedDomains 无任何页面消费；新建向导步骤条造假（步骤2不存在）；死链 /resources/actions；无 404 路由；死代码文件 5 个（Graph.vue/InstanceCharts.vue/TabPropsCanvas.vue/LeftGroupTree.vue/AiFab.vue）。
- 建议优先：加 demo-mode 开关隔离 mock → 修死链+打通全局筛选 → 合并双链接模型 → 决策 ontology 去留。

## 综合诊断书生成
将两份专题报告整合为统一交付物：`docs/BOntoLink-Manager项目诊断与整改建议书-2026-07-24.md`。含问题总览统计表（严重 30 / 中等 21 / 轻微 17）、严重项详解（安全 7 + mock 污染 7 + 架构引爆 5）、中等精选、需求缺口表（R1-R12）、四阶段整改路线图（P0 安全信任 1 周内 → P1 架构解耦 2-4 周 → P2 功能闭环 1-2 月 → P3 工程化持续）。两份底稿保留为分项证据。

## 项目事实备忘
- 实际架构：backend 为 Maven 四模块聚合（bontolink-data/base/admin/ontology），admin 8088 + ontology 8089，Flyway 迁移；README.md 内容已过时（描述单模块）。
